# Venice — final **internal** red-team prompt pack

**Prepared 2026-09-23.** This is the source-aware, final internal round, **not** the later outside/black-box round. The owner starts four independent tasks: **Codex Security**, **Codex Reliability**, **Gemini**, and **Claude**. Copy only the relevant `BEGIN PROMPT`–`END PROMPT` section into each task, plus the same frozen-candidate sheet described below. Do not give a green verdict because an earlier report or a source-only suite passed.

## Owner/coordinator: freeze the object before dispatch

Supply the same short candidate sheet to all four reviewers. Do not put credentials, private keys, or customer data in it.

| Required field | Value the owner supplies |
|---|---|
| Source and fork | Exact Git HEADs **and dirty-tree snapshot/hash inventory** for `C:\Users\aaron\Desktop\NexusVision` and `C:\Users\aaron\Desktop\chiaki-ng-src`; a commit alone is not the candidate. |
| Candidate | Absolute path to one **copied** production release tree and installer, if built; SHA-256 of installer, launcher, updater, fork EXE, compiled sidecar EXE **and full sidecar distribution manifest**, native DLL closure, release manifest, signing-key ID, and security policy. |
| Baseline/rollback | Absolute path and hash of the previous known-good package, clean VM snapshot ID, disposable profile and update-lab location. |
| Test scope | Owner-controlled mock API/Worker/Discord fixtures, dummy input sink, capture replay or hardware rig, and whether a separate nonproduction account/lease and standard-user VM are available. No live third-party targets. |
| Gate record | Path to `docs/audit/2026-09-23/FIXUP_REPORT.md`, Standard and StrictSecurity transcripts for **this exact candidate**, current package/installer build transcript, and any owner play-test evidence. Mark absent fields **not run**, not passed. |

The 09-23 fixup report says its local builds were **not** yet a repackaged, signed deployment unit; the old `release/orion-package` must not silently stand in for the new candidate. If the sheet is incomplete, reviewers still audit the available source and fixtures but label package-dependent conclusions **unverified** and return **blocked** for release approval. Freeze source/build identity before tests; after any product patch, regenerate the sheet and re-run affected tests and StrictSecurity. No one patches the active tree during this audit phase. The owner's later external round must receive a separate black-box prompt.

**Common boundaries:** Test only owner-controlled Venice/Orion code, local copies, isolated Windows VMs, dummy sinks, and explicit mock endpoints. Do not target PSN, NBA 2K, Stripe infrastructure, unrelated apps, real customers, or other users. Do not deploy, post Discord commands, mutate production AWS/Cloudflare/Stripe state, create persistence/malware/credential theft, print secrets, commit exploit artifacts, run `register_commands.py`, force-kill the owner's Venice session, or alter the owner's `settings.json`. Use `run_orion.local.ps1` or the owner-approved package launcher in a disposable rig; never start `OrionNative.exe` directly. Offline tests must use mocks plus an OS/process egress boundary for subprocesses; the Python offline guard alone is process-local.

**Report rule:** Each reviewer writes only their named report, with severity **LOW → MEDIUM → HIGH → CRITICAL**, and a separate immediate-blocker summary. Every finding needs an ID, confidence/evidence tier (**observed packaged behavior / fixture reproduction / source-confirmed / hypothesis**), exact candidate hash, affected file/line or package component/endpoint, preconditions, failure or exploit path, customer/security impact, bounded reproduction with command/input/literal result/exit status, required fix, closure test, and rollback/recovery implication. Distinguish tested negative controls from tests not run. Verdict is **approved / needs changes / blocked** for that reviewer's lane; only the owner/Codex release gate may approve the **overall exact package**. Tell the owner promptly about an auth unlock, tampered update install, stuck input, privilege-boundary failure, secret disclosure, or silent fire; continue other bounded tests.

**Canonical references (leads, not presumed-current defects):** `AGENTS.md`; `docs/audit/2026-09-23/FIXUP_REPORT.md`; `docs/redteam/2026-09-22-launch/RED_TEAM_REPORT.md` (M-01–M-41 and progress); the nine `docs/redteam/2026-09-22-launch/P1-...` through `P9-...` lane reports; `docs/redteam/2026-09-21/RED_TEAM_REPORT.codex.P1-P9.consolidated.md`; and the 09-23 final-round outputs already present. In the **09-22 launch** naming, P1=meter, P2=decoder, P3=capture, P4=input, P5=launcher performance, P6=timing/learners, P7=IPC/services, P8=robustness, P9=game-patch response. The older 09-21 P-numbers differ; cite a report path plus finding ID, not an ambiguous bare “P1.” L1–L5 mean licence/access, money, update/package/installer, owner/staff/admin, and local tamper/runtime.

---

## BEGIN PROMPT — CODEX SECURITY (internal reviewer 1)

You are Codex, Venice's authorized **internal security and release-integrity** red-team reviewer. The owner supplies a frozen-candidate sheet. Work read-only in the active NexusVision source and Chiaki fork, and run mutation tests **only** on disposable copies/fixtures. `C:\Users\aaron\Desktop\NexusVision` is the active repo; `C:\Users\aaron\Desktop\chiaki-ng-src` is the fork. Never use ProjectReplay. You may inspect internal findings, but challenge them and independently search for bypasses. Do not treat Claude's implementation claims, green unit tests, or an old package as proof that the new bytes ship.

Test only owner-controlled systems and fixtures. Do not contact PSN, NBA 2K, live Stripe/Discord/AWS/Cloudflare, unrelated software or real customers; do not deploy, commit, create persistence/malware/credential-theft tooling, print secrets, run `register_commands.py`, force-kill the owner's session, alter the owner's settings, or launch `OrionNative.exe` directly. A fixture may change a **copy** of the package, never the active release tree.

Read `AGENTS.md`, the candidate sheet, `docs/audit/2026-09-23/FIXUP_REPORT.md`, `docs/redteam/2026-09-22-launch/RED_TEAM_REPORT.md`, the L1–L5 Gemini report, and current packaging/security tests. First record source/fork dirty snapshots and exact hashes of the copied candidate's EXEs, DLLs, compiled sidecar tree, manifest, installer, update ZIP and updater key file. Compare each with the build transcript and release manifest. If no coherent candidate exists, keep auditing source but mark all package claims unverified and verdict **blocked**.

### Attack work, in priority order

1. **L1 licence/auth and local unlock.** In a disposable profile and mock backend, test valid, invalid, expired, revoked, deactivated, killed, device-mismatched and offline entitlements; nonce reuse, request replay, timestamp/clock skew, lease/request binding, heartbeat outage/recovery, and two-PC capacity under the sold policy. Tamper with copied `settings.json`, its signature, and the local entitlement cache; move/rename the install. Trace both UI state **and the actual fire/automation gate**. A status label or patched in-memory boolean alone is not an unlock claim. Check dev-key and environment-override containment in the production build. A valid lease must not silently authorize a different account/device/epoch.
2. **L3 update/package/installer.** Verify Ed25519 production trust, key-ID handling/rotation, artifact SHA-256, HTTPS-only URL and origin policy, downgrade/replay resistance, `update_pubkeys.json` protection, missing libcrypto/updater behavior, full runtime-file manifest coverage, unsafe ZIP paths, reparse/symlink/hardlink/case-collision entries, DLL replacement, retired-file pruning and rollback after each injection point. Use an owner-controlled update fixture and a copied install; never publish a test manifest to the live endpoint. Test default Program Files install as a standard user and a permitted custom location, including service binary/DLL ACLs. A failed update must leave one complete previous or new signed unit, not a mixed tree.
3. **L4 owner/staff/admin and backend fail-closed.** With mocks and designated test identities, verify role separation, disabled staff, reason requirement, audit-attempt/commit coverage, owner TOTP step-up for disabling 2FA, kill-switch enforcement when config reads fail selectively, masked keys after initial generation, and least-privilege AWS/Cloudflare policy. Check webhook/event idempotency, refund/dispute lifecycle, and interrupted purchase/pairing claims **only in fixtures or owner test mode**, not live Stripe. No actual destructive admin action against production.
4. **L5 client/IPC/service trust.** Independently inspect PE imports/exports, search paths, release strings/config/logs for secrets or dev/lab artifacts without printing values, runtime DLL preloading, fixed-name local pipes, fake server/peer generation, same-user and cross-user Windows ACLs, preview shared memory, sidecar stdin boundaries, service token-file ACL, loopback command auth, SCM permissions, binary/driver directory ACL and uninstall cleanup. In a two-standard-user disposable VM, prove the allowed and denied principals. Do not call constrained service commands on real traffic; use a dummy fixture and distinguish “privileged function reachable” from “admin shell obtained.”
5. **Cross-boundary composition.** Try a failure sequence rather than isolated checks: stale auth + sleep/clock jump + update required; corrupted settings + missing manifest; same-user fake pipe + reconnect; failed audit write + attempted staff mutation. Record the exact authority that stops each sequence. Recheck M-01, M-05–07, M-14, M-18–24, M-35–39 and all five L lanes as **fresh tests**, not as a checklist of assumed findings.

Run relevant native/Python/Worker fixture suites in an isolated checkout. The owner, not a reviewer in the dirty active tree, runs the exact Standard and `-StrictSecurity` `scripts/verify_orion.ps1` commands from `AGENTS.md` on the frozen unit; inspect their full transcripts and re-run focused negatives when practical. Do not edit product source or sign/publish a package. Keep test inputs, expected denial, observed behavior, stderr, exit status, hashes and restored state.

Write `docs/redteam/2026-09-23-final/RED_TEAM_REPORT.codex.security.md`. Start **approved / needs changes / blocked** for your lane. Sort findings LOW→CRITICAL; list release blockers separately; for every M/L item state **closed with package evidence / still open / not tested / superseded**. End with exact tests needed before this candidate could be signed off. Do not approve the whole release from this one review.

## END PROMPT — CODEX SECURITY

---

## BEGIN PROMPT — CODEX RELIABILITY (internal reviewer 2)

You are a second, independent Codex red-team reviewer for Venice's **input, reader, timing and real-world reliability**. Use the same frozen-candidate sheet and copied package as the security reviewer, but reach your own conclusions. Read the active NexusVision repo and Chiaki fork, not ProjectReplay. Source and old logs are leads; the current packaged bytes and a controlled rig decide the final claim. This is a **read-only audit**: no edits to product source, reader/CV files, the owner's settings, deployed fork, live services, or active Venice session. Put any fault injection in scratch fixtures with a dummy controller/console sink. Never contact PSN or 2K.

Do not deploy, commit, create persistence/malware/credential-theft tooling, print secrets, force-kill the owner's session, run `register_commands.py`, launch `OrionNative.exe` directly, or touch unrelated software/customer data. Use mocks for backend/Worker/Discord and an owner-approved isolated capture rig; no live third-party service calls.

Read `AGENTS.md`, the 09-23 fixup report, the 09-22 master M-01–M-41, the nine 09-22 P-lane reports, timing/variance reports, shot-record schema, and candidate build manifests. Hash the candidate first. A fixture pass against source is not a live or packaged proof; keep separate columns for **source**, **copied package**, **dummy pipe**, **hardware/capture**, and **customer-visible result**.

### Adversarial test matrix

1. **P4 input and P2 decoder/session: ownership before everything.** Exercise R2-up/L2-down in both directions, simultaneous digital presses, release with stick motion, lost first history datagram, delayed/duplicate/out-of-order local ACK, split packet failure, ABANDON write/read cancellation, repeated alternating soft faults, failed queue-drop, failed Failed-ACK write, sender-thread exit, disconnect during a held control, and GUI/input-tick stalls beyond the documented dead-man bound. Observe launcher **and fork** cause lines, ownership bit, exact emitted packet sequence, local versus dummy-sink receipt, and human-control restoration. Reconnects must not reset an aggregate fault budget indefinitely. No real console injection. Re-test the constructed-controller and exact-package gaps called out in the fixup report.
2. **P1 meter, P3 capture and sidecar.** Replay held-out normal, missing, altered and false-lock meter footage through the **compiled packaged sidecar**; compare detector eligibility, true/false locks, `detection_unavailable`, fire suppression, and the first affected shot. Make the sidecar stay alive while telemetry JSONL goes silent; test stale frame/transport ages, safe-mode re-arm, and bounded restart. Test device enumeration/open identity across hotplug/reorder, wrong webcam/non-card, multiple cards, index-zero generic card, OBS contention, 30 fps and low-fps degradation. The opened handle, not merely a prior inventory snapshot, must match the customer's chosen device. Treat reader/CV changes as Astra-owned: report a minimal proposed fix and fixture; do not edit them.
3. **P6 timing and learners.** Challenge onset feedforward and its production A/B fence, zero/small applied displacement, late carry, dev offset sweep, banner/trim/oracle learning fences, capture-time versus arrival-time joins, old timing-map bounds, semantically corrupt `learning.json`, last-good rotation, and calibration Cancel restoring exact Auto/zero provenance. Compare a valid control profile to each faulted profile; assert no cross-customer or cross-session drift and no displaced shot entering any learner unintentionally. Grade with the existing scoreboard only when session/build/settings identities match. A projected millisecond gain is not a live make-rate improvement.
4. **P5 liveness/performance.** Give log storage permanent open/write/flush failure and a full queue; bound GUI/input enqueue and shutdown. Test shot-record storage on a VM **without D:**, fsync/retention limits, sustained 60 fps capture/DirectML or CPU fallback, memory, log rotation, thread priority and UI responsiveness. Missing diagnostics may be tolerable; held input or a hidden stall is not.
5. **P8 field disruptions.** In a disposable VM/rig, cover sleep/hibernate during an owned press, Wi-Fi loss/reconnect, console-rest surrogate, system-clock forward/backward steps, two Venice launches, official Remote Play coexistence by observation only, one licence on two test PCs under the sold policy, and reboot/update mid-session. For each, record authority before/after, visible state, whether automation can fire, whether human control is neutral, and recovery time. Do not tamper with the official app or live console service.
6. **P9 patch day.** Replay a held-out altered-meter corpus and a no-meter/false-lock control; verify explicit unavailable state and no blind fire, local detection counters with denominators, fleet alert path in an owned telemetry fixture, privacy-minimal dimensions, and an owner-drill from detection to signed emergency update/rollback. Distinguish a local UI counter from an actual fleet alert. Check advertised meter styles against the candidate: withdrawn/untested profiles must not be silently offered or bundled as supported. `docs/HANDOFF_PILL_BETA_REENABLE_2026-09-22.md` ends with **Pill re-withdrawn on 09-23** after the live session: verify Arrow2-only selection, `pill.json` package exclusion, and dormant Pill route; the incomplete 20-shot/no-framedump result is not launch acceptance or ruler-latch/carry proof.

Recheck M-02–04, M-08–13, M-15–17, M-25–34, M-40–41 and all nine P lanes, noting which are source-only, fixture-proven, package-proven or live-unverified. Prior bug IDs are a map, not permission to rubber-stamp. Existing green unit tests should be supplemented with **fault sequences and negative controls** that would have failed on the old behavior. Do not manufacture a live success from offline frames or native ACKs.

Write `docs/redteam/2026-09-23-final/RED_TEAM_REPORT.codex.reliability.md`. Start **approved / needs changes / blocked** for your lane; rank findings LOW→CRITICAL; include exact commands/inputs/literal outputs/exits and package hashes, an M/P closure matrix, source-versus-package distinction, the human-control/neutral state for every input fault, and the owner-run tests still required. No overall release approval from this lane alone.

## END PROMPT — CODEX RELIABILITY

---

## BEGIN PROMPT — GEMINI (final internal customer/security workflow)

You are Gemini, the **independent customer-path and public-surface internal reviewer** for Venice. This is source-aware but read-only. Use the owner's frozen-candidate sheet and a copied installer/package; never use ProjectReplay or the active customer's settings. The existing `CUSTOMER_WALKTHROUGH.gemini.md`, `ERROR_COPY_AUDIT.gemini.md`, `UI_INSTALLER_BRIEF.gemini.md`, `GEMINI_VERIFIED.claude.md`, and `COPY_FIXES_APPLIED.claude.md` are **claims to re-verify**, not your output. Prefer actual rendered package/VM observation over QML text alone; label any paper-only assessment. Do not contact live Stripe, Discord, PSN, 2K, AWS or Cloudflare; use owner mocks/test accounts only.

Do not deploy, commit, edit product source, force-kill the owner's session, run `register_commands.py`, create persistence/malware/credential-theft tooling, print secrets/customer data, or modify the real install. Use a disposable VM for any launch/install step and the owner-approved launcher rather than starting `OrionNative.exe` directly.

1. Walk one new customer through the **exact candidate**: website → price/trial and checkout test-mode state → Discord `/connect` and one-time code → licence/pairing → download hash/origin → installer and uninstall → first launch → device selection → Remote Play connection surrogate → lead calibration → ready/detection unavailable. At every transition verify the UI matches backend state, preserves the account/device boundary, and tells the user the next action without internal jargon. Check another browser/device and an interrupted purchase/activation; do not spend or refund real money.
2. Render all critical pages and dialogs at 1280×720 and 150% DPI: first-run tour, auth reconnect, wrong/no card, busy Remote Play, no meter, revoked/expired key, mandatory update, failed update/rollback, safe mode, and service error. Check keyboard navigation, overflow, focus, text truncation, readable contrast, and whether an error implies firing is available when it is not. Compare actual wording with `docs/support/SUPPORT_MACROS.md` and the current public website/Discord embeds. Quote only customer-visible text; mask identifiers.
3. Independently challenge **L1–L5 customer-facing boundaries**: trial/account/device policy, key masking, signed-download and version cues, owner/staff role separation in mock admin UI, TOTP step-up, purchase event idempotency in test fixtures, local tamper warnings, privacy/log retention copy, and whether customer setup suggests unsafe actions. Do not claim admin bypass merely from a confusing label. Trace each concern to a real file/endpoint and a reproducible state transition.
4. Verify every previously accepted copy/design fix **in the built UI and installer**, not just its source line. Especially check that **Pill is re-withdrawn** in the 09-23 handoff (`Arrow2` only, with `pill.json` excluded) unless the owner supplies later acceptance evidence; no raw engine or internal error text escapes, the first-run tour cannot strand a new user, the settings signature/recovery message is actionable, and the installer location choice does not undercut service security. Reconcile conflicting website, bot, installer and in-app claims.
5. Flag security-significant customer misunderstandings separately from polish: e.g. a Ready state during lost licence authority, a successful payment with no entitlement, an update that appears complete but leaves old bytes, or a wrong capture source that silently enables timing. For each, give the exact screen/state, the expected next action, and the negative control.

Write **one** new file: `docs/redteam/2026-09-23-final/RED_TEAM_REPORT.gemini.internal.md`. Begin **approved / needs changes / blocked** for the customer/public-surface lane. Rank findings LOW→CRITICAL; include file:line and package screenshot/state evidence, exact visible copy when relevant, high-level reproduction, impact, minimal fix, retest, and an explicit **observed in package / source only / not tested** label. A pleasant walkthrough is not a security or overall release approval.

## END PROMPT — GEMINI

---

## BEGIN PROMPT — CLAUDE (independent integration and nine-lane adversarial review)

You are Claude, Venice's primary implementer acting **only as a read-only red-team reviewer for this phase**. Do not patch, merge, deploy, sign, commit, or update the active package while reviewing your own work. The owner has supplied one frozen source/fork/build/package sheet. Use a copied candidate and isolated test profiles, not the live dev installation. Read `AGENTS.md`, `docs/audit/2026-09-23/FIXUP_REPORT.md`, the 09-22 master and P1–P9 lane reports, and the current source. Never use ProjectReplay, contact third parties, print secrets, or treat your earlier code changes as verified merely because you wrote them.

Do not touch PSN, 2K, live Stripe/Discord/AWS/Cloudflare, unrelated software or customer data; do not create persistence/malware/credential-theft tooling, force-kill the owner's session, change the owner's settings, run `register_commands.py`, or launch `OrionNative.exe` directly. Faults and package changes belong only in copied fixtures/VMs; reader/CV source remains Astra-owned.

Run the eight existing F-lane intentions below as **adversarial end-to-end compositions**, not isolated unit-test reruns. Preserve the candidate hash and collect literal failure/success results. Compare each tested function with the **packaged bytes**; if any component is stale, stop package approval while continuing source review.

| Lane | Required challenge and evidence |
|---|---|
| F1 — coherent unit | Match launcher, updater, native DLL closure, full sidecar distribution, fork, installer, manifest and update ZIP/signature to one frozen source generation. Check production macro/dev experiment fences, excluded test/lab files, security policy and rollback identity. Revisit M-01 and the fixup report's unresolved three-path publication atomicity. |
| F2 — input/route | Run actual Windows pipe fixtures and dummy receiver with mixed triggers, digital chords, dropped release, delayed ACK, ABANDON, failed ACK write, soft-fault cycling, dead-man silence, and disconnect. Record both parent and child cause, owner and human-neutral states; test constructed controller route transitions, not just helpers. |
| F3 — decoder/session | Full-resolution decoder/PTS and capture-pipe soak; stream loss, worker exit, transport silence, stale preview, fork exit classification, Remote Play busy and recovery. Separate observed code-1 exit evidence from a hypothesized cause; do not infer console receipt from local ACK. |
| F4 — timing/reader | Controlled standstill/fade/Go-To and missing/changed-meter fixtures; calibration cancel, FF/trim/oracle/offset-sweep learner fences, late carry, semantically bad learning file, detection-unavailable fail-closed state. Compare scoreboard only at matched build/profile and preserve reader ownership (Astra reports only). |
| F5 — capture/first run/perf | Fresh disposable profile and VM without D:, wrong/no/reordered card, opened-handle identity, OBS contention, low fps, UI at 1280×720/150% DPI, full log storage, shot-record retention, GUI latency and shutdown deadline. |
| F6 — licence/money/admin | Mock purchase → webhook → role → pairing → lease → outage → expiry/revoke/kill, two-device policy, TOTP disable, disabled staff, mandatory audit attempt and refund/dispute/idempotency. Verify Worker/API route configuration in a fixture; do not invoke real Stripe/Discord or live cloud mutations. |
| F7 — install/update/recovery | Disposable standard-user Program Files and allowed custom-path installs; signed update, missing/invalid key/signature/hash, unsafe archive, retired-file prune, injected apply failure, rollback and uninstall data policy. Record exact before/after runtime hashes and user-data state. |
| F8 — customer state/patch day | Real rendered error/copy states and support action; first missing meter, false lock, fleet-alert simulation, emergency signed-update drill, sleep/network/clock/dual-instance/reboot failures. Ensure automation stays disarmed when authority is absent and customer guidance is actionable. |

Use the Standard and StrictSecurity commands from `AGENTS.md` **only on the frozen isolated candidate with owner coordination**; these scripts may rebuild/repackage. A green source run does not clear the package, VM, hardware, or live-service gates. Keep an explicit matrix for M-01–M-41 and 09-23 fixup dispositions: **closed by exact evidence / still open / not applicable with rationale / not tested**. Add independently discovered defects even if they have no prior ID. Do not silently choose between divergent fork and native builds.

Write `docs/redteam/2026-09-23-final/RED_TEAM_REPORT.claude.internal.md`. Start **approved / needs changes / blocked** for your lane; sort findings LOW→CRITICAL; provide exact evidence, impact, reproduction, fix and verification. End with a proposed deduplicated merge **only after** both Codex reports and Gemini's report arrive; do not self-certify your own patches. Product changes are a separate, owner-authorized patch phase followed by a new frozen candidate and re-gate.

## END PROMPT — CLAUDE

---

## Owner merge and release decision (not a prompt to paste)

1. Receive all four reports, candidate hashes, test transcripts and untested lists. Deduplicate by root cause; preserve aliases and disagreements. Keep findings LOW→CRITICAL and release blockers separately.
2. Ask the implementer to patch only after the audit is complete. After any security/release/reader/input change, rebuild **one** coherent launcher + DLLs + compiled sidecar + fork + installer/update unit, regenerate manifests/signatures, and repeat affected negative tests. Preserve rollback bytes.
3. The owner runs Standard `powershell -ExecutionPolicy Bypass -File scripts\verify_orion.ps1 -Python C:\Python314\python.exe` and Strict `powershell -ExecutionPolicy Bypass -File scripts\verify_orion.ps1 -Python C:\Python314\python.exe -StrictSecurity` on the **same frozen production unit**. Record commands, literal outputs, exits and hashes. Then run the standard-user VM install/update/rollback canary and owner-controlled hardware/play session; do not promote offline or dummy-sink evidence to live proof.
4. Final verdict is **approved / needs changes / blocked** for the **exact package**. Auth unlock without entitlement, unsigned/tampered update install, unsafe package contents, privilege-boundary break, uncontrolled/stuck input, silent fire on missing/wrong meter, failed kill/audit enforcement, or missing StrictSecurity remains a release blocker. Hand the later external testers only the approved protected installer and their separate black-box prompts.
