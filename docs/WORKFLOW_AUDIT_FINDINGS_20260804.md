# Production-readiness audit findings - 2026-08-04

Recovered from workflow run `wf_1168657b-9ac`. The implementation phase was
stopped mid-flight and its code reverted; THESE FINDINGS ARE THE SURVIVING VALUE.
Every audit agent was instructed to challenge its brief rather than agree, and
several briefs were refuted - those refutations are the most important entries.

---

## agent `a55c2a3c74caa390c`

{
  "headline": "The shipping blocker is real but it is NOT the reader flags \u2014 it is the actuation lead: the dev rig's ORION_LEAD_FLOOR_MS/BIAS_MS force `leadOverrideFromEnv=true`, which makes `measuredLeadForActuationMs()` skip the entire shipped `userActuationLeadMs` branch, so the user has never once executed the lead path a customer runs; meanwhile 2 of the 4 reader flags named in the brief are inert (one is not implemented at all).",
  "findings": [
    {
      "title": "BLOCKER: dev rig and customer run different lead code paths; a fresh customer gets a raw, structurally-short authority",
      "evidence": "run_orion.local.ps1:107 sets ORION_LEAD_FLOOR_MS=290 and :117 sets ORION_LEAD_BIAS_MS=62. AutomationEngine.cpp:1405 and :1417 set `leadOverrideFromEnv = true` on either var; :1423 copies it to config_. measuredLeadForActuationMs() gates the shipped branch on `!config_.leadOverrideFromEnv` (AutomationEngine.cpp:9094), so with the env present the `return config_.userActuationLeadMs;` at :9098 is UNREACHABLE. Dev therefore computes lead = max(authority + 62, 290) (:9105-9112). Customer has neither var: bias defaults 0.0 (AutomationEngine.h:783), floor defaults 0.0 (AutomationEngine.h:755), so lead = raw authority verbatim unless userActuationLeadMs is in band. A fresh install has no settings.json \u2014 it is gitignored (.gitignore:113), git-untracked (`git ls-files` errors on it), and tools/package_orion_release.py contains zero references to it \u2014 so AppConfig.h:341 `actuationLeadMs = 0.0` applies and the :9098 branch is also skipped for want of a value. The launcher's own note (run_orion.local.ps1:111-115) records the estimator converging to fixed_ms=227.6 on this rig against a banner-verified good lead of ~300. This rig's settings.json does carry actuation_lead_ms=300 / actuation_lead_user_set=True \u2014 and the env override discards it, which is why the divergence has stayed invisible.",
      "fix": "AutomationEngine.cpp/.h are off-limits to me, so this is the required patch, not an applied one. Minimum ship gate: remove ORION_LEAD_FLOOR_MS and ORION_LEAD_BIAS_MS from run_orion.local.ps1 and re-run a live banner-graded batch so the rig exercises the `userActuationLeadMs` branch at :9098 that customers actually run. That is a launcher-only change and needs no rebuild. The permanent fix is the one AutomationEngine.h:780-782 already names: correct the label in latency_estimator.py to timestamp the shot LOCK instead of the meter's first upward crossing of f_stop, after which bias/floor are unnecessary. Do NOT default autonomousLeadBiasMs to 62/72 \u2014 AutomationEngine.h:773-777 documents that doing so broke 8 engine tests by pushing genuinely short-latency (~70ms) installs past the meter window into live_tip_deadline_missed, i.e. it converts a working install into a fail-closed one.",
      "severity": "blocker",
      "ships_broken": true,
      "refutes_premise": "The brief framed the blocker as reader flag defaults. The reader flags are the smaller problem: only 3 of them genuinely diverge and none is fail-closed-relevant. The lead path divergence is categorically worse because it is not a tuning delta, it is a different branch of measuredLeadForActuationMs() that the dev rig can never enter while the launcher is in use."
    },
    {
      "title": "ORION_READER_TRACK_H_ROBUST_GATE was never implemented \u2014 the launcher enables a flag no code reads, with a validation claim for a mechanism that does not exist",
      "evidence": "run_orion.local.ps1:226 sets it (and :271 repeats it commented) with the comment 'PROVEN safe to enable alone (51/51, byte-identical on healthy history...) recovers a poisoned fill-denominator in ~18 frames (offline poison A/B: under-read 13.7pp -> 1.1pp)'. The string occurs 5 times in run_orion.local.ps1 and 0 times in simple_meter_reader.py, remote_play_orchestrator.py, meter_detector.py, compressed_meter_reader.py, and 0 times under native_orion/, tools/, tests/. `git log --all -S \"TRACK_H_ROBUST_GATE\"` returns exactly one commit, f10aca84, and `git grep -l` at that commit shows only docs/ORION_DETECTION_EVIDENCE_DOSSIER.md, ORION_MASTER_FINDINGS_AND_PLAN.md, ORION_PRODUCTION_AUDIT_20260725.md, ORION_REVIEW_REQUEST.md. The dossier itself labels it at :88 'Proposed fix (flag-gated, default-OFF): ORION_READER_TRACK_H_ROBUST_GATE' and at :166 lists it as Wave-1 work item D3 still to be A/B'd. My empirical instantiation diff (customer env vs launcher env) produced 6 divergent SimpleMeterReader attributes and none of them corresponds to this flag, confirming it binds nothing.",
      "fix": "Delete run_orion.local.ps1:226 and the commented :271. Then decide separately whether D3 is still wanted; if so it must actually be built. Treat the '13.7pp -> 1.1pp' and '51/51' figures attached to it as unsourced \u2014 they describe a code path that has never existed, which is the same failure mode as the two phantom headline numbers already found tonight.",
      "severity": "major",
      "ships_broken": false,
      "refutes_premise": "Refutes the implicit premise that every ORION_READER_* line in the launcher is a real config delta. This one is inert in BOTH configurations. It also means the 'poisoned fill-denominator recovery' the user believes has been protecting live batches since 2026-07-22 has never run."
    },
    {
      "title": "ORION_READER_FAKELOCK_BREAK is a no-op on current defaults \u2014 it is not a divergence",
      "evidence": "The flag is default-OFF at simple_meter_reader.py:1166 and set to 1 at run_orion.local.ps1:288, so the attribute does diverge (my instantiation diff shows _fakelock_break False vs True). But `self._fakelock_break` appears at exactly three lines in the file: the definition :1166 and two call sites, :5893 `elif (self._fakelock_break or self._stalebreak) and _bk_det:` and :5916 `if ((self._fakelock_break or self._stalebreak) and _bk_det and not _grace_active`. Both are OR'd with `self._stalebreak`, which is default-ON at :1484 ('FLIPPED default-ON 2026-07-18 (A/B proven; 46/46 gates)') and which the launcher never sets. The grace gate at :5880 keys on `_stalebreak` alone. So with STALEBREAK true \u2014 true in dev and in a customer build alike \u2014 the static_fake_lock suppression is already armed and _fakelock_break contributes nothing to either config.",
      "fix": "Delete run_orion.local.ps1:288 (it advertises an A/B that cannot produce a difference). Then either delete the ORION_READER_FAKELOCK_BREAK flag and its two OR clauses outright, or flip its default to '1' purely to remove the misleading 'Live-A/B it before defaulting on' comment at :1165 \u2014 both are behaviour-preserving while _stalebreak stays on. Note the comment at :1161-1166 contradicts itself, opening '(default ON)' and then asserting 'DEFAULT-OFF'; whichever route is taken, fix that comment.",
      "severity": "minor",
      "ships_broken": false,
      "refutes_premise": "Directly refutes the brief's claim that ORION_READER_FAKELOCK_BREAK is one of four flags that are 'default-OFF in code while being ON in that launcher' in a way that matters. The default is indeed OFF and the launcher does set it ON, but the OR with default-ON _stalebreak makes dev and customer behaviourally identical here."
    },
    {
      "title": "The genuine reader divergence is exactly three flags / six attributes: ANCHOR (+N1/N3), PCTL_FILL, TRACK_H_CAP",
      "evidence": "Measured, not inferred: I stripped every ORION_* var, instantiated SimpleMeterReader(1920,1080), then re-instantiated with the launcher's exact reader env, and diffed all scalar attributes. Result \u2014 _anchor, _anchor_n1, _anchor_n3, _pctl_fill, _track_h_cap, _fakelock_break: False vs True; DIVERGENT ATTRS: 6, nothing else. Sources: _anchor simple_meter_reader.py:1373 default '0' with N1/N3 following the master at :1378-1379; _pctl_fill :1397 default '0'; _track_h_cap :1466 default '0'. Each genuinely gates code: _anchor_n1 at :5592, _anchor_n3 at :4337, _pctl_fill at :4378 (`if self._robust or self._pctl_fill:`, and _robust is default '0' at :1060 so PCTL_FILL is its only enabler), _track_h_cap at :4228 and :4284. The brief's second list \u2014 BOX_HUG :1152, MICRO_SCALE_GATE :1313, FILL_OVERFLOW :1323, GZ_TOP_EDGE :1328, GREEN_BAND_DIAG :1330 \u2014 are all default '1' and none is set by the launcher, so they are identical in both configs and are not divergences at all. MICRO_RED_DOM :1296 is not a boolean flag; it is `_inum(..., 40)`, a threshold.",
      "fix": "Per flag: TRACK_H_CAP \u2014 FLIP to default '1'. Its evidence is the strongest of the three (run_orion.local.ps1:235-238: on session_20260804_032333 per-shot horizon-flat bias sd 9.48 -> 2.39 ms, worst shot 48.0 -> 4.8 ms, out-of-family frames 10 -> 0, detection byte-identical over 15,000 frames, median fill delta exactly 0.0000 pp with only 5.6% of frames touched, all in the bias-removal direction), and its stated reason for staying off is pure process debt, which I verified below. ANCHOR and PCTL_FILL \u2014 KEEP default-OFF for now: their only cited support is run_orion.local.ps1:214-216 ('offline-proven gate-safe'), which is exactly the class of evidence that finding 5 shows cannot speak about production. Grade them in a live batch first. Do NOT flip all three together; TRACK_H_CAP is byte-identical on detection and the other two are not.",
      "severity": "major",
      "ships_broken": true,
      "refutes_premise": "Narrows the brief's four-flag claim to three, and disproves the six-flag 'default-ON' list as a source of divergence entirely \u2014 those six are the same in both builds."
    },
    {
      "title": "TRACK_H_CAP's default-OFF justification is confirmed process debt: every gate session's framedump is gone, and the gates would not test production anyway",
      "evidence": "run_orion.local.ps1:239-240 justifies default-OFF as 'run_gates can validate 0 of its 6 sessions on this clone (those framedumps were deleted mid-session)'. Verified: tools/regression/replay_gates.py:444-449 defines SESSIONS as five entries (session_20260707_135309, _175017, _121446, session_20260706_190737, session_20260704_210801 \u2014 five, not six), and `ls logs/diagnostics/framedump/` returns exactly one directory, session_20260804_091119, which is none of them. run_gates.py:67 prints 'SKIP (framedump absent)' for each, so a full run validates zero sessions. Independently, the gates could not have cleared this flag for production even with the framedumps present: replay_gates.py:111 and :183 both construct `SimpleMeterReader(W, H)` positionally, while production constructs `SimpleMeterReader(cfg=det_config, require_gameplay_eligibility=True)` at remote_play_orchestrator.py:1358. simple_meter_reader.py:1169-1179 documents exactly what that gap already cost: the fake-lock breaker 'WAS UNREACHABLE IN PRODUCTION' and 'The breaker's own unit tests never caught this because they construct SimpleMeterReader(W, H) WITHOUT require_gameplay_eligibility, the one configuration production never uses.'",
      "fix": "Two separable actions. (1) Unblock the flip: capture one fresh `-Framedump` session and re-baseline the floors, or accept the 15,000-frame byte-identical result on session_20260804_032333 as sufficient and flip simple_meter_reader.py:1466 to '1'. (2) Fix the blindness permanently: change replay_gates.py:111 and :183 to `SimpleMeterReader(W, H, require_gameplay_eligibility=True)` and drive the arm state through set_shot_state (already threaded at :121), so a green gate run becomes evidence about production instead of about a configuration production never uses.",
      "severity": "major",
      "ships_broken": false
    }
  ],
  "confidence_notes": "DIRECT ANSWER TO THE FINAL QUESTION, plainly: yes, the only mechanism is a gitignored dev script. installer/orion.iss contains no Environment, SetEnv, or Registry directives (grep for all four returns only two unrelated comment lines). The only other launcher, \"Launch Orion.local.bat\", is itself gitignored (.gitignore:201) and does nothing but invoke run_orion.local.ps1. tools/package_orion_release.py never writes settings.json or any env file. settings.json is gitignored (.gitignore:113) and git-untracked. There is no bundled settings file and no default profile. A customer build inherits ZERO ORION_* variables.\n\nWHAT I ALSO ESTABLISHED, that shrinks the problem: several launcher lines are redundant rather than divergent, because the C++ already forces them for everyone. OrionAppController.cpp:1522-1531 defines `envDefaultOn`, which qputenv's \"1\" unless explicitly set to 0/false/no/off, and it is applied to ORION_INPUT_HOOK (:1539), ORION_FRAME_PIPE (:1559), ORION_FREEZE_CAL (:1567), ORION_GREEN_SELF_GRADE (:1575), ORION_AUTONOMOUS_VISION (:1584, plus a pre-syncBackendConfig force at :1494), ORION_QML_RENDER (:1589) and ORION_PREVIEW_ASYNC (:1601). RemotePlaySession.cpp:2149 hands the sidecar `QProcessEnvironment::systemEnvironment()`, so those qputenv'd values reach Python. Hence launcher lines 160, 299, 343 and 344 are no-ops. ORION_GREEN_SELF_GRADE deserves a note because it LOOKS divergent (simple_meter_reader.py:1703 defaults it '0') but is not: the native forces it on before the sidecar spawns, in both builds.\n\nSEVEN MORE LAUNCHER LINES ARE DEAD ON THE SHIPPED PATH and I did not raise them to findings only because they are inert in both configs: RemotePlaySession.cpp:2178 unconditionally inserts ORION_SIMPLE_READER=1 and remote_play_orchestrator.py:1073/:1319 already default it to '1', so the reader is always selected; SimpleMeterReader has 0 occurrences of `_locator` and 0 of `meter_box_kalman`/`METER_TRACK`. That makes ORION_METER_LOCATOR/_MODEL/_IMGSZ/_CONF/_EVERY (run_orion.local.ps1:197-201), ORION_LOC_ROI (:207) and ORION_METER_TRACK (:315) inert \u2014 they configure only the retired legacy chain at remote_play_orchestrator.py:1365-1384. ORION_POSE_CAMERA_ANCHOR (:150) is likewise inert: pose_timing is constructed only under `if config.no_meter_enabled` (remote_play_orchestrator.py:1486) and settings.json has no_meter.enabled = false. Recommend deleting all nine lines from the launcher so it stops implying those subsystems are under test.\n\nONE FURTHER SHIP-VS-DEV GAP I confirmed but did not rank as a finding because it is security, not timing: the dev rig never exercises license activation. run_orion.local.ps1:130/136 set ORION_LICENSE_KEY and ORION_AUTO_UNLOCK_LOCAL; OrionAppController.cpp:3397 wraps the handler in `#ifndef ORION_PRODUCTION_BUILD` and :3401-3404 sets `authenticated_ = true; licenseState_ = \"Local Dev\"`, while localDevAllowed() (:7555-7563) returns false outright under ORION_PRODUCTION_BUILD and returns true on this rig merely because native_orion/CMakeLists.txt exists. native_orion/build/CMakeCache.txt confirms ORION_PRODUCTION:BOOL=OFF, and tools/package_orion_release.py:811-816 refuses to package unless it is ON, so the shipped binary is compiled differently at 20+ #ifdef sites. The real activation path has no live coverage from this rig.\n\nWHAT I COULD NOT SETTLE. First, the auto-seed that a customer relies on in place of the env override is `travelPp / velocity` where travelPp = peakFillPct - fillAtReleasePct (AutomationEngine.cpp:10972-10976) \u2014 i.e. the brief's dead metric #1. I am flagging rather than asserting because it collides with the user's own memory note calling travel_pp the only validated lead measure. The suspicious datum is in the launcher itself (run_orion.local.ps1:100-104): configured lead 302/295/284 produced travel_pp of 302/295/284. A 1:1 identity between the knob and the \"measurement\" is the signature of a quantity that returns its input rather than measuring the plant \u2014 a true end-to-end latency would have stayed constant while the lead moved. To be explicit about non-circularity: I am not deriving any timing relationship here, I am reporting an identity between a configured input and a reported output, which is evidence against that output's independence. Settling it needs one deliberate probe \u2014 hold the lead fixed and change nothing else across two sessions, then check whether travel_pp/velocity moves; if it tracks configured lead again, the shipped seed cannot converge and simply latches whatever lead the first 12 landings ran at.\n\nSecond, I did not build or run tests. Nothing I did writes to any file under native_orion/src or tests/, and I made no repo edits at all; every fix above is stated as a required patch. Third, I did not quantify the customer-side timing cost of ANCHOR/PCTL_FILL being off, because the only available evidence for them is offline gate runs and finding 5 shows those gates do not describe production. Fourth, the HEAD in my brief (2d610b61) was stale \u2014 the actual HEAD is e1c5c2e1, two commits later, so line numbers in AutomationEngine.cpp have shifted from the brief's (it cited travelPp at :10702; it is now :10972)."
}

---

## agent `a15f51c9b5351d70a`

{
  "headline": "A real court round trip is not obtainable and is not worth obtaining \u2014 the sidecar's court lock is structurally unreachable in every build (both entry points are dead code), and the brief's premise is wrong: the Chiaki fork's q.rtt is the PC\u2194PS5 remote-play RTT, not a PS5\u2192court RTT; the only in-loop network number that exists (Senkusha, measured 0.367\u20130.419 ms over 6 sessions) proves the network leg is ~0.5% of the ~75 ms actuation loop.",
  "findings": [
    {
      "title": "The sidecar court lock is unreachable in EVERY build \u2014 both entry points into CourtIPDetector are dead, so court=- is structural, not a tuning problem",
      "evidence": "Entry point 1 (packets): OrionAppController.cpp:3181-3201 routes NetworkBridge::packetObserved through packet_bridge_authority::forwardToTimingIfAuthorized(ServiceIdentityTrust::Unverified, explicitDevelopmentExperiment, ...). PacketBridgeAuthority.h:22-29 returns false unconditionally under ORION_PRODUCTION_BUILD; otherwise it returns explicitDevelopmentExperiment, read from env ORION_DEV_ALLOW_UNVERIFIED_BRIDGE_TIMING (OrionAppController.cpp:3193). That string appears EXACTLY ONCE in the entire repo \u2014 the read site itself (Grep over C:\\Users\\aaron\\Desktop\\NexusVision, 1 hit) \u2014 and is unset in the current shell. So RemotePlaySession::observePacket (RemotePlaySession.cpp:1117-1129) is never called; the sidecar's `observe_packet` handler (autogreen_sidecar.py:2707-2721) never fires; CourtIPDetector._peers (rtt_sync_engine.py:174) stays empty forever.\nEntry point 2 (explicit set): RemotePlaySession::setCourtIp (RemotePlaySession.cpp:1111-1115) and clearCourtTarget (:1183-1186) are declared (RemotePlaySession.h:96-97) and defined but have ZERO callers \u2014 grep for setCourtIp|clearCourtTarget across all of native_orion returns only the declaration and definition lines.\nTerminus: telemetry_.rttTargetVerified = true originates at exactly one site, RemotePlaySession.cpp:3951 (`= courtRttReady`), which requires targetVerified from the sidecar snapshot; rtt_sync_engine.py:738-741 makes target_verified require _is_public_ip_addr(court_ip) && sampler_target == court_ip. court_ip can only become non-empty at rtt_sync_engine.py:590 (from observe_packet) or :647 (from set_target_ip \u2190 set_court_ip). Both dead. OrionAppController.cpp:3161 only PRESERVES a true it never originates.\nMeasured: logs/orion_native.log \u2014 57 \"RTT tick\" lines, `grep -o \"court=[^ ]*\" | sort -u` returns exactly one value: `court=-`. \"Court IP locked\" (rtt_sync_engine.py:595): 0 occurrences. \"RTT sync retargeted to court IP\": 0. n reached 931 accepted samples against target 192.168.137.100 (13 occurrences in log), the PS5 over ICS.\nNote this is partly BY DESIGN and test-pinned: tests/test_passive_court_flow_contract.py:75-77 asserts NetworkBridge.cpp contains neither \"observePacket(\" nor \"setCourtIp(\".",
      "refutes_premise": "The brief asks to \"trace why CourtIPDetector never locks ... even now that packet capture works\", implying a detector/scoring defect. It is not the detector. NetworkBridge.cpp:878 emits packetObserved only AFTER passiveCourtFlow_.observe() already returned true \u2014 i.e. only for packets that passed the STRICTER private\u2194public + public-port-30000-30099 test (PassiveCourtFlowClassifier.h:85-97). CourtIPDetector's own test is strictly weaker (rtt_sync_engine.py:182 accepts either port in range on any public IP). If the packets were forwarded it would lock almost immediately. The blocker is 100% the authority gate.",
      "severity": "major",
      "ships_broken": false,
      "fix": "Do NOT wire it up (see the vanity-metric finding). Instead delete the dead surface so nobody re-audits it: remove RemotePlaySession::setCourtIp/clearCourtTarget (RemotePlaySession.cpp:1111-1115, :1183-1186; RemotePlaySession.h:96-97) and the sidecar's now-unreachable `set_court_ip`/`clear_court_target`/`observe_packet` command handlers (autogreen_sidecar.py:2685-2721), or add a one-line startup log stating the packet\u2192timing feed is compiled off so the blank is self-explaining in the log as well as the UI. If the gate is ever intentionally opened for a dev experiment, it must be a build flag, not an env var read at a hot signal handler \u2014 an env var that exists at exactly one site and is set nowhere is a dev/prod divergence trap waiting to fire."
    },
    {
      "title": "Three shipped UI strings promise a court round trip the build cannot produce",
      "evidence": "native_orion/qml/components/MeterConfigPanel.qml:504-507 \u2014 \"The address above is a passive observation. Round trip and jitter appear once the court server itself answers.\" No probe is ever sent to the court address: the only sampler is RTTSampler.measure_rtt_ms (rtt_sync_engine.py:121-135), whose target comes from _sampler.target_ip, which is only ever set to the console/gateway via set_ping_target (remote_play_orchestrator.py:2115, :2330). The court server is never asked, so it can never \"answer\".\nMeterConfigPanel.qml:541 \u2014 \"Turn on to fill these in\" (under \"Court discovery off\"). Turning packet capture on fills COURT IP only: captured packets reach passiveCourtFlow_ \u2192 telemetry_.diagnosticCourtIp (NetworkBridge.cpp:868-873, :699-700). The RTT and JITTER tiles (MeterConfigPanel.qml:442, :450) bind to orion.rttVerified = telemetry_.rttTargetVerified (OrionAppController.h:632), which the capture path explicitly ZEROES on every publication (NetworkBridge.cpp:713).\nMeterConfigPanel.qml:552 \u2014 InfoTip: \"Watches the Remote Play flow to identify the court server and measure the real round trip.\" It identifies (true) and measures no round trip (false); the sniffed packets reach no RTT estimator at all.",
      "refutes_premise": "The brief says \"A footer now explains the blank rather than showing a wrong number.\" The footer is honest about the CURRENT blank but describes a future state that no code path can reach, and the older strings at :541/:552 are flatly false. This is the same failure class as the card that once read \"connected\" while the bridge was dead \u2014 the number is blank but the prose is not.",
      "severity": "major",
      "ships_broken": true,
      "fix": "MeterConfigPanel.qml (I did not apply this \u2014 another agent added the :486-515 footer tonight and would conflict). :504-507 \u2192 \"The address above is passive observation only. Orion does not probe the court server, so there is no round trip to report.\" :539-546 \u2192 change \"Turn on to fill these in\" to \"Turn on to identify the court server\". :552 InfoTip \u2192 drop \"and measure the real round trip\"; end at \"...to identify the court server.\" Keep the :514 InfoTip verbatim \u2014 it is the one string on the card that is exactly right."
    },
    {
      "title": "PREMISE REFUTED: the fork's q.rtt is the PC\u2194PS5 remote-play RTT, not a PS5\u2192court RTT",
      "evidence": "q.rtt is field 5 of tkproto_ConnectionQualityPayload (chiaki-ng-src/lib/protobuf/takion.proto:285-292). Its siblings are target_bitrate, upstream_bitrate, upstream_loss, disable_upstream_audio, loss \u2014 every one a Remote Play STREAM parameter. It arrives in stream_connection_takion_data_idle (streamconnection.c:674, case tkproto_TakionMessage_PayloadType_CONNECTIONQUALITY at :697), i.e. inbound on the Takion control channel from the console to this client. Nothing in the Takion protocol has any concept of a game server. The repo's own prior investigation says the same thing explicitly: docs/ORION_RTT_SYNC_INVESTIGATION_PROMPT.md:41 \u2014 \"measures the *real* PC\u2194PS5 Remote Play RTT (q.rtt in lib/src/streamconnection.c CONNECTIONQUALITY ...) ... (This is the PRIMARY hop per the reframe above, NOT the court hop.)\"",
      "refutes_premise": "The brief states: \"The Chiaki fork measures a genuine PS5->court q.rtt\". It does not. It measures the console's view of the round trip to THIS PC over the remote-play link. Surfacing it would give you a second, live measurement of the same hop Senkusha samples once \u2014 which is genuinely useful \u2014 but it would not give you a court RTT, and labelling it as one would be the exact wrong-number-worse-than-a-blank failure this audit exists to prevent.",
      "severity": "major",
      "ships_broken": false,
      "fix": "If it is ever surfaced, label it \"Console link RTT\" / \"Remote Play RTT\", never \"court\". Do not route it into networkAutomationOffset()."
    },
    {
      "title": "CONFIRMED on the shipped binary: q.rtt is discarded \u2014 it is not merely unstored, the format string is compiled out",
      "evidence": "streamconnection.c:697-712: `tkproto_ConnectionQualityPayload q = msg.connection_quality_payload;` then q.rtt appears ONLY as an argument to CHIAKI_LOGV at :700-706. log.h:49-53: `#ifdef NDEBUG / #define CHIAKI_LOGV(log, ...) ((void)0) / #else ...`. The fork builds Release (chiaki-ng-src/build/CMakeCache.txt:111 CMAKE_BUILD_TYPE:STRING=Release) \u2192 NDEBUG \u2192 the whole call vanishes. streamconnection.h:80 has `double measured_bitrate;` and no rtt field; grep for rtt across all of lib/include/chiaki/*.h returns only launchspec.h:19, senkusha.h:44, session.h:240 \u2014 none of them the connection-quality value.\nByte-scan of the DEPLOYED binary (settings.json:16 chiaki_path \u2192 native_orion/deploy/chiaki-ng-orion/chiaki-ng-Win/OrionStream.exe, 6,997,386 bytes, 2026-08-02 21:04): \"StreamConnection received connection quality\" \u2192 0 occurrences; \"StreamConnection measured bitrate\" \u2192 0; \"Senkusha determined average RTT\" \u2192 1; \"Senkusha received Pong\" \u2192 2. Corroborated by the runtime logs: the 6 session logs in %APPDATA%/Chiaki/Chiaki/log contain 0 lines matching \"connection quality\" and 0 lines tagged [V].",
      "severity": "minor",
      "ships_broken": false,
      "fix": "Surfacing it is a 4-line change with an exact in-file precedent (measured_bitrate). (1) chiaki-ng-src/lib/include/chiaki/streamconnection.h:80 \u2014 add `double connection_quality_rtt;` beside measured_bitrate. (2) lib/src/streamconnection.c \u2014 inside the CONNECTIONQUALITY case, add `stream_connection->connection_quality_rtt = q.rtt;` as a statement next to the existing :707 measured_bitrate assignment (that assignment is OUTSIDE the LOGV macro, which is why it survives NDEBUG \u2014 mirror it exactly). (3) gui/src/streamsession.cpp:2869-2876 \u2014 TriggerFfmpegFrameAvailable already polls session.stream_connection.measured_bitrate every frame; add the same change-detect for the rtt. (4) Transport to Orion: the frame pipe already carries a 64-byte header with a spare `uint32_t reserved` (chiaki-ng-src/gui/include/orionframeexport.h, static_assert size==64) whose Python peer is chiaki_backend.py:1174 `struct.Struct(\"<IHHIQQQIIIIqI\")` \u2014 repurposing the trailing I as a float32 ms value requires a v3 version bump on both sides plus chiaki_backend.py:1574-1582 _wire_contract_reason. Do NOT use the OrionInputAck path: OrionInputClient.h:56-64 is static_assert'd at 12 bytes with only a uint16_t spare, and it is the fire path.\nMANDATORY FIRST STEP before any of this: the units of q.rtt are UNVERIFIED. takion.proto:288 declares `optional double rtt = 5;` with no unit, and the only formatter is \"%.4f\" \u2014 that is equally consistent with seconds or ms, and there is no observation of the value anywhere because the line is compiled out. Build one dev binary with the line promoted to CHIAKI_LOGI, read the magnitude, and compare it against Senkusha's 0.38 ms on the same rig BEFORE any value reaches a UI."
    },
    {
      "title": "Senkusha RTT measures the one hop that IS in the actuation loop \u2014 and its measured value is the strongest evidence that court RTT is pointless",
      "evidence": "What it measures: senkusha_run_rtt_test (senkusha.c:292-386) sends a 0x224-byte Takion AV packet with a random tag to the console, the console echoes it, and the client takes `senkusha->pong_time_us - time_start_us` from chiaki_time_now_monotonic_us (:341, :363-365), averaged over SENKUSHA_PING_COUNT_DEFAULT pings (:385). It is an application-level UDP round trip over the same socket the video and input traverse. It runs ONCE, during session setup (session.c:683 \u2192 session->rtt_us), and is never re-measured; its only consumer is launch_spec.rtt = session->rtt_us / 1000 (streamconnection.c:1005) sent to the console. On failure it is hard-coded to 1000 us (session.c:701).\nMeasured on this rig, 6 sessions in %APPDATA%/Chiaki/Chiaki/log: 0.367, 0.371, 0.372, 0.372, 0.381, 0.419 ms. Mean ~0.38 ms, spread 0.052 ms. (The brief's \"0.32-0.40ms\" is close but the low end is not observed \u2014 the measured minimum is 0.367.)\nAgainst the loop: memory/lead-seed puts the real end-to-end press\u2192pixel latency at ~75 ms (AutomationEngine.cpp:6566 measuredLatencyMs_ is the lead base on the authoritative path). 0.38 / 75 = 0.5%. The entire network leg of the actuation loop is half a percent of it; the other 99.5% is encode + decode + detector + scheduling.",
      "severity": "minor",
      "ships_broken": false,
      "fix": "Worth surfacing, with two hard constraints. (a) Label it \"Link setup RTT\" or similar and stamp it with the session start time \u2014 it is a one-shot setup measurement (senkusha.c:385, session.c:683) and presenting it as a live number would be a second wrong-number failure. (b) Never let it enter the lead: it is already inside the measured round trip that AutomationEngine.cpp:6566/:10317 uses as the lead base. Cheapest honest path: parse \"Senkusha determined average RTT = %f ms\" from the chiaki session log (it is CHIAKI_LOGI and confirmed present in the shipped binary \u2014 1 occurrence in OrionStream.exe), no fork patch required."
    },
    {
      "title": "BLUNT ANSWER: a court RTT is a vanity metric here \u2014 it is not in the actuation loop, the probe would not measure the game flow, and the shipped timing path zeroes it anyway",
      "evidence": "(1) Not in the loop. The bot observes the meter in a decoded remote-play frame and schedules a release; the loop is PS5\u2192encode\u2192LAN\u2192PC decode\u2192detector\u2192OrionInput pipe\u2192chiaki\u2192LAN\u2192PS5. The court server appears nowhere in it.\n(2) The number would not even be a court RTT. RTTSampler pings from the PC, not the PS5, and uses ICMP via ping3 (rtt_sync_engine.py:137-147; ping3 5.1.5 IS installed in .venv) falling back to a TCP:80 connect (:149-163) \u2014 a different protocol on a different queue from the game's UDP flow. Worse, :159-161 counts ConnectionRefusedError as a SUCCESSFUL sample: it would time the SYN\u2192RST of a closed port and publish it as court latency.\n(3) It would contribute nothing on the authoritative path and would be actively harmful on the fallback. AutomationEngine.cpp:6557 and :10312 both read `(useMeasuredLead ? 0.0 : shot_.networkOffsetMs)` \u2014 the comment at :6544-6549 says why: \"the oracle's measured round-trip ALREADY contains the network RTT ... adding networkOffsetMs here as well double-counted the network leg\". So on the measured-lead path a court RTT is discarded by design. On the modelled fallback path it would ADD rtt_half + jitter_margin + 2.5 ms of lead (rtt_sync_engine.py:751-752) derived from a hop that is not in the loop \u2014 i.e. it would fire the bot EARLY by a number with no causal relationship to the deadline. That is a correctness hazard dressed as a feature.\n(4) Non-circularity of the one thing that IS honest: packet counts and inter-packet interval (PassiveCourtFlowClassifier.h:209-212, stamped with the caller's local monotonic clock at NetworkBridge.cpp:867) are computed from packet arrival times alone. They touch no lead knob, no tip_eta_ms/command_eta_ms/lead_ms, and no fill percentage \u2014 they cannot be circular against the 300 ms lead the way travel_pp was.",
      "severity": "major",
      "ships_broken": false,
      "fix": "Do not wire a court RTT. The card should show, and only show: COURT IP (already labelled as an observation), PACKETS IN / PACKETS OUT (already correct and honest \u2014 NetworkBridge.cpp:730-736), packet cadence/interval, and optionally the one-shot Senkusha link RTT under an unambiguous \"setup, console link\" label. Delete the RTT and JITTER tiles rather than leaving two permanently-blank cells with prose explaining why \u2014 a tile that can never fill is worse than no tile."
    },
    {
      "title": "Auto wifi mode can never engage: its only input is hard-zero, so wifiRttDeltaClampMs and wifiFreshnessFactor are dormant on every shot",
      "evidence": "updateNetworkQuality is called from exactly two sites (OrionAppController.cpp:3173, :2631), both passing networkAutomationJitter(), which is `telemetry_.rttTargetVerified ? telemetry_.jitterMs : 0.0` (OrionAppController.h:1291-1293). rttTargetVerified is permanently false (see finding 1), so jitterMs is always exactly 0.0. AutomationEngine.cpp:2562-2565 then computes `networkJitterEmaMs_ = networkJitterEmaMs_ <= 0.0 ? j : ema...` with j == 0.0, so the EMA is pinned at 0.0 forever. :2568 `wifiMode_ = networkJitterEmaMs_ >= config_.wifiJitterThresholdMs` with wifiJitterThresholdMs = 4.0 (AutomationEngine.h:679) is therefore false on every tick of every session. Consequences: the tighter per-shot RTT delta clamp at :3991 always picks rttDeltaClampMs (10.0) and never wifiRttDeltaClampMs (5.0), and all six wifiFreshnessFactor sites (:5576, :5600, :5631, :6376, :8900, :10253) always multiply by 1.0 instead of 0.5.",
      "refutes_premise": "",
      "severity": "minor",
      "ships_broken": false,
      "fix": "Either (a) feed wifiMode_ from a jitter source that actually exists \u2014 the passive classifier's inbound/outbound interval variance (PassiveCourtFlowClassifier.h:381-399), which is measured on the local monotonic clock and is not circular against any lead term \u2014 or (b) delete wifiMode_/wifiRttDeltaClampMs/wifiFreshnessFactor and their branches, since a permanently-false flag reading as an active safety feature is misleading to the next reader. Requires touching AutomationEngine.cpp \u2014 reporting only, not applying, per the do-not-write constraint."
    },
    {
      "title": "Live cost with zero product: the RTT ping loop pings the console forever, and RttEstimator.h has no production caller at all",
      "evidence": "RTTSyncEngine.start() launches a permanent RTTPingLoop thread (rtt_sync_engine.py:525-530, started from remote_play_orchestrator.py:2321) that pings 192.168.137.100 every 400 ms, tightening to 150 ms for 2.5 s after every meter detection (:826-830). logs/orion_native.log shows n=931 accepted ICMP samples in one session, all against the LAN console, all structurally discarded. That is ~2.5-6.7 raw ICMP echoes per second to the PS5 over the same ICS link that carries the Remote Play stream, sustained for the whole session, for an output no consumer can ever read.\nSeparately: native_orion/src/RttEstimator.h is referenced by exactly two files in the whole native tree \u2014 itself and native_orion/tests/AutomationEngineTests.cpp. Zero production callers.\nAlso inert: settings.json has manual_sync_adjust_ms = -50 with orion_sync_mode = \"Auto\"; OrionAppController.cpp:10741-10745 reads manualSyncAdjustMs only in the Manual branch, then returns 0.0 because rttTargetVerified is false. The user has a -50 ms trim configured that does nothing (consistent with the brief's DEAD KNOBS list).",
      "refutes_premise": "docs/ORION_RTT_SYNC_INVESTIGATION_PROMPT.md:39 describes RttEstimator.h as part of the live native path that \"Feeds OrionAppController telemetry \u2192 QML\". It feeds nothing; it is test-only.",
      "severity": "minor",
      "ships_broken": false,
      "fix": "Gate RTTSyncEngine.start() (remote_play_orchestrator.py:2321) behind an actual court lock, or drop the ping loop entirely alongside the court-detector removal \u2014 nothing downstream can consume its output. Delete native_orion/src/RttEstimator.h and its AutomationEngineTests.cpp references, or correct docs/ORION_RTT_SYNC_INVESTIGATION_PROMPT.md:39 to say it is unwired. Neither touches a shipping code path."
    }
  ],
  "confidence_notes": "Could not establish, and how to settle each:\\n\\n1. I did NOT independently confirm that COURT IP populates in a live session. The brief asserts it, and the code path is sound (nexus_svc.log:1785852682142 shows WinDivert opened with filter \\\"ip and (udp or tcp) and (ip.SrcAddr == 192.168.137.100 or ip.DstAddr == 192.168.137.100)\\\", and PassiveCourtFlowClassifier will qualify a private\u2194public UDP flow on public port 30000-30099), but logs/orion_native.log contains zero \\\"court_ip=\\\" lines because the passive qualify path logs nothing on success. SETTLES BY: an Export Diagnostics bundle from a live game \u2014 OrionAppController.cpp:4791-4792 writes `court_ip=` and `court_verified=` into the network block.\\n\\n2. q.rtt UNITS are unverified and this is the single most important gap if anyone acts on the surfacing patch. takion.proto:288 is `optional double rtt = 5;` with no unit and the only formatter is \\\"%.4f\\\"; the value has never been observed on this rig because CHIAKI_LOGV is compiled out. It could be seconds, milliseconds, or microseconds. Displaying it without first comparing its magnitude against Senkusha's measured 0.38 ms in a dev build would be exactly the wrong-number-worse-than-blank failure this audit is guarding against.\\n\\n3. CONNECTIONQUALITY arrival cadence is unverified. I asserted it is \\\"live/periodic\\\" from protocol shape (it drives measured_bitrate recomputation + a stream_stats reset at streamconnection.c:707-709, which only makes sense on a repeating message), but I have no observation of its actual rate \u2014 it may fire every few hundred ms or only on bitrate transitions. Same dev-build LOGI promotion settles it.\\n\\n4. \\\"NBA 2K resolves shot timing on the shooting console\\\" is my architectural inference, NOT repo evidence, and I have deliberately not leaned on it. The case against court RTT stands entirely on repo-internal facts: the measured lead already contains every network leg (AutomationEngine.cpp:6544-6549), the network offset is zeroed on the authoritative path (:6557, :10312), and the probe would be ICMP/TCP-80 from the PC rather than the game's UDP flow. Even if 2K did consult the server, those three facts still make the current implementation unusable.\\n\\n5. The ~75 ms end-to-end figure used in the 0.5% comparison comes from the lead-seed memory, not from a number I re-measured this session. The Senkusha side (0.367-0.419 ms, n=6) I measured directly from the chiaki session logs. If 75 ms is wrong the ratio moves, but the network leg would have to be a ~40x underestimate before it stopped being negligible.\\n\\n6. I made NO code changes, ran no build, and ran no tests. Every finding is static trace + log/binary measurement. The MeterConfigPanel.qml copy patch is reported rather than applied specifically because another agent added the :486-515 footer during this same session and an edit would collide.\\n\\n7. Not investigated, but noticed and possibly worth its own look: logs/orion_native.log has 713 \\\"Packet bridge authentication rejected\\\" lines against 1360 \\\"Packet bridge reachable\\\", and nexus_svc.log ends with \\\"Auth rejected for ('127.0.0.1', 3): auth_required (client sent 'status' before authenticating)\\\". That is outside this audit's scope but it is a ~34% bridge auth failure rate."
}

---

## agent `ac929c5ceacb34b6e`

{
  "headline": "A fresh install ships with a ~218 ms actuation lead against the only lead this project has ever verified (~300 ms), writes every piece of persistent state into C:\\Program Files where an unelevated app cannot write it, and offers a meter-colour picker whose three non-Red options make the detector permanently blind.",
  "findings": [
    {
      "title": "THE LEAD: a fresh install starts ~82 ms short, and every correction that closes that gap lives only in a gitignored file on this rig",
      "evidence": "Fresh-install resolution chain, read end to end: native_orion/src/AutomationEngine.cpp:9072-9114 `measuredLeadForActuationMs()` = max(0, measuredLatencyAuthorityMs_), then (a) user lead at :9094-9098 requires userActuationLeadMs in [150,450] \u2014 default is 0.0 (native_orion/src/AppConfig.h:341, bounds :345-346) so it is skipped; (b) bias at :9105 requires autonomousLeadBiasMs>0 \u2014 default 0.0 (native_orion/src/AutomationEngine.h:783); (c) floor at :9110 requires autonomousLeadFloorMs>0 \u2014 default 0.0 (AutomationEngine.h:755). So lead == the packaged factory prior: models/latency_factory_prior.json:31 mean_ms 218.5 (capture-card-pipe) / :57 226.8 (vigem). Fire time is `fireAtMs = predictedTipMs - effectiveLeadMs` (AutomationEngine.cpp:6021), so a smaller lead fires LATER. What this rig actually runs: settings.json:4 `actuation_lead_ms: 300` with :5 `actuation_lead_user_set: true`, plus run_orion.local.ps1:107 ORION_LEAD_FLOOR_MS=290 and :117 ORION_LEAD_BIAS_MS=62. That launcher is gitignored (.gitignore:121 `*.local.ps1`) and appears in no packaging list (installer/orion.iss [Files] :62-75; tools/package_orion_release.py:380-418), so a customer gets none of it. The delta is ~82 ms \u2014 at the logged live rise of 0.18 pp/ms that is ~15 pp of meter, and the logged green windows are 2.0-2.7 pp wide (run_orion.local.ps1:91). Corroboration that 218.5 is not merely 'unverified' but wrong for this hardware: the release-landing instrument on this rig reads median 303-317 ms over 24 samples (logs/orion_native.log 2026-08-04T12:34:19Z through 14:15:02Z, `Lead measurement: median_ms=...`). Also note the prior is itself this rig's number: docs/LATENCY_FACTORY_PRIOR.md:36-44 lists all 8 labels it was fitted from, every one of them Aaron's capture_card+pipe route, and :71-74 shows the other three route profiles have evidence_n=0.",
      "fix": "Two changes, neither of which I can apply (AutomationEngine.* is owned by another agent tonight). (1) In native_orion/src/AutomationEngine.cpp::applyConfig around :1408-1419, give autonomousLeadBiasMs a settings-backed source in addition to the env var, and default it to the measured structural value (62-72). The project's own argument for portability is at AutomationEngine.h:757-772 \u2014 the bias corrects the LABEL METHOD, not the machine, because latency_estimator.py timestamps the meter's first upward crossing of f_stop while the shot actually locks after the overshoot; that gap is a property of 2K's animation. Shipping it at 0 means every install inherits the short label. AutomationEngine.h:773-777 records the objection (defaulting to 72 broke 8 tests where a genuinely ~70 ms-latency scenario became unschedulable) \u2014 the correct resolution is to apply the bias multiplicatively-capped or only above a latency floor, not to ship 0. (2) Add a first-run Shot Lead prompt: AppConfig.h:517 `actuationLeadAcceptsMeasuredSeed` already gates the one-time seed; have the Setup flow ask the user to take ~12 shots and read the banner, and surface the ShotLeadCard on first launch rather than burying it on RemotePlayPage.qml:1491. Separately, fix the stale comment at AutomationEngine.h:741 (\"Set via settings or ORION_LEAD_FLOOR_MS\") and AutomationEngine.cpp:1389 (\"Env overrides settings\") \u2014 grep confirms neither autonomousLeadFloorMs nor autonomousLeadBiasMs has any settings.json key at all; env is the ONLY source.",
      "severity": "blocker",
      "ships_broken": true,
      "refutes_premise": "The brief's framing that ORION_LEAD_FLOOR_MS=290 + BIAS=62 is 'the single most rig-specific number in the product' that must be removed before shipping is half wrong: it is already removed. Both vars are env-only and the file that sets them is gitignored, so the shipping risk is the exact opposite of what was stated \u2014 customers get NO correction at all and start 82 ms short. The ShotLeadCard direction check also comes back CORRECT, not inverted: ShotLeadCard.qml:12-16 states larger lead = fires earlier, :181-183 tells the user 'Shots landing LATE? Raise this. Landing EARLY? Lower it.', and AutomationEngine.cpp:6021 `fireAtMs = predictedTipMs - effectiveLeadMs` confirms both. The card is also genuinely wired end to end: Q_PROPERTY at OrionAppController.h:491-495, nudge/reset invokables at :1107/:1110, persisted through AppConfig::save (AppConfig.cpp:412) \u2014 and this rig's own settings.json:4-5 proves the round trip works."
    },
    {
      "title": "Every piece of persistent state is written next to the exe, and the installer puts the exe in Program Files and launches it unelevated",
      "evidence": "native_orion/src/main.cpp:284-285 sets `rootDir = QCoreApplication::applicationDirPath()`, and :294-295 compiles the dev redirect to ~/Desktop/NexusVision OUT under ORION_PRODUCTION_BUILD \u2014 so in a shipped build rootDir IS the install directory. Everything writable hangs off it: native_orion/src/AppConfig.cpp:135 `settingsPath() = rootDir_ + \"/settings.json\"` written via QSaveFile at :412; :158-164 learningPath() -> learning.json, written at :519; native_orion/src/OrionAppController.cpp:1458 log sink `rootDir_ + \"/logs/orion_native.log\"`. The sidecar does the same: simple_meter_reader.py:276-278 roots the colour profile at the module's own directory and :382 does makedirs+open there. installer/orion.iss:38 `DefaultDirName={autopf}\\Orion` (= C:\\Program Files\\Orion), :43 PrivilegesRequired=admin for the DRIVER installs only, and :94 launches `{app}\\OrionNative.exe` with no elevation flag; the shortcuts at :78-80 are likewise plain. There is no [Dirs] section granting users-modify on {app}. A 64-bit process gets no UAC file virtualization, so a standard user's write to C:\\Program Files\\Orion\\settings.json fails outright. Failure is silent-ish: AppConfig.cpp:413-425 returns false with an error string, and OrionAppController's seed path at OrionAppController.cpp:2477-2479 simply `return`s on !saveConfigSilently \u2014 the user moves the Shot Lead slider, sees it move, and loses it on restart. This has never been felt on the dev rig because main.cpp:299-300 redirects rootDir to the writable ~/Desktop/NexusVision checkout.",
      "fix": "Split read-only assets from writable state. In native_orion/src/main.cpp around :284-301, keep rootDir = applicationDirPath() for assets/models/qml, and introduce a second path `dataDir = QStandardPaths::writableLocation(QStandardPaths::AppDataLocation)` (QStandardPaths is already included at main.cpp:8), creating it if absent. Pass dataDir to AppConfig (native_orion/src/AppConfig.h:523 ctor already takes the root as a parameter, so this is a one-line call-site change plus settingsPath/learningPath at AppConfig.cpp:133-164), to the log sink at OrionAppController.cpp:1458, and to the sidecar via an ORION_DATA_DIR env var consumed at simple_meter_reader.py:276-278. Migrate an existing install-dir settings.json on first run. Belt-and-braces: add `[Dirs] Name: \"{app}\"; Permissions: users-modify` to installer/orion.iss so an un-migrated build still functions. Verify by installing to C:\\Program Files\\Orion, running unelevated, moving the Shot Lead slider, and restarting.",
      "severity": "blocker",
      "ships_broken": true,
      "refutes_premise": ""
    },
    {
      "title": "The Meter Color picker offers Purple / White / Yellow, but the detector's colour envelope is hard-pinned to red and structurally refuses to widen",
      "evidence": "The UI offers four colours: native_orion/qml/components/MeterConfigPanel.qml:83 `model: [\"Purple\", \"White\", \"Yellow\", \"Red\"]`, bound to orion.meterColor at :84-85, and AppConfig.cpp:627-633 accepts all four as valid (only an unrecognised string falls back to Red). The detector sees none of it. Every acquisition scan is a red BGR inRange: simple_meter_reader.py:942 `_RED_LO, _RED_HI = (0,0,220), (60,60,255)`, consumed by _redmask at :2962-2965 and by the scan path at :3013/:3019/:3031, with the courtwide and micro tiers at :949 and :955 being nothing but looser reds. The calibrator cannot rescue it: ColorCalibrator.RED_ENV is the same constant (simple_meter_reader.py:240), _load_style_config resets red_env to it unconditionally at :313, and at :332-333 will only adopt a style-file envelope if it is byte-equal to the shipped red \u2014 a genuinely different colour is refused by construction. The class docstring at :235-237 states the design: 'Everything is intersected with the v1 ENVELOPE = the shipped constants, so learning strictly narrows... there is NO EWMA widening path, ever.' The selected colour reaches only a lookup that falls back to Red (:321 `red_db = db.get(self.color) or db.get(\"Red\")` \u2014 and meter_styles/Arrow2.json default_bands contains only \"Red\" and \"Green\" keys), a profile dict key (:358), and a passthrough label on the result (:5964, :6001 color_name). So selecting Purple changes a string and nothing else; the meter is then invisible and the bot never fires. There is no preflight check anywhere that warns the user their in-game meter colour must be Red.",
      "fix": "Short term, before this weekend: cut MeterConfigPanel.qml:83 down to `model: [\"Red\"]` (or disable the combo with a 'Red only in this build' caption), and add a setup-flow instruction 'In NBA 2K, set Shot Meter Color to Red and Shot Meter Type to Arrow2' \u2014 that one sentence is worth more than any code change here. Also add a detector-side health line: when armed_hw has been true for N seconds with zero red candidates in the band, emit a user-log line naming the meter-colour setting as the likely cause (simple_meter_reader.py already tracks _armed_nolock_streak at :297). Medium term, if other colours must ship: give each colour its own envelope keyed off self.color in _load_style_config (simple_meter_reader.py:308-339) and per-colour default_bands entries in each meter_styles/*.json, then re-run the replay gate per colour \u2014 note that the green make-window band (_G at :943) is colour-independent and can stay as the acquisition anchor.",
      "severity": "blocker",
      "ships_broken": true,
      "refutes_premise": "Answers the brief's 'is there calibration, and does it self-heal' with a hard no for colour: the ColorCalibrator self-heals only WITHIN red (it narrows, never widens \u2014 simple_meter_reader.py:235-237), so a non-Red meter is unrecoverable, not slowly-recovering."
    },
    {
      "title": "The detection stack this rig has been validated on is not the one that ships: six reader/engine flags are default-OFF in code and are turned on only by the gitignored launcher \u2014 and one of them does not exist at all",
      "evidence": "Shipped defaults, all read straight from the source: simple_meter_reader.py:1373 ORION_READER_ANCHOR default '0'; :1397 ORION_READER_PCTL_FILL '0'; :1466 ORION_READER_TRACK_H_CAP '0'; :1166 ORION_READER_FAKELOCK_BREAK '0'; :1060 ORION_READER_ROBUST '0'; pose_timing.py:351 ORION_POSE_CAMERA_ANCHOR '0'; native_orion/src/AutomationEngine.cpp:1330 ORION_GOTO_METER_WAIT is env-presence-only (absent = off); :117-120 ORION_USER_LOG likewise, so the customer-facing log the launcher comment calls '5b human-readable user log' never exists for a customer. run_orion.local.ps1 turns all of these ON (:150, :219, :220, :243, :288, :293, :294). The sidecar spawn does NOT forward them: native_orion/src/RemotePlaySession.cpp:2173-2199 injects ORION_SIMPLE_READER=1 and passes through ORION_INPUT_HOOK, and nothing else reader-related. (ORION_SIMPLE_READER is genuinely safe \u2014 remote_play_orchestrator.py:1073 and :1319 both default it to '1' \u2014 so the reader itself is the right one; it is the tuning around it that diverges.) Worse, run_orion.local.ps1:226 sets ORION_READER_TRACK_H_ROBUST_GATE and attributes to it the fix for 'first shots perfect then degrades over the session' with a measured A/B (under-read 13.7pp -> 1.1pp). That environment variable is read by no code: a repo-wide search finds it in six docs files and zero .py/.cpp files. The real bundle flag is ORION_READER_ROBUST (simple_meter_reader.py:1060, consumed at :2982, :3085, :3714-3716), which the launcher never sets \u2014 so _robust is False on the dev rig too, and that measured fix has been enabled nowhere, ever.",
      "fix": "Decide per flag and make the code default match, rather than shipping a config nobody has played on. Concretely, in simple_meter_reader.py flip the defaults at :1373 (_anchor), :1397 (_pctl_fill) and :1166 (_fakelock_break) to '1' \u2014 these are the three the dev rig has actually accumulated live hours on \u2014 and leave :1466 TRACK_H_CAP off until its live batch closes. Add pose_timing.py:351 camera anchor default '1' and make ORION_USER_LOG default-on in AutomationEngine.cpp:117 (a customer with no user log is undiagnosable over Discord). Then delete the two dead ORION_READER_TRACK_H_ROBUST_GATE lines from run_orion.local.ps1 (:226 and the commented :271) and either wire ORION_READER_ROBUST or strike the claim from the record \u2014 right now the launcher documents a fix that has never run. Finally, make the divergence impossible to reintroduce: have RemotePlaySession.cpp:2173 log the full resolved ORION_READER_* set at session start so a support log shows exactly which reader config produced it.",
      "severity": "major",
      "ships_broken": true,
      "refutes_premise": "Directly extends the brief's trap #2 ('gates are blind to production') in the opposite direction: it is not only the gates that differ from production \u2014 the DEV RIG differs from production too, on at least six flags. Any statement of the form 'it works on this rig' is a statement about a configuration no customer will ever run."
    },
    {
      "title": "The one-time 'measured on your setup' lead seed cannot bootstrap from the factory prior \u2014 the guard that protects it is triggered by exactly the condition a too-short lead creates",
      "evidence": "The seed is the product's only self-calibration story (ShotLeadCard.qml:198-199 promises 'Venice is measuring your setup... It fills this in for you after about a dozen shots'). It needs 12 accepted samples (native_orion/src/AutomationEngine.h:808 actuationLeadSeedMinSamples = 12; gate at AutomationEngine.cpp:10989) and it drops any landing whose meter pinned: AutomationEngine.cpp:10969-10971 `if (peakFillPct >= 99.0) return;`. Measured over the 253 `Release landing:` records in logs/orion_native.log: 117 (46%) already fail that guard at this rig's 300 ms lead, leaving 136 that pass, mean peak_fill 95.49. A fresh install fires ~82 ms later (finding 1), which at the logged 0.18 pp/ms adds ~14.7 pp to fill_at_release and hence to peak \u2014 pushing that 95.49 mean to ~110, i.e. clipped. Replaying the guard over the real landings with that shift: survivors fall 136/253 (54%) -> 7/253 (3%). At a 3% acceptance rate, 12 samples takes ~400 shots, so the promise on the card is not kept and the install stays 82 ms short indefinitely. The seed is also one-shot (AppConfig.h:512-517 `actuationLeadAcceptsMeasuredSeed` returns false once actuationLeadMs>0), so a bad early seed is permanent until Reset.",
      "fix": "Replace the hard drop at AutomationEngine.cpp:10969-10971 with a censored-sample estimator: a pinned landing is not 'no information', it is the statement lead >= (99 - fillAtRelease)/velocity. Feed those as lower bounds (e.g. take the max of the median of accepted samples and the 75th percentile of the censored lower bounds) so a too-short starting lead produces evidence that pushes the seed UP, which is the safe direction. Alternatively \u2014 and much simpler for this weekend \u2014 add a guided first-run calibration that deliberately sets the lead HIGH (e.g. 340, inside the [150,450] band at AppConfig.h:345-346) for the first dozen shots, where the meter cannot pin and every landing is measurable, then installs the median and lets the user trim on the banner. I cannot patch AutomationEngine.cpp; this needs to go to whoever owns it.",
      "severity": "major",
      "ships_broken": true,
      "refutes_premise": "Also flags a contradiction in the project's own record that I could not resolve and that the fix depends on \u2014 see confidence notes: run_orion.local.ps1:100-105 records travel_pp tracking the configured lead 1:1 across a 302/295/284 sweep, which if true makes this instrument circular rather than merely starved."
    },
    {
      "title": "The Meter Style picker offers six styles, but every geometry prior in the shipped reader is Arrow2's, and the per-style geometry in the style files is deliberately not read",
      "evidence": "MeterConfigPanel.qml:71 offers `[\"Arrow\", \"Arrow2\", \"Dial\", \"Pill\", \"Straight\", \"Sword\"]` and AppConfig.cpp:619-625 accepts all six. The reader's gates are Arrow2-only and hard-coded: simple_meter_reader.py:916-931 is headed '--- meter geometry priors: EXACT 2k_Vision Arrow2.json params (@1080p) ---' with BAND, W_MIN/W_MAX 20/34 ('Arrow2 contour width 23-30'), H_ACQ 33 ('Arrow2 contour height min 33'), H_MAX 165, AR_MIN 1.8 (a tall-thin vertical bar), plus the pop-in structure priors at :980-983 that assume a green tip dot 110-190 px above a red stub. The class docstring at :910 states the scope outright: 'Red/Arrow2 park meter.' The only per-style input is the style JSON, and _load_style_config reads only the default_bands / envelope / calibration blocks \u2014 its docstring at :311-312 says 'NEVER the legacy loose `colors`/`search` blocks', so the per-style `search` and `contour` geometry present in meter_styles/Arrow2.json (and its five siblings) is intentionally ignored. A Dial meter is a circle and a Straight meter is a horizontal bar; neither can satisfy AR_MIN 1.8 or the tip-above-stub relation.",
      "fix": "Same shape of fix as the colour picker and it should ship in the same change: reduce MeterConfigPanel.qml:71 to `[\"Arrow2\"]` (or grey out the rest with a 'more styles coming' note) and put 'Shot Meter Type: Arrow2' in the setup instructions next to the Red requirement. If styles must be supported later, the geometry constants at simple_meter_reader.py:916-983 need to move out of the class body and be loaded per style from the `search`/`contour` blocks that already exist in meter_styles/*.json, with a replay gate per style.",
      "severity": "major",
      "ships_broken": true,
      "refutes_premise": ""
    },
    {
      "title": "Release packaging copies every .json it finds in the build directory, so a stray settings.json or learning.json would ship the dev's console IP, capture index and 300 ms lead",
      "evidence": "tools/package_orion_release.py:138-148 `should_copy_top_file` returns True for any top-level file whose suffix is .exe, .dll or .json, and :393-395 copies each such file from BUILD_DIR into the package; the forbidden-name filter it consults (tools/release_filter_policy.is_forbidden_file_name, imported at :36/:49) does not list settings.json or learning.json. Those files are created next to whichever exe runs (AppConfig.cpp:135, :158 \u2014 see finding 2), which for a build-dir launch is native_orion/build/Release. Today the dir is clean (no *.json present) and this rig is protected by accident: main.cpp:299-300 redirects rootDir to the repo checkout whenever ~/Desktop/NexusVision/settings.json exists, so the build-dir exe writes to the repo instead. That protection is compiled out of a production build (main.cpp:294-295), so a production-flavoured smoke test run from the build dir before packaging would drop a settings.json in exactly the place the packager sweeps. The payload would be real: this rig's settings.json:123 holds `remote_play_console_ip: 192.168.137.100`, :15 `capture_card_index: 0`, and :4-5 the 300 ms lead with actuation_lead_user_set true \u2014 that last flag would permanently block the customer's own measured seed via AppConfig.h:517.",
      "fix": "Add \"settings.json\", \"learning.json\" and the learning.<slug>.json pattern to the forbidden set in tools/release_filter_policy.py (FORBIDDEN_FILE_SUFFIXES / the name predicate behind is_forbidden_file_name), so should_copy_top_file at tools/package_orion_release.py:140 rejects them and scan_forbidden at :421-445 fails the build loudly if one is present. Better still, narrow :148 from a blanket `.json` allow to an explicit allowlist of the JSONs that are actually meant to ship.",
      "severity": "minor",
      "ships_broken": false,
      "refutes_premise": ""
    },
    {
      "title": "Sub-720p or non-16:9 capture sources are rejected outright with no user-visible explanation",
      "evidence": "remote_play_orchestrator.py:85-113 `_detector_frame_contract_reason` returns 'undersized' below 1280x720 and 'aspect' when the ratio deviates from 16:9 by more than 3.5%; the tolerance can only be tightened, never relaxed (:854-858 clamps ORION_DETECTOR_ASPECT_TOLERANCE to <=0.035, and :845/:850 floor the min dimensions at 1280/720). The comment at :838-840 is explicit that this is fail-closed by design. That is the right call for correctness, but the consequence for a user whose card negotiates 1024x576, or who is on a 16:10 / ultrawide capture path, is a bot that simply never sees a meter. This is the correct behaviour paired with a missing diagnosis.",
      "fix": "Surface the rejection reason once per session in the user-facing log and in the Stream Setup card: 'Capture is 1024x576 \u2014 Orion needs at least 1280x720 at 16:9. Set your capture card to 1080p60.' The reason string is already returned by _detector_frame_contract_reason and consumed near remote_play_orchestrator.py:3505-3513; it just needs to reach the UI instead of dying in a counter.",
      "severity": "minor",
      "ships_broken": false,
      "refutes_premise": "This refutes the brief's resolution premise. Detection is NOT fragile to resolution and the thresholds are NOT raw pixels: remote_play_orchestrator.py:3513 normalizes every accepted frame to exactly 1280x720 with an aspect-preserving centre crop plus a uniform scale (:140-176), and simple_meter_reader.py:1964-1991 `_recompute_scale` rescales every pixel prior by sx = W/1920, sy = H/1080 against the reference at :1006. A 1080p or 1440p or 4K 16:9 source behaves identically to this rig's 720p. The real resolution risk is the fail-closed floor below 720p, not thresholds in pixels."
    },
    {
      "title": "Capture-device and controller identification are broadly portable \u2014 but the device-latency term is Elgato-shaped and non-Sony pads take a different, less-tested path",
      "evidence": "Capture: capture_card_backend.py:754-757 `_find_best_device_index` scans up to 8 DirectShow indices for a bright non-webcam device, :319 applies vendor-name hints, :806-854 falls back through the cached index then the brightness scan, and :512-520 tries DSHOW then MSMF (ORION_CAPTURE_API=msmf swaps the order). No index or FriendlyName is hardcoded to the HD60X. The one Elgato-specific number, capture_card_backend.py:110 HD60X_DEVICE_LATENCY_ESTIMATE_MS = 35.0, is documented at :107-109 as NOT applied by default and is referenced only by a log line at :911 \u2014 so it is inert, and correspondingly there is no per-device latency term for anyone else's card, which is a silent contributor to finding 1 on unlike hardware. Controller: native_orion/src/OrionAppController.cpp:224-238 `classifyRawInputControllerPath` returns empty for any device whose path lacks VID_054C, and findRawInputController at :286-300 skips empty kinds \u2014 so an Xbox pad is never the RawInput physical controller. There is an XInput path (:8608 'a real Xbox pad -> WinMM fallback not needed') and a WinMM fallback (:526-600), but the WinMM button mapping is explicitly Sony-ordered (native_orion/src/WinMmButtonMapping.h:13, include comment at OrionAppController.cpp:29).",
      "fix": "Capture: nothing needed for device selection. Do add a settings-backed capture-device-latency field so a user on a high-latency USB card is not silently mis-led by a term tuned to 35 ms of Elgato pipeline; today the only lever is the Shot Lead itself, which conflates it with everything else. Controller: before shipping, physically test an Xbox pad end to end \u2014 specifically that the physical shot edge (Square/X) arms the shot gate \u2014 and if the WinMM route is what serves it, verify applyWinMmButtonsToSample against the Xbox HID button order rather than the Sony order assumed at WinMmButtonMapping.h:13. If it does not work, say 'DualSense or DualShock 4 required' in the setup instructions rather than letting the user discover it.",
      "severity": "minor",
      "ships_broken": false,
      "refutes_premise": "Partially refutes the brief's 'CAPTURE DEVICE ... Elgato HD60X via DirectShow \u2014 what happens with a different card' concern: device selection is genuinely auto-discovered (name hints, brightness scan, cached index, DSHOW/MSMF fallback), not pinned. The residual risk is the missing per-device latency term, not the device binding."
    }
  ],
  "confidence_notes": "WHAT I COULD NOT SETTLE, and how to settle it.\n\n1. Is the landing lead measurement circular? This decides the FIX for finding 5, not whether it is broken. AutomationEngine.cpp:10946-11007 computes lead = (peak_fill - fill_at_release) / velocity. Two readings conflict. (a) NON-CIRCULAR: over the 253 `Release landing:` rows in logs/orion_native.log, peak and fill_at_release correlate at r=0.75 and var(travel_pp)=14.4 is much SMALLER than var(fill_at_rel)=30.9 and var(peak)=27.5 \u2014 if peak were a fixed target and travel were just (const - fill_at_rel), the correlation would be ~0 and the variances equal. That says travel_pp is a real physical interval. (b) CIRCULAR: run_orion.local.ps1:100-105 records a three-point sweep where configured lead 302/295/284 produced travel-derived 302/295/284 \u2014 a 1:1 response. A true end-to-end latency is a property of the hardware and MUST NOT move when the lead knob moves, so a 1:1 response is the signature of an echo. I could not join per-landing rows to the configured lead in force at that moment to adjudicate, because the sweep predates the `Release landing:` lines I have. THE DECISIVE TEST, ~20 minutes live: fix everything else, take 25 shots at ORION_LEAD_FLOOR_MS=340, then 25 at 260, and compare the two `Lead measurement: median_ms=` values. If the medians differ by ~80 ms the instrument is circular and finding 5's censored-estimator fix is worthless (the seed would just read back the factory prior \u2014 the ShotLeadCard's 'Measured on your setup' pill would be a lie, and the guided-high-lead calibration is the only viable fix). If they agree within ~15 ms the instrument is sound and the censoring fix is correct. Note both outcomes leave the fresh install broken; only the remedy changes. This is exactly the class of circular metric in trap #1, and it is NOT resolved by the fact that travel_pp is listed in memory as 'the only validated lead measure' \u2014 that note and the launcher's own sweep record disagree.\n\n2. The Program Files write failure (finding 2) is inferred from code paths plus the Inno config; I did not install and run. It is a five-minute empirical check and should be done before Saturday: install to C:\\Program Files\\Orion, run unelevated as a standard user, move the Shot Lead slider, restart, see if the value survives. If UAC virtualization somehow applies, the finding collapses to a warning \u2014 but 64-bit processes do not get file virtualization, so I rate this near-certain.\n\n3. The 82 ms shortfall (finding 1) is a delta against THIS rig's verified lead. I have no second machine, so I cannot say whether 218.5 is short by 82 ms on someone else's hardware or merely different. What I can say without a second machine is that the prior was fitted entirely from this rig's 8 labels (docs/LATENCY_FACTORY_PRIOR.md:36-44, all capture_card+pipe), that three of its four route profiles have evidence_n=0 (models/latency_factory_prior.json:47/:59/:71), and that the project's own analysis says the label method under-reports by a game-derived constant that applies to every install (AutomationEngine.h:757-772). The shortfall direction (too short = fires late) is certain from AutomationEngine.cpp:6021.\n\n4. The 3%-survival number in finding 5 is a counterfactual replay, not an observation: it assumes travel_pp stays fixed while fill_at_release shifts by the lead delta, i.e. it assumes reading (a) above. It is also an upper bound on the effect \u2014 the measured separation between pinned and non-pinned landings is only 2.36 pp (~13 ms), so pinning is not purely fire-time driven. Directionally I am confident (a +14.7 pp shift is 6x that separation and puts the 95.49 mean peak over 99); the exact figure I am not.\n\n5. I did not verify the Xbox-controller path empirically (finding 9) \u2014 only that RawInput excludes non-Sony VIDs and that an XInput branch exists. Needs a physical pad.\n\n6. Not audited for time: the console pairing/registration flow on a network that is not this rig's ICS 192.168.137.x (RemotePlaySession.cpp:1331 broadcasts 192.168.137.255 alongside 192.168.1.255 and 255.255.255.255, so discovery looks portable, but I did not trace registration or the court-IP path); the OrionUpdater channel; and the licence activation path against api.zaeorion.com, which this rig bypasses entirely via ORION_AUTO_UNLOCK_LOCAL (run_orion.local.ps1:136) \u2014 meaning the real activation flow a customer hits has not been exercised here. That last one deserves its own look before Saturday."
}

---

## agent `af2a2052db50363b3`

{
  "headline": "The production build itself is healthy \u2014 it compiles clean at HEAD e1c5c2e1 and passes 429/429 unit tests + 9/9 ctest suites \u2014 but a shipped build stops firing ~15 minutes after activation, because ORION_PRODUCTION force-enables the lease gate while the currently-deployed backend never returns the `lease_sig` the client now demands.",
  "findings": [
    {
      "title": "Production force-enables the lease gate, but the DEPLOYED backend never signs the lease \u2192 automation hard-stops ~15 min after activation",
      "evidence": "Build-time force-on: native_orion/src/LeaseGate.cpp:35-38 (`#ifdef ORION_PRODUCTION_BUILD ... return true;` \u2014 no env can disable it; dev returns the ORION_LEASE_GATED_FIRE opt-in, which is NOT set anywhere in run_orion.local.ps1, so the dev rig never exercises this). Client now REQUIRES a signature: native_orion/src/LeaseGate.cpp:156-166 returns a non-empty 32-byte key (`nKx5ximQYHX1/eMB9m7tzcPtO/gtSBIaCDd4BiT4itw=`); native_orion/src/OrionAppController.cpp:1753-1759 \u2014 `if (!leaseKey.isEmpty() && !verifyLeaseSignature(...)) { log(\"lease signature INVALID \u2014 lease not refreshed\"); return; }` skipping recordHeartbeatOk. LeaseGate.cpp:141 returns false the moment `signatureBase64Url.isEmpty()`. Deployed server does not send it: discord_launch/deployed_bundle/orion-activate/lambda_deploy.zip (lambda_function.py entry dated 2026-08-02 09:49) contains `LEASE_TTL_S = 900` and `handle_validate` returning ONLY {ok, license_key, machine_id, lease_expires_at, tier}; `def sign_lease` is ABSENT and the string `lease_sig` does not occur anywhere in the deployed file. The repo copy backend/lambda_function.py:1332-1336 does sign, but fail-soft inside try/except. Live probe: GET https://api.zaeorion.com/api/version \u2192 200 {\"service\":\"orion-activate\",\"version\":\"0.4.0\"} \u2014 that is the same function the stale zip deploys. Timeline: OrionAppController.cpp:1701 seeds the lease via LeaseGate.cpp:51-57 at min(now+900s, token_expiry) = 15 min; heartbeat every 5 min (OrionAppController.cpp:1790) is rejected; LeaseGate.cpp:84-96 then fails BOTH the wall-clock check (nowEpochS < leaseValidUntilEpochS_) and the 15-min monotonic staleness check; AutomationAccessPolicy.h:18-31 returns false when leaseGateEnabled && !leaseFireAllowed; OrionAppController.cpp:7569-7586 wires that into automationSecurityAllowed(), and :5574-5581 blocks Remote Play with \"Lease expired (server lease required)\". docs/SERVER_DEPLOY_CHECKLIST.md:41-44 states the required ordering explicitly \u2014 \"Deploy the server lease-signing FIRST ... THEN embed the public key and rebuild the client\" \u2014 and the embed happened first. Not circular: this is a compile-time flag + a byte-level absence in a deployment artifact, no derived timing metric involved.",
      "fix": "Do NOT ship until one of these is true, verified by a real >20-minute production session (the failure is invisible in any run shorter than 15 min): (1) preferred \u2014 deploy the current backend/lambda_function.py to the orion-activate function AND provision SSM SecureString /orion/lease_signing_key with the Ed25519 private half matching LeaseGate::leaseVerifyPublicKey(), then confirm a /api/license/check response carries lease_sig; or (2) if the server cannot ship first, make the client tolerate the transition explicitly rather than silently: in OrionAppController.cpp:1753-1759 keep the hard requirement but add a startup log + UI surface of leaseGate_.stateText(), and gate the requirement on a build flag the release engineer must flip. Never 'fix' this by weakening AutomationAccessPolicy::allowed \u2014 the gate failing closed is correct; the server is what is missing. Add a lease_signing:true field to /api/version so the client can self-report the mismatch at launch instead of dying 15 minutes in.",
      "refutes_premise": "",
      "severity": "blocker",
      "ships_broken": true
    },
    {
      "title": "No production package can be built today: both packager guards and the sidecar freshness check REFUSE (real output captured)",
      "evidence": "Ran the packager's own guards against the current tree with .venv/Scripts/python.exe: `require_production_build()` \u2192 \"[orion-package] REFUSED: build was not configured with -DORION_PRODUCTION=ON\" (tools/package_orion_release.py:799-819 reads BUILD_DIR.parent/CMakeCache.txt = native_orion/build/CMakeCache.txt, which has ORION_PRODUCTION:BOOL=OFF); `require_pinned_production_libcrypto()` \u2192 \"[orion-package] REFUSED: production build has no valid ORION_LIBCRYPTO_SHA256 compile-time pin\" (tools/package_orion_release.py:822-845; native_orion/build/CMakeCache.txt has ORION_LIBCRYPTO_SHA256 empty). Sidecar: tools/sidecar_bundle_manifest.verify_manifest(ROOT, build/sidecar/autogreen_sidecar.dist) returns 6 failures \u2014 \"sidecar build input digest mismatch\", \"sidecar source digest mismatch\", \"source files changed after sidecar build: chiaki_backend.py, compressed_meter_reader.py, latency_estimator.py, native_orion/backend/autogreen_sidecar.py, nexus_svc.py, remote_play_orchestrator.py, simple_meter_reader.py\", \"model inputs changed after sidecar build: models/latency_factory_prior.json\", \"compiled sidecar source identity mismatch\", \"bundled model differs from source input\". tools/package_orion_release.py:287-299 turns that into a hard SystemExit. The bundle was built 2026-08-03T02:19Z; 20+ commits have landed since. The last actual package (release/orion-package/OrionNative.exe, release_manifest.json) is dated Jul 6 and predates the compiled-sidecar requirement entirely \u2014 it contains NO OrionSidecar.exe, so it would fail validate_staged_package_for_publish (tools/package_orion_release.py:474-488) today.",
      "fix": "Release prep sequence, in order: (1) `cmake -S native_orion -B native_orion/build -DORION_PRODUCTION=ON -DORION_LIBCRYPTO_PATH=<exact libcrypto-3-x64.dll>` and rebuild Release \u2014 note the packager hard-codes BUILD_DIR=native_orion/build/Release, so the production configure must land in that dir, not a side dir; (2) `scripts/build_orion_sidecar.ps1` to regenerate build/sidecar/autogreen_sidecar.dist against current sources; (3) re-run the two guard functions above as a pre-flight before invoking the packager.",
      "refutes_premise": "",
      "severity": "blocker",
      "ships_broken": false
    },
    {
      "title": "A non-elevated first launch of an installed build hard-locks automation with 'Settings signature missing or invalid' and cannot recover",
      "evidence": "installer/orion.iss:38 `DefaultDirName={autopf}\\Orion` + :43 `PrivilegesRequired=admin` \u2192 install target is C:\\Program Files\\Orion. The 127-line .iss has NO [Dirs] section and no Permissions directive, so the install dir keeps default Program Files ACLs (standard users: read/execute only). Every piece of mutable state is install-dir relative and there is no per-user fallback: native_orion/src/SecurityManager.cpp:834-842 (settings.json, settings.json.sig), :844-847 (.vault/orion_entitlement.cache), native_orion/src/AppConfig.cpp:133-136/154-159 (settings.json, learning.json), native_orion/src/OrionAppController.cpp:4895 and :10142 (logs/). grep for QStandardPaths across native_orion/src returns only DesktopLocation (OrionAppController.cpp:4870) and findExecutable \u2014 no AppDataLocation redirection anywhere. In production, main.cpp:294-298 pins rootDir to applicationDirPath (dev-root fallback compiled out). Lock chain: a fresh install ships NO settings.json (release/orion-package/release_manifest.json lists only security_policy.json among top-level .json), so SecurityManager.cpp:820-825 settingsDigest() returns empty \u2192 :381-389 verifySettingsSignature() false \u2192 the dev auto-resign at :302-308 is skipped because it is guarded on `!status.releaseManifestRequired` and production forces that true (:572-585) \u2192 :344-348 sets securityLockActive = true. The two dev escapes are compiled out: OrionAppController.cpp:1607-1615 (#ifndef ORION_PRODUCTION_BUILD around the ORION_AUTO_SIGN_SETTINGS hook) and :10261-10267 (#ifndef around the local-dev lock bypass). The only writer is a settings save (OrionAppController.cpp:10149-10165 / :10167-10182), first reached at the legal gate (:7321-7331) \u2014 which needs a writable install dir. Inno's postinstall [Run] entry (installer/orion.iss:94-95) launches elevated by inheritance, which is exactly why this is invisible in a smoke test: the FIRST launch works and every later Start-Menu launch is asInvoker (no manifest requesting elevation exists anywhere in native_orion/CMakeLists.txt or the src tree).",
      "fix": "Redirect mutable state off the install dir: add a rootDir-vs-dataDir split so settings.json / settings.json.sig / learning.json / logs/ / .vault/ resolve to QStandardPaths::AppDataLocation (or {localappdata}\\Orion) while release_manifest.json / security_policy.json / binaries stay install-relative and manifest-verified. That is a real code change in SecurityManager.cpp + AppConfig.cpp + the logs paths in OrionAppController.cpp. Stop-gap if that cannot land before release: change installer/orion.iss to DefaultDirName={localappdata}\\Programs\\Orion with PrivilegesRequired=lowest (keeping the driver installs as elevated [Run] steps), or add a [Dirs] entry granting users-modify on {app}. Do not 'fix' it by relaxing the settings-signature lock.",
      "refutes_premise": "",
      "severity": "blocker",
      "ships_broken": true
    },
    {
      "title": "REFUTED: the sidecar-interpreter premise. A production build never resolves a Python interpreter at all",
      "evidence": "native_orion/src/RemotePlaySession.cpp:2053-2059 sets productionBuild=true under ORION_PRODUCTION_BUILD and passes it to shouldUseCompiledSidecar (native_orion/src/SidecarLaunchPolicy.h:9-13, `return productionBuild || developmentOptIn;`) \u2014 the build bit alone forces true. :2063 then short-circuits: `const QString py = useCompiledSidecar ? QString() : pythonExecutable();`. pythonExecutable() (RemotePlaySession.cpp:1688-1735, the function that probes .venv311 / applicationDir/python / Miniconda / PATH with a cv2 import test) is therefore never called in production. Instead :2066-2075 requires applicationDirPath()/OrionSidecar.exe and fails closed with \"Production package is incomplete: required OrionSidecar.exe not found\". The packager guarantees that file is present (tools/package_orion_release.py:287-299 and release_filter_policy.py COMPILED_SIDECAR_REQUIRED_FILES). Separately, run_orion.local.ps1:146 pins .venv while pythonExecutable() probes .venv311 \u2014 a genuine mismatch, but it is dev-only and masked because the launcher exports ORION_PYREMOTEPLAY_PYTHON, which is candidate #1.",
      "fix": "No fix needed for production. If you want the dev path consistent, align the .venv311 candidate in RemotePlaySession.cpp:1696/1698 with the .venv the launcher actually creates, so a dev without the env var set does not silently fall through to a Miniconda env.",
      "refutes_premise": "The brief's premise 'does the sidecar interpreter resolve? run_orion.local.ps1 pins $PSScriptRoot\\.venv\\Scripts\\python.exe. A customer has no .venv. What does auto-discovery do' does not apply to production \u2014 auto-discovery is compiled past, not merely unused. The customer-facing failure mode is a missing OrionSidecar.exe, which the packager already blocks.",
      "severity": "minor",
      "ships_broken": false
    },
    {
      "title": "REFUTED: models/*.pt are already excluded from the release bundle \u2014 there is no bundle-size/AV-surface decision left to make",
      "evidence": "The packager never copies models/ at all: tools/package_orion_release.py:390-419 copies top-level files matching should_copy_top_file (:138-148), TOP_LEVEL_DIRS (:87-95), then only 'assets' and 'meter_styles', the chiaki runtime, two backend .py scripts and the sidecar dist. Empirically release/orion-package contains zero .pt and zero .onnx (find returned nothing over a 491 MB tree). Belt-and-braces: tools/release_filter_policy.py:113-118 approves exactly {latency_factory_prior.json, tip_registration.json} and :189-197 flags every other models/ path, enforced at package_orion_release.py:313-314 and :442-444. The sidecar bundle's models/ dir holds only those two JSONs; scripts/build_orion_sidecar.ps1:106 passes --nofollow-import-to=torch,torchvision,ultralytics,scipy,mediapipe,onnxruntime, and the 181 MB dist contains only cv2/numpy/pywin32/windows_capture. The reader itself is pure CV: grep for 'locator|torch|onnx|ultralytics' in simple_meter_reader.py returns ZERO hits. ORION_METER_LOCATOR is read only by meter_detector.py and meter_locator_infer.py (the retired chain), which cannot be selected: native_orion/src/RemotePlaySession.cpp:2178 inserts ORION_SIMPLE_READER=1 into the sidecar env unconditionally (outside any useCompiledSidecar branch), and remote_play_orchestrator.py:1319 now defaults it to '1'. models/ on disk is 59 MB of dev baggage that already never ships.",
      "fix": "None required. Two stale docs should be corrected so this does not get re-litigated: docs/DETECTION_PLAN.md:28 still claims ORION_SIMPLE_READER is 'OFF by repo evidence' because the env insert used to sit inside `if (useCompiledSidecar)` \u2014 it no longer does (RemotePlaySession.cpp:2178); and the brief's cited orchestrator lines :586-631 are stale, the selection block is now remote_play_orchestrator.py:1307-1384.",
      "refutes_premise": "The premise 'a launcher comment claims ORION_SIMPLE_READER=1 takes precedence ... which would make ORION_METER_LOCATOR and the .pt files inert and removable from the bundle' is correct about precedence but wrong that a removal decision is pending: the .pt files are not in the bundle and are actively rejected by two independent filters if they ever appear.",
      "severity": "cosmetic",
      "ships_broken": false
    },
    {
      "title": "Production compiles out the packet-bridge \u2192 timing feed entirely, so any RTT/court-derived timing feature is permanently dead in a shipped build",
      "evidence": "native_orion/src/OrionAppController.cpp:3183-3200: under ORION_PRODUCTION_BUILD `constexpr bool explicitDevelopmentExperiment = false;` and the trust argument is hard-coded ServiceIdentityTrust::Unverified, so packet_bridge_authority::forwardToTimingIfAuthorized never calls remotePlay_.observePacket. The compile-time fence is native_orion/src/PacketBridgeAuthority.h:22-29 (`(void)explicitDevelopmentExperiment; return false;`). observePacket (RemotePlaySession.cpp:1117-1129) is the ONLY path that ships observe_packet into the sidecar, which is what feeds court-IP detection and the RTT-adaptive inbound delay. This matches the existing note that manual_meter_delay_ms is a silent no-op locally \u2014 in production it is not merely un-engaged, it is unreachable by construction.",
      "fix": "No code change unless one of those features is meant to ship. If it is: the fence requires ServiceIdentityTrust::VerifiedService (PacketBridgeAuthority.h:19-21), so the work is proving privileged service identity for the loopback listener, not flipping a flag. Until then, remove or grey out any UI/settings that imply RTT-adaptive delay works, so a customer is not tuning a dead knob.",
      "refutes_premise": "",
      "severity": "minor",
      "ships_broken": true
    },
    {
      "title": "The orphaned-sidecar reaper only ever kills python.exe, so it can never reap a production OrionSidecar.exe holding the capture card",
      "evidence": "native_orion/src/RemotePlaySession.cpp:2042-2045 runs `taskkill.exe /FI \"WINDOWTITLE eq ActiveMovie Window\" /IM python.exe /F /T`. In production the sidecar image is OrionSidecar.exe (RemotePlaySession.cpp:2066-2068), so the /IM filter never matches. The comment at :2030-2041 explicitly describes the target as 'a python process holding a CAPTURE DEVICE'. New launches are still covered structurally by the KILL_ON_JOB_CLOSE job object mentioned at :2041, so the exposure is limited to a sidecar orphaned by a force-kill or crash of a previous OrionNative.",
      "fix": "In RemotePlaySession.cpp, run the same taskkill filter for /IM OrionSidecar.exe (or make the image name follow the resolved sidecar executable) so the production image is reaped on the same terms as the dev image.",
      "refutes_premise": "",
      "severity": "minor",
      "ships_broken": true
    }
  ],
  "confidence_notes": "WHAT I ESTABLISHED WITH REAL OUTPUT. The production build works: `cmake --build native_orion/build_prod_codex --config Release` (cache has ORION_PRODUCTION:BOOL=ON, ORION_LIBCRYPTO_SHA256 pinned) completed at HEAD e1c5c2e1 with exit 0 and ZERO compiler/linker errors \u2014 the only diagnostic in the whole log is `main.cpp(299,30): warning C4127: conditional expression is constant`, which is the production `const bool allowDevRootFallback = false;` branch. native_orion/src is clean in git, so that build reflects committed HEAD. The production-compiled binaries then RUN: OrionNativeTests.exe \u2192 \"Totals: 429 passed, 0 failed, 0 skipped\" and `ctest -C Release` \u2192 9/9 suites passed (18.0 s). The dev baseline quoted in the brief (422 passed / 5 skipped) differs because the 5 skips are `QSKIP(\"OpenCV not enabled\")` (AutomationEngineTests.cpp:4812-4906) and this production cache has OpenCV_DIR set \u2014 not a production-vs-dev behaviour difference.\n\nFULL GATE ENUMERATION (24 sites, all read): compile-out of dev license paths (OrionAppController.cpp:3397-3423 ORION_AUTO_UNLOCK_LOCAL + ORION_LICENSE_KEY, :3884-3909 NVDEV- offline key, :7557-7564 localDevAllowed\u2192false, :7569-7570 localDevBypass\u2192false, :10261-10267 lock bypass); update-gate dev detection (:1504-1514 CMakeCache probe); debug UI (OrionAppController.h:563-567); dev harness (:1628-1634 ORION_FORCE_VIRTUAL_NEUTRAL); settings auto-sign (:1607-1615); diagnostics marker (:4735-4739). Load-bearing rather than convenience: LeaseGate.cpp:35-38 (finding 1), SecurityManager.cpp:574-584 mandatory release manifest + :445-455 no test pubkey override (finding 3's precondition), RemotePlaySession.cpp:2053-2076 compiled sidecar only (finding 4), :58-64 + AppConfig.cpp:37-43/90-95 bundled-OrionStream-only executable policy, SidecarLaunchPolicy.h:18-22 forced decoded-frame pipe, Ed25519.cpp:208-232 hash-pinned libcrypto (a mismatch here would make ed25519Available() false \u2192 \"Release manifest verifier unavailable\" \u2192 total lock; the packager guards it at package_orion_release.py:822-845), main.cpp:294-298 dev-root gate, PacketBridgeAuthority.h:22-29 (finding 6).\n\nVERIFIED NON-ISSUES. Authenticode enforcement is opt-in and OFF in the shipped policy: SecurityManager.cpp:675-686 returns false unless security_policy.json carries enforce_authenticode, and write_security_policy (package_orion_release.py:247-257) does not emit that key \u2014 an unsigned legitimate build is not bricked. TLS pinning is live-correct: I fetched the real chain with openssl and the SPKI SHA-256s are CN=WE1 \u2192 908769e8\u2026afc0e (matches NetworkSecurity.cpp:42) and GTS Root R4 \u2192 9847e565\u2026d84347 (matches :44); WE1 is valid to 2029-02-20 and the pin is an issuer-SPKI pin, not a leaf pin, so Cloudflare's 90-day leaf renewals do not brick it. The live API is up: GET /api/version \u2192 200, service \"orion-activate\", version 0.4.0, accepted by LicenseClient.cpp:69. settings.json is NOT hashed into release_manifest.json (the Jul 6 manifest's 2789 files include security_policy.json only), so runtime settings writes cannot break integrity verification \u2014 the writability problem in finding 3 is about permissions, not about the manifest.\n\nWHAT I COULD NOT ESTABLISH, AND HOW TO SETTLE IT. (1) I did not launch the production GUI. Two concrete reasons: native_orion/src/main.cpp:275 calls registerOrionProtocolHandler() on EVERY launch, which repoints the HKCU orion:// handler at whichever exe started \u2014 launching my scratch build would hijack the user's activation deep link until their real app restarts; and the singleton guard at main.cpp:264-271 only short-circuits when a deep-link URI is present, so a second full instance would have come up beside the user's live OrionNative.exe (PID 23796) and contended for ViGEm/the capture device. To settle finding 3 empirically, install a real package into C:\\Program Files\\Orion, log out of the elevated session, and launch from the Start Menu as a standard user: expect \"Settings signature missing or invalid\" in the security pill and \"Settings save failed\" in logs/orion_native.log. (2) Whether the AWS orion-activate function currently running is byte-identical to discord_launch/deployed_bundle/orion-activate/lambda_deploy.zip. That zip is the user's own deployment artifact dated 2026-08-02 and its handle_validate demonstrably lacks lease_sig, but it is not proof of the live function. Settle it in 2 minutes: grep CloudWatch for \"[LEASE] signing unavailable (fail-soft)\", or `aws ssm get-parameter --name /orion/lease_signing_key`, or add a lease_signing field to handle_version so /api/version answers it directly. I could not test /api/license/check myself \u2014 it is behind require_edge_auth (backend/lambda_function.py:1259-1278) whose shared secret the Cloudflare worker injects, and I have no valid license key. (3) I did not run the packager end-to-end; both of its production guards refuse today (finding 2), so any end-to-end claim would have required reconfiguring native_orion/build, which other agents are building in."
}

---

## agent `a7fdda46c27288881`

{
  "headline": "The decoder path already ran live on this rig today and its measured end-to-end loop is 227.0 ms vs the capture card's 234.5 ms \u2014 a 7.5 ms difference (95% CI \u22120.4 to +15.4), so the claimed ~150 ms win is refuted by a factor of ~10 and this batch should not be spent on the latency hypothesis.",
  "findings": [
    {
      "title": "REFUTED: the decoder path does not remove ~150 ms \u2014 it removes 7.5 ms \u00b1 8 (measured, same rig, same day, interleaved sessions)",
      "evidence": "The engine's own accepted latency label is the correct instrument and it is video-path-sensitive: `lat_total = t_star - float(self._pending_release_ms)` (latency_estimator.py:1556), where t_star is derived from the fill series fed with the frame's CAPTURE-EPOCH stamp (remote_play_orchestrator.py:4853 `_snap_latency_estimator.update(_frame_wall_ms, ...)`), and `_pending_release_ms` is the PC epoch stamp of the release COMMAND (RemotePlaySession.cpp:1082-1087: \"wall_ms is epoch ms (same clock as the sidecar's time.time()*1000 fill samples) so the oracle's t*-minus-release subtraction is exact\"; orchestrator side remote_play_orchestrator.py:5318 `ts = float(wall_ms) if wall_ms is not None else time.time()*1000.0`). The whole video return path therefore sits inside the label. Attributing every accepted label in logs/orion_native.log by exact session identity (the \"Sidecar env keys:\" line at session start \u2014 ORION_FRAME_PIPE vs ORION_CAPTURE_CARD, not the lagging Capture-health tier): DECODER n=8 mean 227.0 sd 6.6 median 224.9 (223.6, 233.0, 239.8, 222.1, 226.3, 220.2, 228.8, 222.1; sessions starting 2026-08-04T05:21:28Z and T06:30:41Z). CAPTURE-CARD, same log, same estimator build (latency_estimator.py mtime 2026-08-04T00:14, committed 968b127f 01:02, i.e. before every label counted) n=58 mean 234.5 sd 22.4. Welch: diff 7.5 ms, se 3.77, t=2.00, df 35.5, 95% CI [-0.4, +15.4] ms. The arms are naturally interleaved on the same rig (capture 05:18 / decoder 05:21 / capture 06:28 / decoder 06:30 / capture 07:10), so between-session drift is not confounding. NON-CIRCULAR: nothing here touches tip_eta_ms / command_eta_ms / lead_ms / travel_pp / peak-fillAtRel \u2014 t_star is the freeze-ONSET bracket midpoint of the trailing settled run (latency_estimator.py:1524-1535), and the ~72-82 ms label-bias correction (AutomationEngine.cpp:9100-9106, ORION_LEAD_BIAS_MS, present in the sidecar env of both arms) is common-mode and cancels in the difference. Corroborating collapse of the sub-claims: (a) the \"Elgato ~120-195 ms, central ~160 ms\" figure is a residual, not a measurement, and the repo's own device estimate is `HD60X_DEVICE_LATENCY_ESTIMATE_MS = 35.0` (capture_card_backend.py:110) \u2014 and it is not even applied by default (`self._latency_ns = ... ORION_CAPTURE_DEVICE_LATENCY_MS, 0.0`, capture_card_backend.py:494); (b) docs/LATENCY_FACTORY_PRIOR.md:89 assigns the decoder-vs-card VIDEO differential a symmetric \u00b133.3 ms uniform prior CENTRED ON ZERO because \"which is slower depends on encoder queue depth and card buffering\" \u2014 the project's own declared ignorance is \u00b133 ms, not \u2212150 ms; (c) \"the loop is ~300 ms\" conflates the APPLIED lead with a measured loop: the log line at 2026-08-04T06:30:59.432Z reads `lead_kind=factory lead_ms=301.241` = the 218.5 ms prior (models/latency_factory_prior.json capture-card-pipe mean_ms) plus the ORION_LEAD_BIAS_MS correction; the measured loop is 227-235 ms; (d) the residual is dominated by terms IDENTICAL in both arms \u2014 the PC\u2192PS5 input transport is the same OrionStream pre-encryption pipe in both modes, and the game's own input\u2192meter-render reaction is untouched by the video source. Finally, the horizon-error figures quoted in the brief (rMAD 6.5 / 15.7 / 23.9 / 45.9) exist nowhere in the repo (`grep -rni 'rMAD'` over all .py/.md/.cpp/.json returns only numpy's `hermadd`).",
      "refutes_premise": "Yes \u2014 the central premise. \"Removing ~150 ms would move the firing decision from the 300-400 bucket to 100-200\" is false: the decoder path removes 7.5 ms (95% CI [-0.4, +15.4]). The supporting premise that the Elgato is \"~160 ms, roughly HALF the loop\" is also false; the repo's own estimate is 35 ms and its own prior says the differential's SIGN is unknown. The horizon-vs-rMAD table cited as measurement does not exist in the tree.",
      "ships_broken": false,
      "severity": "blocker",
      "fix": "Do not spend a batch on the latency hypothesis. Concretely: (1) do NOT flip video_source for latency reasons; (2) fold the 8 decoder labels into models/latency_factory_prior.json so the decoder-pipe profile stops being evidence_n=0 \u2014 but FIX the builder's attribution first (see the separate finding), because running it today contaminates the decoder sample with two capture-card labels including the corpus minimum 151.8; (3) if a live batch is spent at all, spend it on the two questions that ARE open: does ORION_COMPRESSED_READER=1 improve fill accuracy on the decoder path, and is the decoder route's latency VARIANCE genuinely lower (see the variance finding)."
    },
    {
      "title": "The decoder path WORKS today at a clean 60/60 fps \u2014 two of the three documented 'unhardened' blockers are stale docs, not live defects",
      "evidence": "Three live decoder sessions on 2026-08-04 (logs/orion_native.log): 05:21:28Z, 06:30:41Z, 07:09:5xZ. Sustained health across the middle session, 06:30:49Z\u219206:31:44Z, 8 consecutive samples: `Capture health: tier=decoder uniqfps=60 dup%=0 export_fps=60 gap_fps=0 cv_fps=60 detect_ms=0.9-1.3`, preview `source_fps=59.9-60.1 jitter_drop=0 underflow=0`, zero wire rejects, detection locked (fill_pct 17.5-24.2 on SCHEDULE FIRE REJECT lines), and 6 accepted latency labels \u2014 which require a clean rising-flank fill series plus a detectable freeze onset. Frame ages 7.1-18.3 ms. This refutes two standing claims: (a) the \"~38 fps software-decode cap\" (native_orion/src/AppConfig.h:96-98 \"The ~38fps software cap (CPU contention) is the trade\"; docs/HW_DECODE_READBACK_FOR_GLM.md:5-8 \"stuck on software decode \u2014 which tops out at ~38 unique fps\") \u2014 measured 60/60 unique at software decode, hw_decoder is empty by default (RemotePlaySession.cpp:1270-1276, ORION_HW_DECODE unset); (b) the \"export starves because it is coupled to the Vulkan swapchain present\" root cause (docs/STUTTER_ROOT_CAUSE_AND_FIX.md) \u2014 the fork moved the export onto the DECODE CALLBACK: C:\\Users\\aaron\\Desktop\\chiaki-ng-src gui/src/streamsession.cpp:625-626 (`orion_frame_export = new OrionFrameExport(...); chiaki_ffmpeg_decoder_set_frame_decoded_cb(ffmpeg_decoder, FfmpegDecodedFrameCb, this)`) \u2192 streamsession.cpp:2983-2984 (`frame_export->exportFrame(frame)`), invoked from lib/src/ffmpegdecoder.c:386-387, entirely off the present path. The export gate was also de-aliased (SidecarLaunchPolicy.h:36-39 `kRemotePlayFrameExportGateFps = 120`, vs the exporter's own 30 fps default at chiaki-ng-src gui/src/orionframeexport.cpp:81-88). Note the brief's supporting quote is itself incoherent: AppConfig.h:87-88 says capture_card is \"DEFAULT while we harden the HDMI path\" \u2014 but capture_card IS the HDMI path, so that comment cannot be read as evidence that the decoder path is the unhardened one.",
      "refutes_premise": "Partly. \"The decoder path exists but is unhardened\" is too strong as stated, and the two most-cited hardening gaps (38 fps software cap; present-coupled export starvation) are stale documentation superseded by fork commits 1ea25990 / 25d6766f. The real gaps are different ones, enumerated in the separate findings.",
      "ships_broken": false,
      "severity": "major",
      "fix": "Correct the stale comments so the next audit is not misled: native_orion/src/AppConfig.h:96-103 (drop the ~38 fps claim; the ceiling was the export gate, already fixed) and AppConfig.h:87-88 (the \"while we harden the HDMI path\" clause is self-contradictory \u2014 say what is actually being hardened). Mark docs/HW_DECODE_READBACK_FOR_GLM.md and docs/STUTTER_ROOT_CAUSE_AND_FIX.md as SUPERSEDED with a pointer to chiaki-ng-src 1ea25990. No code change."
    },
    {
      "title": "The whole compressed-stream reader workstream is unreachable in production: ORION_COMPRESSED_READER is set by nothing, so the decoder path runs the pristine-tuned SimpleMeterReader",
      "evidence": "`RemotePlaySession.cpp:2178` unconditionally does `env.insert(\"ORION_SIMPLE_READER\", \"1\")` on ALL paths. The selector at remote_play_orchestrator.py:1325 reads `os.environ.get('ORION_COMPRESSED_READER', '0')`, and a whole-repo grep finds no launcher, script, installer or native source that sets it \u2014 only docs and the reader itself (compressed_meter_reader.py:22, docs/DETECTION_PLAN.md:29 \"ORION_COMPRESSED_READER | '0' | nowhere | OFF\"). So the entire M0-M7 body of work \u2014 CompressedMeterReader, luma_meter.py, stream_quality.py's q_frame/q_session controller, the BT.709 rung prior seeded by `set_stream_profile` (remote_play_orchestrator.py:1346, the fix for design bug #1 in docs/COMPRESSED_PATH_DESIGN.md) \u2014 is dead code on the live decoder path. Confirmed against the live sessions: the 2026-08-04 decoder runs logged `Tip registration ENABLED` and `Measured-latency oracle ENABLED` but no `CV detector = CompressedMeterReader` line anywhere in either log. This matters because the decoder arm is exactly where the compressed reader was built to help, and because the rig's persisted stream preset is the WORST rung: every `Streaming preset:` line in logs/orion_native.log and .log.1 reads `Balanced (720p60 / H264 / 4 Mbps)` \u2014 the rung docs/COMPRESSED_PATH_DESIGN.md rates at a 68-78% P(made) ceiling with \"acquire-blind chroma + green-tip death\" as the binding constraint.",
      "refutes_premise": "No \u2014 it sharpens the cost side. Any decoder A/B run today measures 'compressed video + a reader tuned for pristine 1080p', not 'the decoder path'.",
      "ships_broken": false,
      "severity": "major",
      "fix": "Two options, both cheap. Preferred: in RemotePlaySession.cpp, inside the existing decoder branch (the `requireRemotePlayFramePipe` / `frameExportEnabled` arms around :2200-2216), add `env.insert(\"ORION_COMPRESSED_READER\", \"1\")` so the compressed reader is selected exactly when the source is the decoder pipe \u2014 the class subclasses SimpleMeterReader and asserts `for_quality(1.0) == ReaderParams()`, so it collapses to identical behaviour on a pristine feed. NOTE: I am not permitted to edit RemotePlaySession.cpp is not in the do-not-touch list, but AutomationEngine.{cpp,h} is \u2014 this patch does not need those files. Minimum alternative if you want zero code motion: run the A/B with ORION_COMPRESSED_READER=1 exported in the launch environment and record which reader logged, so at least the arm is labelled."
    },
    {
      "title": "build_latency_factory_prior.py mis-attributes labels across a session boundary, and today that contamination points in the direction of the hypothesis under test",
      "evidence": "Video-source attribution is last-seen `Capture health: tier=` (tools/timing/build_latency_factory_prior.py:105 `_VIDEO_TIER_RE`, applied at :145), but that line is emitted only every ~5-10 s and the first one of a session lags the session start by 15-20 s. Running the builder's own `collect()` over logs/orion_native.log + .log.1 today yields ('decoder','pipe') n=10 mean=220.0 sd=24.7 \u2014 but two of those ten labels belong to CAPTURE-CARD sessions: 151.8 ms at 2026-08-04T06:28:51Z (session started 06:28:41Z with ORION_CAPTURE_CARD in its env; first capture-card health line 06:28:54Z) and 232.0 ms at 07:11:10Z (session started 07:10:55Z, first health line 07:11:12Z). The correct decoder set is n=8 mean=227.0 sd=6.6. The 151.8 ms label is the minimum of the entire 107-label corpus, so the bug both drags the decoder mean 7 ms low and inflates its sd 3.7x \u2014 i.e. it manufactures a small version of exactly the 'decoder is faster' story being audited, and simultaneously destroys the one real decoder signal (low variance).",
      "refutes_premise": "No, but it would have produced a false confirmation of it if anyone had run the tool to 'check' the decoder route.",
      "ships_broken": false,
      "severity": "major",
      "fix": "In tools/timing/build_latency_factory_prior.py, add a session-start regex alongside `_VIDEO_TIER_RE` and let it win: match `Sidecar env keys: (?P<keys>.*)` and set `tier = 'capture_card' if 'ORION_CAPTURE_CARD' in keys else ('decoder' if 'ORION_FRAME_PIPE' in keys else tier)`. That line is emitted exactly once, at session start, before any label can arrive, and it is the same key the runtime branches on (RemotePlaySession.cpp:2200-2262). Re-run `--write` after the fix; the decoder-pipe profile then gets evidence_n=8, mean 227.0, sd from the real posterior instead of the \u00b133.3 ms declared-ignorance widening in docs/LATENCY_FACTORY_PRIOR.md:85-99."
    },
    {
      "title": "Detection cost of the decoder path, quantified \u2014 and it lands on the same sigma budget the horizon reduction was supposed to free",
      "evidence": "logs/diagnostics/reencode_ladder/session_20260704_210801/report.txt, the only quantitative artifact that exists. At Balanced (720p60/4 Mbps \u2014 the rig's actual persisted preset), dup mode (realistic per-frame bit budget): detection recall 96.5%, per-phase recall rising 90.5% / peak 99.0 / spent 99.6, fill |\u0394| pp mean 3.95 median 0.96 p90 14.43, RED mask survival median 0.70, GREEN tip survival median 0.0. In seq mode (motion stress): recall 91.9%, rising 79.4%, fill p90 32.66, RED survival median 0.26, GREEN tip median 0.0. Green-tip survival median is 0.0 at EVERY rung including UltraLow. The fill-error tail is the load-bearing cost, not recall: the predictor sigma it inflates enters `decision.combinedSigmaMs = sqrt(decision.sigmaMs\u00b2 + leadSigmaMs\u00b2 + tickSigmaMs\u00b2)` gated at `<= config_.autonomousTipMaxCombinedSigmaMs` (AutomationEngine.cpp:9599-9609) \u2014 the same 65 ms budget whose feasibility window docs/LATENCY_FACTORY_PRIOR.md:118-130 computes. So compression consumes precisely the budget a shorter horizon would have freed. Second cost, structural: the capture card is opened at 1920x1080 (capture_card_backend.py:352-353 `width: int = 1920, height: int = 1080`) while the decoder arm inherits the stream preset's `sidecarResolution` \u2014 1280x720 at Balanced/Performance (RemotePlaySession.cpp:203, 213). Switching source today therefore also drops 1080p\u2192720p (2.25x fewer pixels on a meter whose priors are px@1080p, simple_meter_reader.py:1006 `_REF_W, _REF_H = 1920.0, 1080.0`). Counter-evidence bounding the harm: the 06:30-06:31 live decoder session read fills cleanly at 720p/4M and produced 6 accepted latency labels, so live detection is at least label-grade \u2014 but that is ~90 s of evidence. Ladder caveats stated in the report itself: x264 is not the PS5 hardware encoder, the dump cadence is ~15 fps vs the 60 fps design point, relative comparisons only.",
      "refutes_premise": "No \u2014 it confirms the brief's stated worry (green tip does not survive) and adds the number the brief lacked: the fill p90 error, 14.4 pp at rest and 32.7 pp under motion, which is what actually costs the timing gate.",
      "ships_broken": false,
      "severity": "major",
      "fix": "Nothing to patch here; this is the price sheet. If the decoder path is ever promoted, it must ship with ORION_COMPRESSED_READER=1 (see that finding) and the rung must be pinned to Performance (720p60/12 Mbps) or Quality (1080p60/12 Mbps) rather than the persisted Balanced 4 Mbps \u2014 RemotePlaySession.cpp:200-219; the AppConfig.h:128 default is already \"Performance\", it is the saved settings.json that is on Balanced."
    },
    {
      "title": "The decoder path is universal, not Remote-Play-only: the 'capture-card scenario' already runs a full Remote Play session for input, and there is no console-direct actuation path at all",
      "evidence": "In capture-card mode OrionStream still performs the complete PS5 handshake and is used input-only, with video discarded before decode: remote_play_orchestrator.py:1835 passes `disable_video=bool(self._cc_mode)` into remote_play_client.py:459 `chiaki_controller_env(disable_video)`, which at :499-500 sets `CHIAKI_ORION_DISABLE_VIDEO=1` and at :502 explicitly pops it on every other route \"so decoder-pipe users always retain decoded video\". SidecarWatchdog.h:202 states it directly: \"in capture-card mode Chiaki is INPUT-ONLY (the card is the video)\". Live corroboration: capture-card sessions log the identical `Controller route isolation: direct Chiaki pipe owns PS5 input; desktop XUSB held neutral` and a connected input-hook heartbeat, same as decoder sessions. Orion's only actuation route in either mode is RawInput \u2192 (pre-encryption `\\\\.\\pipe\\orion_input` | ViGEm XUSB fallback) \u2192 OrionStream \u2192 PS5 (remote_play_client.py:459-491; OrionAppController.cpp:1536-1543); nothing in the tree injects into a console being played directly. Consequences: (a) selecting the decoder source costs no new session and no new hardware \u2014 it only removes CHIAKI_ORION_DISABLE_VIDEO, i.e. asks the already-running OrionStream to actually decode; (b) the two genuine capture-card-only affordances are the pre-connect live preview (OrionAppController.cpp:5506 `if (!isCaptureCardSource(config_.data())) return; // decoder-pipe source: no pre-connect preview is possible (needs Chiaki)`) and warm-sidecar Connect reuse (SidecarWatchdog.h:211-216, which requires BOTH sides capture_card), so a decoder user gets a slower Connect and no preflight video check (FirstRunTour.qml:95 `capturePass: captureCardSource ? (orion.captureDeviceList.length > 0) : true`).",
      "refutes_premise": "It resolves the brief's open question in the decoder's favour: this is not a Remote-Play-users-only feature. But it also removes the framing that the decoder path is 'free because the stream is already present' \u2014 the stream is present, the DECODE is not, and enabling it is what adds the H.264 decode + pipe + BT.709 conversion cost (chiaki_backend.py:1265-1272, measured 6.91\u21923.95 ms/frame after the scratch-reuse fix in 968b127f).",
      "ships_broken": false,
      "severity": "minor",
      "fix": "No code change required for correctness. If the decoder path is ever promoted to default for card-less users, add a decoder-aware preflight step (FirstRunTour.qml:94-99 currently passes trivially when the source is not capture_card, so a card-less user gets no video check at all) and consider a decoder equivalent of the warm-sidecar Connect reuse."
    },
    {
      "title": "The one decoder hypothesis worth a live batch is VARIANCE, not mean latency \u2014 pooled within-session sd 7.2 ms vs 22.6 ms, worth ~+21 ms of feasibility window if real",
      "evidence": "Pooled within-session sd of accepted labels (logs/orion_native.log, sessions with n>=3): decoder 7.2 ms (n=6, session 06:30:41Z) vs capture_card 22.6 ms (n=56 across 6 sessions: 06:28:41 sd 45.2, 07:10:55 sd 7.4, 10:17:04 sd 12.9, 12:29:44 sd 21.6, 14:11:29 sd 24.7). Mechanism is plausible and not circular: the Remote Play video path is a fixed encoder\u2192jitter-buffer\u2192decoder pipeline whose frames are stamped at the decode callback in QPC (chiaki_backend.py:1825-1833, `capture_timestamp_ns = producer_monotonic_ns`), whereas the capture-card stamp is a cv2.read()-return time whose scheduling/USB/DirectShow jitter the CadenceLock PLL only partly removes (capture_card_backend.py:96-105 documents exactly this). Leverage: with `combinedSigma = sqrt((0.20\u00b7H)\u00b2 + leadSd\u00b2 + tickSigma\u00b2) <= 65` (AutomationEngine.cpp:9599-9609, tickSigma 4.8 at :9595), leadSd 28.7 gives H_max 290.6 and a feasibility window H_max \u2212 lead = 72.1 ms (reproducing docs/LATENCY_FACTORY_PRIOR.md:124-127 exactly); leadSd 10 gives H_max 320.2 and, at the measured decoder lead of 227.0, a window of 93.2 ms \u2014 +29%. NOT ESTABLISHED: the decoder sd is single-session (n=6, 66 s), one capture-card session also achieved sd 7.4 (07:10:55, n=10), and single-session sd systematically understates between-session spread.",
      "refutes_premise": "It replaces the refuted 150 ms mean-latency story with a smaller, mechanistically-grounded, testable one. The gain is ~21 ms of feasibility window, not ~150 ms of horizon.",
      "fix": "The A/B that would settle it, and what MUST be instrumented first. (1) PRE-WORK, non-negotiable: fix the builder attribution bug above and rebuild models/latency_factory_prior.json, otherwise the two arms differ in their SEED as well as their video and the result is uninterpretable \u2014 decoder-pipe currently carries evidence_n=0 with a mean copied from capture-card (models/latency_factory_prior.json, profile \"decoder-pipe\"). (2) DESIGN: interleave the source every N shots inside one physical sitting, never A-block-then-B-block \u2014 docs/LATENCY_FACTORY_PRIOR.md:58-65 documents an 11 h regime shift with Welch t\u22484.9, i.e. between-session drift is LARGER than the effect being measured; today's log already happens to be interleaved and that is why it is readable. (3) PIN THE CONFOUNDS: one fixed bandwidth preset for the whole run (Performance 720p60/12 Mbps, not the persisted Balanced 4 Mbps) and one fixed reader \u2014 either both arms on SimpleMeterReader, or run the decoder arm twice (with and without ORION_COMPRESSED_READER=1) so 'source' and 'reader' are never confounded. (4) ENDPOINTS: primary = pooled WITHIN-session sd of accepted latency labels per arm (that is the quantity the sigma gate consumes), plus aborts per physical shot epoch and greens per released shot. Do NOT use mean total_ms as the primary \u2014 it is already answered and flat. (5) POWER: for the sd comparison, >=25 accepted labels per arm across >=3 sessions per arm; for a P(green) 0.60\u21920.70 comparison, ~350 shots per arm. Today's decoder evidence is 8 labels over ~150 s and cannot answer either. (6) READ-OUT HYGIENE: report frame_age_ms and decode_latency_ms per arm but NEVER difference them across arms \u2014 the stamp semantics differ (decoder = chiaki decode-callback QPC, which excludes pipe+read+YUV\u2192BGR; capture card = cadence-locked read-return, which includes the whole USB path) so a cross-arm difference of those columns is meaningless. (7) TRIPWIRE: abort and re-run any decoder block in which `OrionFramePipeBackend: rejected wire frame` count > 0 or `stall watchdog: no unique frame` appears \u2014 a silent 100%-reject latch would make the decoder arm look catastrophic for reasons unrelated to the question (that is precisely what happened at 05:22:15Z, see the pts-ratchet finding).",
      "severity": "minor",
      "ships_broken": false
    },
    {
      "title": "The decoder feed's 100%-reject freeze was fixed six hours ago and has ~90 seconds of live soak; the fix is also fresh enough that any A/B run now is testing a one-day-old code path",
      "evidence": "The first decoder session today died: from 2026-08-04T05:22:15.255Z `stall watchdog: no unique frame for 250ms -> meter_present=false (was holding)`, then `OrionFramePipeBackend: rejected wire frame reason=pts_backlog` at counts 240 / 360 / 600 / 720 while chiaki kept decoding at 60/s, ending in `Capture health: tier=decoder uniqfps=0 export_fps=1 cv_fps=20 source_skip=457`. Root cause and fix are commit 968b127f (2026-08-04 01:02:19 -0500, an ancestor of HEAD): a `min()`-floored `queued_ms = arrival_offset - pts_arrival_floor` ratchet against a nominal 60.000 fps PTS counter on a real 59.9454 source, drifting 0.910 ms/s until it crossed the 50 ms budget at 42-55 s, after which every frame was rejected forever. Note `pts_backlog` no longer exists anywhere in the tree \u2014 the identical ratchet had a SECOND copy in remote_play_orchestrator (`decoder_pts_backlog`), both removed. Post-fix live evidence is exactly the 06:30:44Z-06:31:44Z window plus the 12 s 07:09Z window \u2014 about 90 seconds. The capture-card path was never exposed to this class of bug (its CadenceLock PLL absorbs the 59.94-vs-60.00 offset). Related, still live: every decoder teardown logs `producer lease revoked reason=expected_process_missing` \u2192 `producer identity unavailable`, which correctly drops route attestation via `_guard_decoder_latency_route` (remote_play_orchestrator.py:2858-2905) \u2014 fail-closed and correct, but it means any pipe flap costs the session its timing route scope, and the only tolerance is a 0.25 s startup race window (chiaki_backend.py:1328-1336).",
      "refutes_premise": "No \u2014 this is the substantive 'what is unhardened' answer the brief asked for, in place of the two stale ones.",
      "ships_broken": false,
      "severity": "major",
      "fix": "No new patch needed \u2014 968b127f is landed and its revert-trace is stated in the commit body (old gate first rejects at frame 3156 / t=52.6 s then 2244 of 2244; new gate 0 rejects over 90 s of simulated drift). What is needed is SOAK, not code: before any decoder A/B, run at least three uninterrupted decoder sessions of >=10 minutes each and assert zero `rejected wire frame` lines and zero `stall watchdog: no unique frame` lines. The freeze took 42-55 s to appear, so every existing decoder session is long enough to have caught the old bug but far too short to establish the new gate is stable."
    },
    {
      "title": "PLAUSIBLE shipping gap: the production Nuitka bundle hand-lists three modules for inclusion and chiaki_backend is not one of them",
      "evidence": "scripts/build_orion_sidecar.ps1:100-103 passes `--include-package=cv2 --include-module=decoder_pipe_identity --include-module=latency_estimator --include-module=tip_registration_infer`. `chiaki_backend` \u2014 which owns OrionFramePipeBackend, the ONLY authoritative video source in a production Remote Play launch \u2014 is absent, and it is imported late and function-locally: `from chiaki_backend import OrionFramePipeBackend` at remote_play_orchestrator.py:2637. That is the same import shape as `decoder_pipe_identity`, which someone evidently had to add explicitly (chiaki_backend imports it for ProducerExpectation / WindowsNamedPipeServerIdentityApi). tools/sidecar_bundle_manifest.py:49-53 documents the underlying hazard in its own words: \"The sidecar's imports are deliberately feature-gated and often occur inside functions, which makes a hand-maintained import list easy to under-specify.\" If Nuitka does not follow it, the failure is silent-then-fatal in exactly the shipped configuration: production sets `requireRemotePlayFramePipe` (SidecarLaunchPolicy.h:18-22, productionBuild && !captureCardSource) so `_frame_pipe_required` is true, the import lands in the `except Exception` at remote_play_orchestrator.py:2661-2664, and the orchestrator returns False with window fallback disabled \u2014 a card-less production user gets no video at all, while the dev rig (which runs from source) works perfectly. I could not confirm or refute this: no built bundle exists under this tree (`find -name OrionSidecar.exe` returns nothing) and building one was out of scope.",
      "refutes_premise": "No.",
      "ships_broken": true,
      "severity": "major",
      "fix": "Cheap and safe regardless of whether the bug is real: add `--include-module=chiaki_backend` (and, for symmetry with the reader selector, `--include-module=simple_meter_reader --include-module=compressed_meter_reader --include-module=capture_card_backend`) to scripts/build_orion_sidecar.ps1:100-103. Nuitka ignores a redundant include. To verify the current state instead, build the bundle and run `OrionSidecar.exe` with ORION_REQUIRE_FRAME_PIPE=1 and no pipe present: the log must say `Decoder frame-pipe backend unavailable`, not `Decoder frame-pipe backend error (No module named 'chiaki_backend'\u2026)`."
    }
  ],
  "confidence_notes": "WHAT I ESTABLISHED SOLIDLY. The latency comparison is the strongest result and I verified the instrument end to end rather than trusting the log field: `lat_total = t_star - _pending_release_ms` (latency_estimator.py:1556); t_star comes from the fill series stamped with the frame's capture-epoch time (remote_play_orchestrator.py:4853); `_pending_release_ms` is the PC epoch stamp of the release command (RemotePlaySession.cpp:1082-1087, remote_play_orchestrator.py:5318). I specifically checked the failure mode that would have invalidated it \u2014 if the release stamp had ALSO been a frame-derived time, the video-path latency would cancel and the label would be blind to exactly the thing under test. It is not; the two stamps are in different domains by design, and the code comment at remote_play_orchestrator.py:5316-5320 says so explicitly (\"decoder video returns over the private Remote Play path while capture-card video returns over HDMI. Keep the oracle in its directly measured TOTAL domain on both routes\").\n\nWHAT I COULD NOT ESTABLISH. (1) Whether the decoder's lower latency variance is real. n=6 labels from a single 66-second session against n=56 from six capture-card sessions is not a fair variance comparison, and one capture-card session matched it (sd 7.4). Settling it needs >=25 decoder labels across >=3 separate sessions \u2014 that is the batch I would actually spend. (2) Whether chiaki_backend survives the Nuitka bundle. No built OrionSidecar.exe exists in this tree; the finding is a shape-match against three modules that DID need explicit inclusion, not a reproduction. Building the bundle and running it with ORION_REQUIRE_FRAME_PIPE=1 settles it in one command. (3) Live detection quality on the decoder path at the rung level. The reencode_ladder report is offline x264 at a ~15 fps dump cadence, and the only live decoder detection evidence is ~150 seconds total \u2014 enough to show the reader locks and labels, nowhere near enough for a recall or fill-accuracy claim. (4) I did not verify the deployed OrionStream.exe (dated 2026-08-02) matches the chiaki-ng-src working tree I read (dirty, last touched 2026-07-31), so my claims about the fork's decode-callback export describe the SOURCE; a byte-level match to the shipped binary was not attempted.\n\nRESIDUAL BIAS I CHOSE NOT TO CORRECT FOR, and its direction. The two arms stamp frames at different points in their own pipelines: the decoder stamps at chiaki's decode callback (chiaki_backend.py:1825-1827, `capture_timestamp_ns = producer_monotonic_ns`), which excludes the pipe transfer, Python read and YUV->BGR conversion (~4-10 ms, carried separately as decode_latency_ms); the capture card stamps at cadence-locked cv2.read()-return (capture_card_backend.py:1152-1157) with the device-latency correction disabled by default. So the decoder's total_ms is biased LOW by a few ms relative to the card's. The measured 7.5 ms decoder advantage is therefore an UPPER bound on the real \"when can the detector act\" difference \u2014 correcting for it would push the difference toward zero, which strengthens rather than weakens the refutation.\n\nTHINGS IN THE BRIEF I CHECKED AND COULD NOT SOURCE. The rMAD-by-horizon table (6.5 / 15.7 / 23.9 / 45.9) appears nowhere in the repository \u2014 a case-insensitive grep for \"rmad\" across all .py/.md/.cpp/.json matches only numpy's `hermadd`. The \"Elgato inferred at ~120-195 ms\" figure likewise has no in-repo source; the only Elgato latency number in the tree is 35.0 ms (capture_card_backend.py:110), and it is an estimate that is not even applied by default. I did NOT re-test the standing-DirectShow-queue claim; I accepted the brief's statement that it was tested and is false."
}

---

## agent `a34b7263b4adb333d`

{
  "headline": "The brief's discriminator does not discriminate \u2014 all 51 fires and all 9 aborts have a max in-rise fed-to-fed gap of \u226450ms \u2014 and the largest real coverage loss is not the fed tier at all: 22.4% of 36,480 real-meter fed frames across 10 sessions sit outside the reader's search band, which is what strands the diving meter that produced epoch 54's hole.",
  "findings": [
    {
      "title": "REFUTED: \"max fed-to-fed gap during the rise\" separates nothing \u2014 5 of the 9 aborts had flawless gap-free detection",
      "evidence": "Substrate = the CSV the brief named, logs/diagnostics/detframes_20260804_074153.csv (42,229 rows, wall_ms 1785846588877 = 2026-08-04T12:29:48.877Z), joined to logs/orion_native.log by UTC. 51 \"Scheduled fire:\" and 9 \"Shot automation aborted:\" lines in-window \u2014 matches the brief. Segmenting each event's rise (walk back through fed=1 rows, stop on a >900ms fed gap or a >8pp backward fill rise): FIRES n=51 max fed-to-fed gap = [17 x46, 18, 33, 33, 34, 50] \u2014 max 50ms, not 166ms. ABORTS n=9 max gap = 33,33,17,17,17,17,17,17,17 \u2014 max 33ms. Four aborts are raw-clean: pe=21 t+201635 (rows 10917-10926: first visible frame reads 14.63 at conf 1.000, then 16.62/19.63/21.97/24.53/27.35/30.51/33.26/38.83 at a dead-flat 16-17ms cadence, 0.153 pp/ms, straight through the abort instant); pe=41 t+338927 identical (14.11 -> 40.70, 0.167 pp/ms); pe=82 t+578762 (27.48 -> 85.28, 0.182 pp/ms); pe=85 t+587028. pe=6's \"233ms coast\" is rows 7851-7863, t+150256..150455, with fill FLAT at 50.43 \u2014 not during a rise. Only epoch 54's 167ms hole survives (rows 21496-21506, t+378308 -> 378475, fill 20.91 -> 48.14, meter_memory x8 + held_reseat x1, box x 322->264 at 6-14 px/frame). NOT CIRCULAR: every quantity is the reader's own wall_ms vs its own fill_pct; no tip_eta_ms / command_eta_ms / lead_ms term enters, and the engine side contributes only a timestamp.",
      "fix": "Drop \"fed-gap during the rise\" as the gating metric \u2014 it cannot be the abort predictor because it is 17ms for 6 of the 9 aborts. Do not spend effort on pe=5/21/41/82/85: for those the detector delivered a perfect 60Hz rise and the deadline was missed downstream (AutomationEngine, contended \u2014 out of scope here). Re-scope \"detector-implicated\" to pe=1 (false lock), pe=6 (frozen fed, below), pe=54 (band-stranded coast, below): 3 of 9, not 9 of 9.",
      "ships_broken": false,
      "severity": "major",
      "refutes_premise": "Refutes \"THE DISCRIMINATOR: max fed-to-fed gap during the rise. Fires: median 17ms, max 166ms. Epochs 6 and 54: 233ms and 167ms. That is coverage, not noise.\" Fires do reach 300-602ms gaps but only across the inter-shot dead time; inside a rise their max is 50ms and the aborts' max is 33ms. Also refutes the epoch-6 attribution: its 233ms gap is a flat-fill coast, not a rise hole."
    },
    {
      "title": "green_hold re-emits a byte-frozen fill as a RAW-ACCEPTED fresh sample, with no cap \u2014 this, not a gap, is what killed epoch 6",
      "evidence": "simple_meter_reader.py:5442-5449: `green_hold=False; if subpix <= 0.5 and (evidence==\"green\" or (self._occl and locked and self.last_fill > 2.0)): subpix, coarse = self.last_fill, self.last_coarse; tbox = self.last_tbox...; green_hold = True; self._held_run += 1`. The read path's single return, simple_meter_reader.py:5792-5795, labels EVERY frame `\"rejection_reason\": \"green_not_found\"` \u2014 green_hold included. remote_play_orchestrator.py:4490-4502 `_is_raw_accepted` returns True for `('', 'green_not_found')`, so a held fill feeds the oracle / tip-registration / forecaster as FRESH \u2014 precisely what the `_should_feed_engine` docstring at 4475-4481 says the sampler tier exists to prevent (\"synthetic/held fill can never look fresh to a fire path\"). `_held_run` has NO cap: its only consumer is the `_occl_wide` launder at 5471, which additionally requires `self.last_fill - subpix > 20.0`, i.e. a DOWNWARD step, so an upward-hiding freeze never trips it. Census over 8 sessions / 32,124 fed frames: 121 runs of >=4 fed frames with byte-identical (fill_pct, x, y) = 685 frames = 2.13% of all fed; longest 334ms / 21 frames; 86 of the 121 start below 70% fill. The named session's epoch 6: rows 7864-7879, t+150472..150722, fill EXACTLY 50.43 and box EXACTLY (848,306) for 16 consecutive fed frames at conf 1.000, while green_center_pct changed inside the run (94.9 -> 95.3 -> 93.9, proving the frames were not duplicates); the next fed frames are 36.40 then 74.95, so the freeze concealed a real ~50->75 move. That epoch aborted live_trajectory_timeout_abort at t+151784. Honest limit: only 3 of 117 freezes measurably concealed >8pp (median post-freeze step 0.1pp) \u2014 the mechanism is uncapped, the observed damage is narrow.",
      "fix": "Two-file, additive, zero real-meter frames lost. (1) simple_meter_reader.py:5792-5795 \u2014 replace `\"rejection_reason\": \"green_not_found\",` with `\"rejection_reason\": (\"green_hold\" if green_hold else \"green_not_found\"),`. (2) remote_play_orchestrator.py:4487-4488 \u2014 `_should_feed_engine`: `return getattr(result,'rejection_reason','') in ('', 'green_not_found', 'green_hold', 'fill_gated', 'dead_reckoned')`; leave `_is_raw_accepted` (4501-4502) UNCHANGED. Net: a held frame still feeds the engine (no starvation, no new abort surface) but is marked stale_or_memory and can never seed the oracle or a fire path. (3) Cap the hold: add `self._held_max = _inum('ORION_READER_HELD_MAX', 6)` beside 1907, and at 5443 guard `if subpix <= 0.5 and self._held_run < self._held_max and (...)` so an over-long hold falls through to the ordinary coast (meter_memory) instead of re-emitting. Measured cost of the cap at 6 frames: 95 of 32,124 fed frames (0.296%) change tier; at 4 frames, 201 (0.626%). Real-meter frames retained: 100% \u2014 no frame stops being fed, only stops being called fresh.",
      "ships_broken": false,
      "severity": "major",
      "refutes_premise": "Refutes the brief's epoch-6 story (\"a 233ms coast, then 16 consecutive frames frozen at exactly 50.43\") in its causal ordering: the 16 frozen frames are not a consequence of the coast, they are fed=1 at conf 1.00 and they are the defect; the 233ms coast preceded them with the fill genuinely flat."
    },
    {
      "title": "The real coverage boundary is the search BAND: 22.37% of real-meter fed frames lie outside it, and that is what stranded epoch 54",
      "evidence": "simple_meter_reader.py:919 `BAND = (5, 250, 1815, 770)` px @1080p, i.e. normalized x 0.0026..0.9453, y 0.2315..0.7130 (scaled by sx/sy at :1967, so the SAME normalized rectangle at any resolution). Across 10 detframes sessions spanning both 1280x720 and 1920x1080, 36,480 fed frames: 8,162 (22.37%) fall at least partly outside it \u2014 floor below y1 in 5,142 (14.10%), top above y0 in 2,388 (6.55%), right past x1 in 667 (1.83%). Floor/frame_h p95=0.7852, p99=0.8625, p99.9=1.0000, max=1.0000. Top/frame_h p1=0.0736, p5=0.1236. The `_vzoom_down` widen (:1526-1527, default ON, cap 260px@1080p) only reaches 0.9537*H, leaving 110 fed frames (0.35%) below even the widened bottom. A cold acquire scans `_band_eff()` only; `_tracking_bounds()` (:2164-2178) hands back the FULL frame only when `_bounds_relaxed()` (:2184-2189) is true = `_courtwide_lock` OR (`self.box` AND `_shot_armed_hw` AND `_gameplay_lock_authorized`) \u2014 otherwise the relocate ROI is clipped back to the band and a miss becomes `meter_memory`. PIXEL CORROBORATION (the only un-overwritten frames available): framedump session_20260804_091119 run-A files f02703..f02710_1_raw.png contain an unmistakable meter column under the reader's OWN production band `_RED_LO/_RED_HI = (0,0,220),(60,60,255)` (:942) \u2014 w 16-17, h 30-51, red fill fraction 0.92-1.00 \u2014 translating x 405->542 and y 690->480 at 18-27 px per 33ms dump (~9-13 px/frame), with its FLOOR at y 696-740 in a 720-high frame = 0.967-1.000*H, past the band bottom AND past the vzoom_down cap. The meter was there, in full production-band red, while the reader could not clip its ROI to reach it.",
      "fix": "(a) simple_meter_reader.py:1527 \u2014 `_vzoom_down_max_px` default 260 -> 320. Justification is the measured floor p99.9 = 1.0000*H: 770/1080 + 320/1080 = 1.0130, clamped to H by the existing `min(float(self.H), ...)` at :2203, so the widened bottom reaches the frame edge and no further. It is scaled by `sy` so the behaviour is identical at 720p (bottom 513 -> 726, clamped 720) and at 1080p, and at any future resolution. This is a WIDEN of a live-lock-gated bottom (`_band_down_widen` returns unchanged with no live lock and no dived floor), so no cold/off-shot decor area is admitted. (b) OPTIONAL, verify first: extend `_bounds_relaxed()` (:2184-2189) with `or (self.box is not None and self._fresh_lock_streak >= 8)` so a lock that has earned 8 consecutive fresh red reads keeps a full-frame CLIP for its already-pad-bounded ROI without needing `_shot_armed_hw and _gameplay_lock_authorized`. I did not verify `_fresh_lock_streak`'s reset semantics \u2014 check before shipping. (c) Do NOT widen BAND itself: the right trim exists to kill the x 1846-1902 banner and the top trim the score/clock red digits (:3681-3689 docstring), and 1.83%/6.55% of frames do not justify re-admitting them globally.",
      "ships_broken": true,
      "severity": "blocker",
      "refutes_premise": "Refines the brief's epoch-54 diagnosis. The hole is real, but the pixel evidence says the failure mode is the ROI's band CLIP under a diving meter, not motion smear of the red column \u2014 the column's red fraction was 0.92-1.00, i.e. pristine. Enabling the ORION_READER_ROBUST blur widening would not have reached it."
    },
    {
      "title": "REFUTED: no height, width, or aspect floor separates the 9x77 false lock from real meters \u2014 the false lock sits at the 5th percentile of REAL frames",
      "evidence": "Normalized fed box height h/frame_h over 36,924 fed frames, 11 sessions, both resolutions: min 0.0250, p0.1 0.0389, p1 0.0759, p5 0.1065, median 0.1500, p95 0.1653, max 0.2130. The 9x77 false lock is 77/720 = 0.1069 \u2014 the 5th percentile of REAL fed frames. Normalized width w/frame_w: p1 0.00703 (= 9.0 px @1280), p5 0.00937, median 0.01328 \u2014 the false lock's w=9 is again exactly p1. Aspect h/w: false lock 77/9 = 8.56 vs real p95 8.31 and max 50.67 \u2014 no separation at all. A floor placed to exclude 0.1069 excludes >=5% of real-meter frames; at a different camera distance the same corpus already spans 0.025-0.213 normalized height, so the overlap widens, and at a different resolution the px thresholds must be normalized anyway (h/frame_h) or the gate silently changes meaning. What DOES separate cleanly is inter-frame physics at the SEAT. Over 31,781 consecutive fed pairs (same w,h, dt<=40ms, 8 sessions), |dfill/dt| p99 = 0.436 pp/ms, p99.9 = 4.31; the 9x77's two seat frames are 5.357 and 5.343 pp/ms \u2014 the two largest values in the corpus (rows 5337-5340: fill 14.29 -> 100.00 -> 100.00 -> 9.17). Box-centre step p99 = 10.8 px/frame, p99.9 = 24.0; the 9x77 hits 221 px/frame (largest of 31,781; box y 326->279->239->213->141 with w,h pinned at 9x77). Classifying 234 lock instances as REAL (peak>=85% reached over >=250ms) vs OTHER: a seat veto of |dfill/dt| > 1.0 pp/ms OR box-centre step > 0.055*H per frame, evaluated only over the FIRST 4 fed frames of a fresh seat, vetoes 0 of 195 REAL (real max 0.817 pp/ms and 0.0115*H) and 8 of 39 OTHER including the 9x77. REAL-METER INSTANCE RETENTION: 195/195 = 100.0%, with 1.22x margin on slope and 4.8x on box step. NOT CIRCULAR: both terms are the reader's own output vs its own wall clock; neither derives from lead_ms/eta.",
      "fix": "Do not add a size gate. Add a SEAT-PHYSICS VETO in simple_meter_reader.py, distinct from the rise-rate clamp that was correctly refused: (1) on a fresh seat, latch `self._seat_fill0/_seat_ts0/_seat_cxy0` and a frame counter; (2) on seat frames 2-4 only, if `abs(fill - prev_fill)/dt_ms > 1.0` OR `max(|dcx|,|dcy|)/dt_ms*16.7 > 0.055*self.H`, drop the seat exactly as the CONF_MIN path does (`self.conf = 0.0; self.box = None; self.tmpl = None`) and return the ordinary miss result with `rejection_reason='seat_nonphysical'` so the real meter can acquire 2-4 frames later; (3) never evaluate it after frame 4, never modify a published fill, never touch an established lock. Thresholds: 1.0 pp/ms is 2.3x the all-pairs p99 and 1.22x the worst REAL seat; 0.055*H is 40px@720p / 60px@1080p and 4.8x the worst REAL seat, and being a fraction of frame height it is resolution-independent by construction. Expose as ORION_READER_SEAT_PHYSICS (default ON) with the two numbers as _fnum envs so a regression run can A/B them.",
      "ships_broken": false,
      "severity": "major",
      "refutes_premise": "Directly refutes the brief's own hypothesis: \"Height looks like a stronger identity check than width (fires 108-127; the false lock 77...)\". Within the single named session that is true; across all 11 available sessions, normalized, h=0.1069 is the 5th percentile of real detections and w=9px is the 1st. The brief's own warning about a fixed pixel floor turns out to apply to the normalized floor too."
    },
    {
      "title": "CONFIRMED: ORION_READER_ROBUST never runs in production, and the entire sampler tier is dead \u2014 but enabling it would not have fixed either named epoch",
      "evidence": "simple_meter_reader.py:1060 `self._robust = _os.environ.get('ORION_READER_ROBUST','0')...` \u2014 default OFF. run_orion.local.ps1:218 sets ORION_SIMPLE_READER=1 and never sets ORION_READER_ROBUST; a repo-wide grep finds it set to '1' only in tests/test_reader_robust.py, tests/test_color_calibrator.py:395,417, scratch/occl_probe.py:29 and scratch/span_probe.py:20, and pinned to '0' in tools/diagnostics/replay_simple_reader.py:42. So :3714-3716 (`blur_on = self._robust and bvx > 3.0`, `w_extra = int(bvx) if self._robust else 0`, `rf_min = max(0.30, 0.45-0.02*bvx) if self._robust else None`) is dead in the shipped path. The dead-reckoned coast bridge at :5389 (`if (self._robust and armed and self._dr_frames < 10 and self._fit_provider is not None)` plus `fs.get(\"n\",0) >= 8`) is behind the same flag: rejection_reason 'dead_reckoned' occurs ZERO times in the named 42,229-row session, and so does 'fill_gated' \u2014 the complete census is gameplay_ineligible 27219, green_not_found 8739, roi_not_found 3381, meter_memory 2495, no_cap_anchor 348, shot_candidate_unverified 29, held_reseat 7, occl_relock 6, static_fake_lock 3, strip_truncated 2. Both sampler-tier reasons in `_should_feed_engine` (remote_play_orchestrator.py:4487-4488) are therefore unreachable in production, exactly as the brief states.",
      "fix": "Do NOT flip ORION_READER_ROBUST \u2014 it also gates the `_dims_rebased` scan-gate rebase at :3006-3013 and the armed slow-decay at :5291-5297, neither of which is A/B'd. If the blur widening is wanted later, split it out per docs/DETECTION_PLAN.md N5 into `self._blur_relocate = _flag('ORION_READER_BLUR_RELOCATE','1')` used only at :3714-3716 \u2014 it is confined to `_relocate`, which is already position-bounded, band-clipped and colour-gated. But rank it BELOW the band fix: the one hole I could verify at the pixel level (f02703-f02710) had red fraction 0.92-1.00, so column smear was not its failure mode. Leaving `dead_reckoned`/`fill_gated` unreachable is fine and arguably correct \u2014 a synthetic fill bridging a hole is exactly the kind of \"decision arriving on faith\" the hard constraint forbids; the green_hold retier in finding 2 gives the engine continuity without inventing a value.",
      "ships_broken": true,
      "severity": "minor",
      "refutes_premise": ""
    },
    {
      "title": "Systematic ~83ms of every rise is never observed \u2014 the single largest uniform coverage tax, but unproven at the pixel level",
      "evidence": "Over 210 lock instances with a physically-plausible rise (>=8 fed frames, first-6-sample rate <=0.40 pp/ms) across 6 sessions: entry fill (the fill on the FIRST fed frame of the instance) median 14.1%, p10 ~7.9, p90 17.7, max 29.9; implied acquisition lag (entry_fill / local rise rate) p10 49ms, median 83ms, p90 158ms, max 1026ms; 21.6 seconds of rise time unobserved over 210 shots. In every case the immediately preceding frames are `roi_not_found` with bbox (0,0,0,0) \u2014 the reader scanned and found nothing (e.g. rows 10911-10916 at t+201386..201469, then 14.63 at 201486). The code's own floors bracket the observed values: H_ACQ=33 and H_ACQ_ARMED=15 @1080p (:921-922, = 22px and 10px at 720p), STUB_H_MIN=10 (:983, 7px), STUB_H_MIN_EARLY=4 (:1002, 3px); the measured red-column height at acquisition is 7.1px for w=14 boxes but 17.1px for w=23 boxes on the SAME ~115px track, i.e. a WIDER (closer) meter is acquired LATER, which no pure pixel-height floor predicts. But the width regression is weak (pearson r(box_w, red_h_at_acq) = 0.197, OLS slope 0.459 vs the 0.70 that AR_MIN_ARMED would predict), so I could not pin a single mechanism.",
      "fix": "Do not act on the 83ms number yet \u2014 instrument it first. The acquire ladder already computes `acq_tier` and `acq_floor` (remote_play_orchestrator/simple_meter_reader :5106-5107, set to scan_learned / scan_default / popin_stub / courtwide:* / early_stub with the floor it enforced). Add both to the detframes CSV writer alongside the existing columns; one live session then answers, per acquisition, which tier seated the lock and at what pixel floor, and whether the 14-18% entry cluster is a tier-ordering artefact or a genuine render delay. That is a diagnostic-only change with zero production behaviour risk.",
      "ships_broken": false,
      "severity": "major",
      "refutes_premise": ""
    }
  ],
  "confidence_notes": "WHAT I COULD NOT ESTABLISH. (1) Whether the meter is actually rendered during the ~83ms pre-acquisition window. \\\"Implied lag\\\" = entry_fill / local rise rate, which assumes the meter renders from fill 0 and rises linearly back to the first observed sample. I could not check this at the pixel level: BOTH framedump sessions are two runs merged. session_20260804_032333 is the one the brief already flagged; session_20260804_091119 has the same defect (filename is f{idx:05d}_{det} per remote_play_orchestrator.py:4553-4554 \u2014 run A wrote 09:12:56-09:13:38, run B 09:15:12-09:19:21 over the SAME idx range), and worse, run B's det=0 files OVERWROTE run A's det=0 files, so exactly the pre-acquisition frames I needed no longer exist. What would settle it: one framedump run with ORION_FRAMEDUMP_INTERVAL=0 and a per-session idx namespace, or simply adding a wall_ms/frame_no field to the dump filename. (2) Whether the band fix actually closes epoch 54. I proved the meter was 0.967-1.000*H (past both the band bottom and the vzoom_down cap) in f02703-f02710 of the 091503 session, which is the same failure shape, but 074153 has no framedump at all so epoch 54 itself cannot be replayed. A before/after needs a live run. (3) My framedump-to-CSV alignment cross-correlates the binary det sequences and matches only 73.3% (dumps drop when the writer is behind), so I deliberately did not rest any claim on which CSV row a given dump corresponds to \u2014 the f02703-f02710 finding is self-contained: a meter-shaped, 0.92-1.00-pure red column under the reader's own production band, below the band bottom, in 8 consecutive frames. (4) The seat-physics retention number (195/195 real instances retained) uses \\\"reached >=85% fill over >=250ms\\\" as the real-meter label because only 51 of the ~234 instances have a fire to anchor on; a mislabelled instance would move the OTHER-bucket veto rate (8/39) but cannot move the REAL retention, since every one of the 195 is comfortably inside both thresholds (worst 0.817 pp/ms vs 1.0; worst 0.0115*H vs 0.055*H). (5) I did not run the build or the gates \u2014 per trap #2 a green gate run would be no evidence about production anyway, since replay_gates and replay_simple_reader.py:37-46 construct the reader without require_gameplay_eligibility/armed_hw, and `_bounds_relaxed()` (simple_meter_reader.py:2184-2189) reads `_shot_armed_hw` and `_gameplay_lock_authorized` directly, so the band-clip path in finding 3 is one the gates structurally cannot exercise. (6) I wrote no repo files; AutomationEngine.cpp/.h/Tests were read-only throughout and simple_meter_reader.py was not touched \u2014 all reader changes are emitted as diffs above."
}

---

## agent `a90f9937870f23bae`

{
  "headline": "The tip is predictable to rMAD 8.7 ms from ONE animation-phase constant \u2014 1.9x tighter than the shipped predictor's 16.1 ms \u2014 and it drops in mean-neutrally; everything else (fusion re-weighting, sigma honesty, peak_fill/travel_pp tuning) is noise or actively misleading, because the reader's fill saturates 3.5 pp below its own target and the system is currently on-aim only because a +62 ms lead bias cancels a measured +61.0 ms predictor bias.",
  "findings": [
    {
      "title": "BIGGEST WIN: an animation-phase lookup beats the sampler crossing 1.9x on rMAD at the same horizon, with zero mean shift and no model",
      "evidence": "Measured on logs/diagnostics/detframes_20260804_074153.csv + _091503.csv, paired to the 81 release markers after logs/orion_native.log:43496. Restricted to the NON-CIRCULAR subset fill_at_release>=40 (n=54), where the meter's stop time is set by the animation, not by our own command \u2014 proven by regressing plateau fill on fill_at_release: slope -0.154 +-0.142 for far>=40 (flat/saturated, command-independent) vs +1.193 +-0.177 for far<40 (command-set). Phase predictor = 'time from the first crossing of fill=30% to the meter stop': median 318 ms, rMAD 8.7 ms, sd 13.0, p10-p90 301-331. Identical across shot types (Standstill n=32 -> 318, Left n=10 -> 318, Right n=7 -> 318). Same anchor, fill=25%: 346 ms rMAD 8.9; fill=40%: 262 ms rMAD 8.4 \u2014 i.e. horizon-FLAT, unlike the sampler. Shipped predictor on the same 54 shots: (crossingEta at submit) - (measured stop) = median +61.0, rMAD 16.1, sd 28.3. Non-circularity: the phase constant differences two detector-clock frame times (the 30% crossing and the stop); it never touches lead_ms, tip_eta_ms, command_eta_ms, peak_fill or fill_at_release, so it cannot be moved by the 300 ms lead knob. Arithmetic check that it reproduces today's aim exactly: t30->release measured -84 ms; with constant 318+61=379 and lead 303 the fire lands at t30+76 (observed t30+84, 8 ms scheduling slop); with constant 318 and ORION_LEAD_BIAS_MS=0 (lead 241) it lands at t30+77 \u2014 the two forms agree to 1 ms.",
      "fix": "In AutomationEngine::canonicalAutonomousTipDecision (native_orion/src/AutomationEngine.cpp:9391) add a third predictor member alongside samplerFit and regTipAbsMs: record shot_.fillPhaseAnchorMs = the capture-clock time the shot's fill first crossed config_.tipPhaseAnchorPct (30.0) \u2014 the crossing is already available from sampler_ samples; reset it in the same place shot_.peakFillPct is reset (AutomationEngine.cpp:2392). Then decision.phaseTipAbsMs = shot_.fillPhaseAnchorMs + config_.tipPhaseConstantMs, with sigma = a fixed 13.0 ms (measured, horizon-flat). Ship it as the primary member when the anchor exists and the fill has passed the anchor, keeping the existing sampler path as fallback. SET config_.tipPhaseConstantMs = 379.0 (318 + the measured +61.0 bias) and leave ORION_LEAD_BIAS_MS=62 untouched, so the change is mean-neutral BY CONSTRUCTION and only the spread moves \u2014 that satisfies the 'no lead re-tune' constraint exactly. Learn the constant per session as the running median of (stop - anchor) over the last 10 landings (standard error of the median at n=10 with rMAD 8.7 is ~3.4 ms). Do NOT ship the de-biased fusion instead: reg_sigma median is 95.0 vs sampler 18.4 at the firing horizon, reg_weight median 0.0371, and the w_reg*139.6 re-aim is a 70-105 ms mean shift that has to be exactly right \u2014 this gets a larger variance reduction with a 0 ms mean shift.",
      "refutes_premise": "Refutes 'the single largest available reduction is the de-biased fusion (p90 51.5 -> 34.9)'. The phase lookup gets rMAD 16.1 -> 8.7 (p10-p90 band 30 ms wide) with no re-aim and no second model. It also refutes the framing that the predictor error is 'a steep function of HORIZON' \u2014 the error is a function of the sampler's EXTRAPOLATION, not of horizon: the same anchor evaluated at 346/318/262 ms horizons has rMAD 8.9/8.7/8.4.",
      "severity": "blocker",
      "ships_broken": false
    },
    {
      "title": "The system is on-aim only because ORION_LEAD_BIAS_MS=62 cancels a measured +61.0 ms predictor bias \u2014 1 ms apart, by coincidence. A default (no-env) build fires ~61 ms LATE for its first 12 shots",
      "evidence": "run_orion.local.ps1:117 sets $env:ORION_LEAD_BIAS_MS = \"62\"; :107 sets ORION_LEAD_FLOOR_MS=290. AutomationEngine.h:770/783 ship autonomousLeadFloorMs = 0.0 and autonomousLeadBiasMs = 0.0 \u2014 the +62 exists ONLY in the dev launcher. Measured predictor bias on the non-circular subset (n=54): crossingEta at submit minus measured meter stop = +61.0 ms median. So lead = authority + 62 and the predictor over-reads by 61.0: they cancel to 1 ms, which is why the landing sits exactly on the reader's saturation point (plateau fill median 96.47 vs the visible green-cap bottom at 96.40 \u2014 0.07 pp apart). On a build with no env vars and no user lead, measuredLeadForActuationMs (AutomationEngine.cpp:9077-9114) returns the raw authority: this rig's authority walked 206.6-272.1 (release-marker l_fixed), median ~235, so the command lands ~61 ms after the meter stop = late. It self-corrects only after actuationLeadSeedMinSamples=12 landings (AutomationEngine.h:808) install the travel_pp/vel median (~303) \u2014 and that seed is only 'right' because travel_pp/vel is inflated by almost exactly the predictor's bias (see next finding).",
      "fix": "Never change one of these without the other. Concretely: (a) if you adopt the phase predictor with constant 379 (finding 1), keep ORION_LEAD_BIAS_MS=62 \u2014 mean unchanged; (b) if you instead want to remove the +62, you must simultaneously set the phase constant to 318 (or, without the phase predictor, enable AutomationEngine.h:904 samplerHorizonDebiasEnabled with a de-bias that removes +61 at the commit horizon). Add a hard guard so this cannot be half-done: in AutomationEngine::applyConfig near line 1423, if config_.autonomousLeadBiasMs > 0 and config_.samplerHorizonDebiasEnabled, emit a startup diagnostic naming both, since the two corrections are for the SAME physical error and applying both double-counts it by ~61 ms.",
      "refutes_premise": "Refutes the AutomationEngine.cpp:9435-9447 rationale for shipping the horizon de-bias OFF ('enabling it blind would add a mean EARLY shift to a system already firing early'). The system is not firing early relative to its own aim: it lands within 1 ms of the meter stop in the median. The real hazard is the opposite \u2014 the de-bias and ORION_LEAD_BIAS_MS both correct the same +61 ms, so enabling the de-bias while the env bias is set would fire ~61 ms early.",
      "severity": "blocker",
      "ships_broken": true
    },
    {
      "title": "The Shot Lead slider is DEAD on this rig \u2014 every lead sweep the user has run through the UI this session changed nothing, while the log said it applied",
      "evidence": "run_orion.local.ps1:107/:117 set ORION_LEAD_FLOOR_MS and ORION_LEAD_BIAS_MS; AutomationEngine.cpp:1399-1423 turns that into config_.leadOverrideFromEnv = true; AutomationEngine.cpp:9094 then SKIPS the userActuationLeadMs branch entirely ('if (!config_.leadOverrideFromEnv && ...)'). Log proof: 'Shot lead set to 300 ms (your value; measurement will not change it)' at 2026-08-04T12:56:20.263Z (emitted by OrionAppController.cpp:6636), settings.json actuation_lead_ms=300 actuation_lead_user_set=true \u2014 and the next session (14:11 onward) ran every reservation at lead_ms=314.378, not 300. userActuationLeadMs is wired (AutomationEngine.cpp:1396) and 300 is inside [150,450] (AppConfig.h:345-346), so the value is valid and simply discarded. Production has no env vars, so in production the slider IS live and lead is a constant 300.000 \u2014 a different and MORE STABLE lead than the dev rig's 290.0-334.9 walk. Every timing conclusion tuned on this rig via the slider is void and does not transfer.",
      "fix": "Tonight, before any other change: comment out run_orion.local.ps1:107 ($env:ORION_LEAD_FLOOR_MS = \"290\") and :117 ($env:ORION_LEAD_BIAS_MS = \"62\"). With settings.json actuation_lead_ms=300 / user_set=true this makes measuredLeadForActuationMs return exactly 300.000 on every shot (mean moves -2.8 ms vs today's 302.8 median \u2014 negligible) and makes the slider live for A/B. Fail-closed is untouched: the user branch sits inside the 'authority exists' guard at AutomationEngine.cpp:9077-9080. Separately, make the log honest: OrionAppController.cpp:6636 must not print 'your value' when config_.leadOverrideFromEnv is set \u2014 add the flag to the controller's view and print 'ignored: ORION_LEAD_* override active' instead.",
      "refutes_premise": "",
      "severity": "blocker",
      "ships_broken": true
    },
    {
      "title": "peak_fill is the MAXIMUM of a noisy 1-second plateau, not a landing; travel_pp/velocity over-reads the true lead by ~70 ms, and the launcher's 'the lead was NOT inflated -- it was right' is false",
      "evidence": "AutomationEngine.cpp:2166 keeps meterCapPeakFillPct_ = max over every accepted post-release sample; the capture window runs to postReleaseMaxWindowMs. Reconstructing each shot's fill trace from detframes: logged peak_fill EQUALS the plateau's maximum within 0.02 pp on 79 of 80 shots, and over-reads the plateau's own median by +2.52 pp (median) \u2014 worth +13.5 ms at vel 0.183. The divisor is worse: AutomationEngine.cpp:10936 sets meterCapRiseVelocityPctPerMs_ = sampler_.velocityPctPerMs() at SUBMIT (fill~42, median 0.1827), while the meter's slope over the interval actually travelled is 0.2230 (median, fitted over the last 140 ms of each rise) \u2014 ratio 1.225, p10 1.104, p90 1.354. Net: travel_pp/vel = 308.0 ms median vs a directly measured submit->meter-stop of 233.6 ms (n=74, robust plateau-crossing estimator; the conservative first-plateau-sample estimator gives 249.0). lead_ms at release exceeded the measured stop time on 72 of 73 shots, median excess +67.8 ms. run_orion.local.ps1:86-88 asserts 'travel_pp / velocity = 302.4ms median (p10 283, p90 320) against a configured 300-306, so the lead was NOT inflated -- it was right' \u2014 that number is (inflated peak)/(under-read velocity) and reproduces the correct DIAL value only because the velocity under-read is the same defect that makes the predictor read +61 ms late.",
      "fix": "AutomationEngine.cpp (contended \u2014 patch, do not apply here). (1) At line 11052, move `recordLandingLeadSample(peak, meterCapFillAtReleasePct_);` to AFTER the settle gate at line 11060 and pass the settled-run median instead: `recordLandingLeadSample(fill, meterCapFillAtReleasePct_);` (fill is computed at 11100). That removes the +13.5 ms extreme-value inflation. (2) The velocity term cannot be fixed by choosing a different existing divisor \u2014 the honest quantity is the elapsed TIME from submit to the meter stop, which the engine can measure directly: in evaluatePostReleaseMeter, scan meterCapSamples_ for the first sample whose fill is within 0.5 pp of the settled median and take (s.tMs - releaseSubmitMs). Replace the whole travel_pp/velocity computation at AutomationEngine.cpp:10972 with that. (3) Until (2) lands, gate the seed: change the emit at AutomationEngine.cpp:11007 to fire only when a new config flag `actuationLeadSeedFromLandingEnabled` (default FALSE) is set, so a fresh install does not silently install a 70 ms-inflated lead. Also update the comment block at 11116-11117 which states 'divided by the meter velocity this is a direct read of the TRUE lead' \u2014 it is not.",
      "refutes_premise": "Refutes the memory note 'travel_pp is the only validated lead measure' and the launcher's step-3 conclusion. travel_pp does track the DIAL (302/295/284) because the dial is what moves fill_at_release; it does not track the LATENCY, which is 234 ms.",
      "severity": "blocker",
      "ships_broken": false
    },
    {
      "title": "ANSWER TO Q1: the early/late question is not answerable from any log metric, because the reader's fill scale saturates ~3.5 pp BELOW the engine's own 100% target \u2014 the green cap hides the top of the track",
      "evidence": "simple_meter_reader.py:4202 sets cand = float(ph - track_top) with track_top = the green cap's APEX, so fillable_h spans cap-top -> floor; simple_meter_reader.py:4418 computes subpix = (ph - sub_top)/fillable_h*100 where sub_top is the top of the VISIBLE RED, which cannot rise above the cap's bottom edge. The reader's own comment at 4192-4196 already states the green make-window descends with the red. Measured consequence: on the frozen frames the visible green_center median is 98.20 (=> cap bottom at 96.40) and the plateau fill median is 96.47 \u2014 the two coincide to 0.07 pp. Fill histogram over 11,249 detected frames in one session: bins 96 and 97 hold 30.3% of all frames, then 98 -> 4.13%, 99 -> 1.25%, 100 -> 0.59% (a hard shoulder, not a distribution). Max plateau across 81 shots = 99.19; exactly 1 shot >= 99. Regression of plateau fill on fill_at_release: +1.193 +-0.177 for far<40 (instrument LIVE, releases land below saturation) and -0.154 +-0.142 for far>=40 (instrument DEAD). And simple_meter_reader.py:4421-4425 documents that the [0,100] clamp makes 'no shot ever overshot 100' a tautology. So the engine aims at a fill value its own reader can never report, and every metric that references 100 or the green window measures the reader's ceiling.",
      "fix": "You cannot fix this by picking a better fill metric; the top 3.5 pp are not observable. Two things to do. (a) SMALLEST EXPERIMENT THAT SETTLES EARLY-VS-LATE (~5 min, 36 shots): after killing the env override (finding 3), run three blocks of 12 shots at Shot Lead 300 / 240 / 180 and record the game's TIMING banner by hand for each shot. At lead 180 the landing sits ~22 pp below saturation, i.e. squarely inside the LIVE regime, so dP/d(lead) is directly measurable from detframes there and can be extrapolated; the banner anchors which side of green the 300 block is on. Nothing else in the repo can answer this. (b) Make the saturation visible: remote_play_orchestrator.py:617 is the detframes header \u2014 append the reader's existing pre-clamp diagnostics (_green_band_raw, _green_band_bound from simple_meter_reader.py:3842-3844, plus the ORION_READER_FILL_OVERFLOW pre-clamp fill) as new trailing columns, so 'the fill hit its ceiling' stops being invisible.",
      "refutes_premise": "Refutes 'Median peak_fill is 98.17 against a target of 100 with 10 of 51 shots below 96' as evidence of an early bias \u2014 peak_fill is right-censored at the reader's ~96.5 ceiling AND is an extreme-value statistic. Also refutes 'settled_fill has a flat response': settled_fill IS the plateau (median |settled_fill - plateau median| = 0.08 pp over 80 shots) and its response is +1.193 pp/pp in the unsaturated regime. The response is not flat; the instrument is clipped, and every past lead sweep was run entirely inside the clipped region.",
      "severity": "blocker",
      "ships_broken": false
    },
    {
      "title": "The lead authority walks 206.6 -> 272.1 ms WITHIN a session while every latency observation is rejected (accepted=0, n=0) \u2014 the bot aims differently shot to shot for no evidential reason",
      "evidence": "Release-marker l_fixed over the two batches: session 0 210.2 -> 206.6 -> 220.0 -> 222.0 -> 237.4 -> 242.2 -> 237.6 -> 233.9 -> 232.5 -> 230.9 -> 227.1; session 1 210.2 -> 272.1 -> 264.7 -> 251.7 -> 239.0 -> 236.1 -> 241.7 -> 238.1. Every paired 'latency observation' line reads accepted=0 status=rejected_near_cap reject=near_cap n=0 corr=0 validated=0. The effective lead followed it: lead_ms at release spanned 290.000 (the ORION_LEAD_FLOOR_MS=290 rail, 132 reservations) to 334.926, sd 9.6 ms, p10 290.0, p90 314.4 across the 73 paired shots. That is a 24 ms p10-p90 aim swing with zero accepted evidence behind it, and it is a pure consistency loss: it is uncorrelated with anything the shot can compensate for.",
      "fix": "Removing run_orion.local.ps1:107 and :117 (finding 3) fixes this outright for this rig \u2014 the user branch at AutomationEngine.cpp:9094-9099 returns a constant 300.000. That is the highest-value single action available tonight. For the shipped path, the walk is upstream in latency_estimator.py (fixed_ms moving while every observation is rejected near_cap); at minimum add a rate limit so measuredLatencyAuthorityMs_ cannot move more than a few ms per session without an ACCEPTED observation, mirroring the reasoning already written at AutomationEngine.h:747-753.",
      "refutes_premise": "",
      "severity": "major",
      "ships_broken": false
    },
    {
      "title": "ANSWER TO Q3: making the sampler sigma honest costs a lot of fire rate at the current cap, and it silently re-weights the fusion as a side effect",
      "evidence": "AutomationEngine.h:739 autonomousTipMaxCombinedSigmaMs = 75.0. Measured over 583 reservations in the batch: 10.1% are invalid, and 100% of those are the combined-sigma gate. In the FIRING band (tip_eta 250-340 ms, n=121): median combined sigma 21.7, and 14.0% already exceed 75 \u2014 but every one of those is registration-sourced (reg_sigma median 95.0); among valid=1 decisions at the firing horizon the gate binds on 0.0%. Applying the honesty factor k=3.1 implied by the brief's 24.4-vs-75.8 numbers to the sampler side: firing-band failures 14.0% -> 41.3%, all-reservation failures 10.1% -> 59.9%. To hold today's 10.1% refusal rate the cap must move 75 -> ~196 ms. Second-order effect nobody has costed: reg_weight = smpVar/(smpVar+regVar), so inflating ONLY the sampler sigma by 3.1 lifts reg_weight from 0.0371 (measured median at the firing horizon; the brief's 0.0201-0.0288 is confirmed) to ~0.27 \u2014 i.e. it quietly implements 7x of the fusion re-weighting using the un-de-biased registration member that was explicitly rejected.",
      "fix": "Do NOT make the sampler sigma honest as a standalone change. If finding 1 lands, the question disappears: the phase member's sigma is a measured, horizon-flat 13.0 ms, which is honest AND smaller than today's dishonest 19.6, so combined sigma falls and the 75 ms cap gains headroom instead of losing it. If you must correct the sampler sigma first, move autonomousTipMaxCombinedSigmaMs (AutomationEngine.h:739) to 196.0 in the SAME commit and add an explicit reg-weight clamp in canonicalAutonomousTipDecision (AutomationEngine.cpp:9542-9545) so the fusion weighting does not move as a side effect of a sigma change.",
      "refutes_premise": "Partially refutes 'inflating combinedSigma against the fixed 75 gate would COST fire rate' as stated \u2014 at the firing horizon among VALID decisions the gate currently binds on 0%, and the 14% that do fail there are registration-sourced, not sampler-sourced. The cost is real but it lands on long-horizon reservations, not on the shot.",
      "severity": "major",
      "ships_broken": false
    },
    {
      "title": "The green window on settled frames is occlusion-contaminated and moves with the fill, so settledInGreen is partly tautological",
      "evidence": "simple_meter_reader.py:3831-3832: _band_raw = (cap_bot - cap_top + 1)/fillable_h*100; band = min(20.0, max(2.0, _band_raw)) \u2014 cap_bot is the bottom of the VISIBLE green, which rises as the red covers it. Measured: unoccluded green_center during the early rise (fill 15-65) is 95.30 median; on the frozen plateau it is 98.20 \u2014 a +2.30 pp shift, and plateau green_center regressed on plateau fill gives slope +0.134, r 0.488, n 47. So green_start and the fill it is compared against are the same measurement seen twice. Of 50 graded landings in session 0, 30 (60%) report green_start = 98.00, the exact 2.0 pp FLOOR rail; green_end is 100.00 on 50 of 50 by construction (simple_meter_reader.py:3846 'g_end = 100.0' is hardcoded, not measured). This is the mechanism behind the 'flat response' recorded at run_orion.local.ps1:100-107: raising the lead raises the fill, which raises green_start, which cancels most of the gap.",
      "fix": "Do not grade against green_start on settled frames. In AutomationEngine::evaluatePostReleaseMeter (AutomationEngine.cpp:11101-11103) the gs/ge/gc medians are taken over the SETTLED run; take them instead from the pre-release samples with fill in [15,65] (the unoccluded window), which meterCapSamples_ does not currently cover \u2014 so either extend the capture to start at shot arm rather than at release, or carry the last unoccluded green window forward on shot_ and use that. Until then, treat every 'landed in green' verdict in the log as unproven.",
      "refutes_premise": "Refines '82% of green windows are clamp artifacts (simple_meter_reader.py:3807)': the clamp line is 3832 in the current tree, the floor-rail rate in this batch is 60% (30/50), and the clamp is a symptom \u2014 the underlying defect is that the band measures the UNCOVERED remainder of the cap, i.e. it is a function of the fill.",
      "severity": "major",
      "ships_broken": false
    },
    {
      "title": "The stated mechanism for the sampler's late bias is backwards \u2014 the meter ACCELERATES, it does not ease in",
      "evidence": "AutomationEngine.cpp:9404-9406: 'The weighted-linear crossing runs LATE by ~0.22ms per ms of horizon because the meter eases IN'. Measured segment slopes over 69 shots: 25%->50% = 0.185 pp/ms, 50%->75% = 0.214 pp/ms, and the last 140 ms of rise = 0.223 pp/ms (median). A decelerating (easing-in) meter would make a linear extrapolation fire EARLY, not late; the observed lateness is caused by a fit over the low-fill window under-reading the future rate, ratio slope_top/vel_submit = 1.225 (p10 1.104, p90 1.354). The sign and magnitude of the +0.22 coefficient survive; the causal story does not \u2014 and the story matters, because the SPREAD of that ratio (+-11% around 1.225) is itself worth +-26 ms of tip error at a 300 ms horizon, which is the dominant remaining term and is not addressable by any de-bias constant.",
      "fix": "Correct the comment at AutomationEngine.cpp:9404-9406 and the companion note on AutomationEngine.h:905 samplerHorizonBiasMsPerMs to name the real mechanism (accelerating rise; local fit under-reads the future slope). This also settles the pivot argument at AutomationEngine.cpp:9428-9447: because the bias is multiplicative in the velocity ratio rather than additive in horizon, no fixed k*(h - h_ref) removes it exactly, which is a further argument for replacing the extrapolation entirely with the phase lookup in finding 1.",
      "refutes_premise": "Refutes the mechanism (not the sign) recorded in the code and repeated in the brief.",
      "severity": "minor",
      "ships_broken": false
    }
  ],
  "confidence_notes": "WHAT I COULD NOT ESTABLISH. (1) Whether the shot is actually EARLY or LATE in the GAME's terms. I proved the reader's fill saturates ~3.5 pp below the engine's own 100% target (green cap over the track top), so no log-derived quantity \u2014 peak_fill, settled_fill, f_stop, travel_pp, green gap \u2014 can answer it, and none ever will while the aim point sits above the ceiling. The only instrument left is the human reading the TIMING banner. The 36-shot, 3-block sweep in finding 5 settles it; nothing offline does. This is a HARD negative for the metrics, a SOFT one for the question. (2) The +61.0 ms predictor bias is measured against the meter STOP (= the reader's saturation point), not against the true tip. If the true tip is N ms past saturation, every 'bias' figure here shifts by N, but every DIFFERENCE (the 1 ms cancellation, the 8.7-vs-16.1 rMAD, the mean-neutral phase constant of 379) is unaffected, because they all reference the same anchor. (3) My direct martingale test of the predictor's horizon drift is too thin to report: reservation lines are only emitted on >=8 ms deadline moves and the shot fires at eta ~= lead, so only 6 of 82 shot groups had a short-horizon update to anchor on (n=13 at 300-400 ms, sampler bias +29.3 ms, rMAD 47). The +61.0 figure comes from the submit-time crossingEta vs the measured stop on 54 shots, which is the sounder route. (4) One rig, one game mode, two sessions, one meter skin. The phase constant agreed to 0 ms across three shot types (318/318/318), which is encouraging, but 318 ms is a property of THIS animation at THIS resolution and must be learned per session, not hard-coded. (5) I did not build or run any test \u2014 this is read-only analysis of logs/orion_native.log (lines >43496; note the batch is 81 releases across TWO sessions, 12:29-12:39Z and 14:11-14:15Z, not the 51 the brief describes) plus detframes_20260804_074153.csv and _091503.csv. Every patch in the findings is emitted, not applied; AutomationEngine.cpp/.h/AutomationEngineTests.cpp were read only. (6) Two figures in the brief I could confirm: reg_weight 2-3% (measured median 0.0371 at the firing horizon) and lead_sd 6.0 / tick 4.8. One I could not reproduce: the 24.4-vs-75.8 sampler-sigma coverage numbers appear nowhere in the repo or the logs, so my Q3 trade table is parameterised by an assumed honesty multiplier k rather than by that pair."
}

---

## agent `aeceda21b41f2486a`

## What I implemented (all revert-traced)

**1. Sub-720p / non-16:9 capture failed silently on both shipping video sources** — `remote_play_orchestrator.py`
The contract at `_detector_frame_contract_reason` (:85-113) correctly fails closed, but the only log line was gated on `self._frame_backend is None` (:4233) — the *window-grab fallback*. Capture-card and decoder-pipe both set a backend (:2506, :2585, :2616, :2653, :2688), so on both production sources the reason died in `_frame_integrity_counts` with **zero** output. This is worse than the finding stated ("dies in a counter"): it was total silence. Added `_note_frame_contract_diagnosis()` naming measured geometry, requirement and consequence, announced once per `(reason, W, H)` so a renegotiated source re-announces but a 60 fps reject loop can't spam.
Revert-trace: `assert "1024x576" in text` → `AssertionError: assert '1024x576' in ''`.

**2. Armed shot after shot with no meter, and nothing says why** — `remote_play_orchestrator.py`
Added `_note_physical_arm_meter_health()` on the `_arm_shot_gate` edge (:1594): after 6 consecutive physically-armed shots with zero meter reads, one line names the real requirement (in-game meter must be RED "Arrow 2"). Debounced at 2.5 s so pose/square/stick refreshes within one shot count once.
Revert-trace: `assert len(msgs) == 1` → `assert 0 == 1` — six armed shots previously produced nothing.

**3. Meter Color / Meter Style pickers advertised configurability that does not exist** — `MeterConfigPanel.qml`
Both combos set read-only with a caption stating the in-game requirement. Validated at runtime: `productionQmlTreeInstantiatesOnTheSupportedQtRuntime()` loads this file from the source tree and **passes** with the edit; qmllint 0 errors.

**4. `learning.<slug>.json` escaped the release filter** — `tools/release_filter_policy.py`
`settings.json`/`learning.json` are already forbidden, but `AppConfig::learningPath()` (`AppConfig.cpp:164`) writes `learning.<slug>.json` for any non-Default profile — which carries `learned_latency_ms` (load-bearing for lead seeding). Added `_PROFILE_LEARNING_RE`.
Revert-trace: `AssertionError: learning.aaron-ps5.json`.

## Findings I was handed that are false — not implemented

- **"Selecting Purple makes the detector permanently blind; the bot never fires."** Refuted empirically. All four colours give **byte-identical** detection (`(True, 30.94, (900,464,24,139))` … across four fill fractions). `meter_color` reaches only `ensure_calibrator()` (:2913) and a passthrough label (:5964); masks are the class constants `_RED_LO/_RED_HI`. Two independent pins force Red anyway: `remote_play_orchestrator.py:1293-1297` (park) and `update_meter` (:1762-1764) which *rejects* live non-Red changes. The picker is inert, not dangerous — which changes the fix from "per-colour envelopes + replay gate per colour" to "stop implying it's a lever." Style is equally inert: `set_active_style` (:6024) only stores a string.
- **"Packaging copies every .json; a stray settings.json would ship the dev's console IP."** False — `settings.json`, `settings.json.sig`, `learning.json` are already in `FORBIDDEN_PATH_COMPONENTS` (`release_filter_policy.py:34,46,47`) and `should_copy_top_file` checks forbidden **first**. Only the slugged variant was real.
- **"chiaki_backend is absent from the Nuitka bundle."** False — `build/sidecar/autogreen_sidecar.build/module.chiaki_backend.c` exists; Nuitka followed the function-local import. So did `simple_meter_reader`, `compressed_meter_reader`, `capture_card_backend`, `luma_meter`, `stream_quality`. No fix needed.
- **Capture device index** — confirmed portable (name-gated candidates → cached index → brightness scan, `capture_card_backend.py:806-830`). Nothing to fix.

## Reported, not applied (C++ relink impossible)

`OrionNative.exe` (PID 23796) is live — a relink fails LNK1104 and I won't kill the user's app.
1. `RemotePlaySession.cpp:1696` probes `.venv311`; **only `.venv` exists**. Masked solely by the launcher's `ORION_PYREMOTEPLAY_PYTHON`. Add `.venv` as a candidate — the cv2 probe already guards correctness. Dev-only (production compiles past `pythonExecutable()`).
2. `RemotePlaySession.cpp:2042-2045` reaps `/IM python.exe`; the production sidecar image is `OrionSidecar.exe`, so it can never match.
3. Program Files writability (`main.cpp:284-285` roots all mutable state at `applicationDirPath`) — a genuine first-run blocker, but a 4-file C++ change touching the settings-signature lock. Not something to land unbuilt and unverified.

## Baselines

| Suite | Before | After |
|---|---|---|
| pytest | 1279 passed, 0 failed | **1288 passed, 0 failed** (+9 mine) |
| OrionNativeTests.exe | 424 / 0 / 5 | **424 / 0 / 5** (no C++ touched) |
| ctest | 8/9 | **8/9** |

Two caveats worth your attention. The brief's baselines (1275 pytest, 422 native) were stale — other agents added tests concurrently. And `ctest` was **already** 8/9 before I started: `OrionPreviewPresentationTests::shotLeadCardShowsTheLeadInForceNotTheBandMidpoint` fails on `text(pill).contains("ORION_LEAD")`, which belongs to another agent's in-flight `ShotLeadCard.qml` / `OrionAppController` work against a stale `native_orion/build`. I proved it isn't mine by swapping the baseline `MeterConfigPanel.qml` back in and re-running: identical `32 passed, 1 failed`.

One thing to flag: `MeterConfigPanel.qml` is **co-owned** right now — another agent has uncommitted network-card changes in it. My temporary swap during that revert-trace put their work at risk for ~40 s. I verified their footer/NetStat changes are intact (596 lines vs 558 at HEAD, both change sets present), but that was sharper than it needed to be and I wouldn't repeat it on a contended file.

---

## agent `a2fc6fc2743380f23`

All changes are in and verified. Final state.

## What I verified before implementing (the audit's central claim holds)

`rttTargetVerified` is unreachable in **every** build — I re-traced it end to end rather than trusting the brief:

- `RemotePlaySession.cpp:3948` — `courtRttReady = ready && targetVerified && filtered > 0 && courtAddress.isGlobal()`; `:3951` is the only non-false assignment of `telemetry_.rttTargetVerified`.
- `rtt_sync_engine.py:738-741` — `target_verified` requires `_is_public_ip_addr(court_ip)`.
- `court_ip` has exactly two inlets: `rtt_sync_engine.py:590` (`observe_packet`) and `:647` (`set_target_ip`).
  - Inlet 1's only feeder is `OrionAppController.cpp:3199`, inside `packet_bridge_authority::forwardToTimingIfAuthorized`. `PacketBridgeAuthority.h:22-29` returns an unconditional `false` under `ORION_PRODUCTION_BUILD`; the dev branch reads `ORION_DEV_ALLOW_UNVERIFIED_BRIDGE_TIMING`, which a whole-repo grep finds at **one** site — the read itself.
  - Inlet 2's only feeder is `RemotePlaySession::setCourtIp` (`RemotePlaySession.cpp:1111`), which has **zero** callers (`clearCourtTarget` at `:1183` likewise).

So RTT/JITTER were two cells that could never fill, for any user, in any build — and the card's prose promised they would.

## Implemented (5 changes, each revert-traced)

| # | Change | File | Assertion that fails without it |
|---|---|---|---|
| 1 | Deleted the RTT and JITTER tiles | `native_orion/qml/components/MeterConfigPanel.qml:425-454` | `test_network_card_never_binds_an_unreachable_court_round_trip` → `assert "orion.rttMs" not in bindings` |
| 2 | Killed three false strings; retargeted the footer | same file, `:471-515`, `:541`, `:552` | `test_network_card_copy_promises_no_court_measurement` → `assert '"Turn on to fill these in"' not in source` |
| 3 | Startup log line stating court RTT is not measured | `native_orion/src/OrionAppController.cpp:3228-3257` | `test_controller_states_in_the_log_that_court_rtt_is_absent` → `assert source.count("Court round trip: not measured.") == 2` |
| 4 | Diagnostics bundle prints absence, not `0.0` | `native_orion/src/OrionAppController.cpp:4809-4832` | `test_diagnostics_bundle_prints_absence_not_a_confident_zero` → `assert "court_rtt_ms=" in network_block` |
| 5 | `RTT tick` log names its ping target + hop class | `rtt_sync_engine.py:884-910` | `test_rtt_tick_line_names_the_host_it_actually_pinged` → `assert "target=%s" in tick` |

Revert-trace was run for real: with the three source files reset to `git show HEAD` and the tests kept, **4 of 5 failed**; after restoring, 11/11 pass. Change 4 was traced separately (2 failed on HEAD content, 11 pass restored).

Details worth flagging:

- **Change 2** — the retired strings were `"Round trip and jitter appear once the court server itself answers"` (no code path sends a probe to the court, so it can never answer), `"Turn on to fill these in"` (discovery fills the address and the two counters, never RTT), and `"...identify the court server and measure the real round trip"` (the sniffed packets reach `PassiveCourtFlowClassifier`, never an RTT estimator). The footer now covers the blank that **is** reachable and temporary — no court endpoint identified yet — because `diagnosticCourtIp` genuinely populates once a match runs. The refusal itself survives, moved into the COURT IP InfoTip and the footer tip, because "we will not print your LAN hop as court latency" is the load-bearing statement.
- **Change 4** — `network.txt` printed `rtt_ms=0.0 jitter_ms=0.0` (both hard-zero unless verified). A support reader sees a flawless 0 ms court round trip. Now `court_rtt_ms=not-measured`, `court_jitter_ms=not-measured`, and the surviving raw figure is renamed `sampler_rtt_raw_ms=… hop=local` so it can never be quoted as session latency. No parser consumes `network.txt` (grep across `.py/.cpp/.md/.js`) — the rename is safe.
- **Change 5** — `RTT tick: raw=0.4ms … n=931 court=-` was technically honest but a sub-millisecond RTT with no named target reads as a spectacular court latency. Now `target=192.168.137.100 hop=local court=-`. Lock ordering is unchanged: the surrounding block already holds `RTTSyncEngine._lock` and reads/writes `self._sampler.target_ip` at `:591` and `:628` under exactly the same ordering.

I also added a sixth test, `test_court_rtt_inlets_stay_structurally_unreachable`, which **passes both before and after** — deliberately. It is a tripwire, not a behaviour lock: the card is silent *because* court RTT is unreachable, so if someone makes it reachable the test fails and says to restore a tile rather than delete the test.

## Deliberately NOT implemented

- **Surfacing the Senkusha link RTT** as a replacement tile. It is honest and measured (0.367–0.419 ms, n=6), but it is a one-shot setup number (`senkusha.c:385`, `session.c:683`), it needs new log-parsing machinery, and adding a field is the opposite of what the honest outcome called for. Three real values beat four with one that needs a paragraph of caveat.
- **Gating/removing the RTT ping loop.** I confirmed the audit's premise: its only consumer is `remote_play_orchestrator.py:5128-5130`, `_trusted_rtt = ready and target_verified`, so `update_network_offset` is never called. But the audit's own remedy ("gate on court lock") equals deleting it outright, which trades the only console-reachability heartbeat in the log for an unmeasured jitter benefit I cannot validate live. That is a performance change, not an honesty one — reporting, not shipping.
- **Deleting `setCourtIp`/`clearCourtTarget`/`observePacket` and the sidecar handlers.** Removing `observePacket`'s call site also removes the authority fence that is currently doing the work. I took the audit's stated alternative (the self-explaining log line) plus the tripwire test.
- **`wifiMode_` is permanently false** (`AutomationEngine.cpp:2562-2568`, fed by `networkAutomationJitter()` which is `rttTargetVerified ? jitterMs : 0.0`, i.e. always `0.0`, against a `4.0` threshold). `wifiRttDeltaClampMs` and all six `wifiFreshnessFactor` sites are dormant. **Required patch, not applied** — `AutomationEngine.*` is off-limits.

## Suite state

- **Python:** `1296 passed, 3 failed, 26 skipped, 6 xfailed, 1 xpassed`. The 3 failures are another agent's in-flight `seat_nonphysical` seat-physics veto in `simple_meter_reader.py` (`assert 'seat_nonphysical' == 'green_not_found'`) — a file I never touched. `tests/test_rtt_sync_engine.py`: 16/16 pass.
- **OrionNativeTests.exe:** `424 passed, 0 failed, 5 skipped` (brief baseline 422/0/5; +2 from another agent). This binary is stale — `OrionCommon.dll` cannot relink because the user's live `OrionNative.exe` (PID 23796) holds it (`LNK1104`), and I did not kill the user's app. It is also structurally unaffected: `native_orion/CMakeLists.txt:552-553` builds `OrionNativeTests` from `AutomationEngineTests.cpp` alone.
- **ctest:** 8/9. The single failure is `OrionPreviewPresentationTests::shotLeadCardShowsTheLeadInForceNotTheBandMidpoint` — `git show HEAD:native_orion/tests/PreviewPresentationBufferTests.cpp | grep -c` returns **0**, i.e. the test does not exist at HEAD; it belongs to another agent's in-flight ShotLeadCard work (`ShotLeadCard.qml` is dirty).
- **My C++ compiles:** `MSBuild /t:ClCompile` on `OrionAppController.cpp`, exit 0, zero diagnostics.
- **My QML loads:** the 32 *passing* tests in that same preview suite instantiate `RemotePlayPage.qml` at runtime, and `RemotePlayPage.qml:1317` embeds `MeterConfigPanel` eagerly (direct `ColumnLayout` child, no `Loader`), so `QVERIFY2(page != nullptr, errorLines)` clearing is real evidence my edit parses and instantiates. `qmllint`: **0 Errors**, total diagnostics 200 → 96 (fewer bindings).

## One thing you need to know — my mistake

To run the revert-trace I twice restored `native_orion/src/OrionAppController.cpp` by whole-file copy while another agent was actively editing it. Between my snapshot and my restore, that agent wrote `OrionAppController.h` (mtime 09:55:48, inside my window). At one point the tree had `publishActuationLeadEffective()` called at `:4519` and declared at `OrionAppController.h:1128` but **not defined** — a link break I may have caused.

The tree is coherent now: the definition and all four call sites are present, `resolveActuationLead`/`ActuationLeadResolution` resolve from `AppConfig.h:585`, and the file compiles clean. But I cannot prove I did not cost that agent a re-edit, and whoever owns the actuation-lead work should confirm nothing of theirs is missing. I should have used targeted `Edit` reverts instead of `cp`. No further whole-file writes were made.

## Files changed

- `C:\Users\aaron\Desktop\NexusVision\native_orion\qml\components\MeterConfigPanel.qml`
- `C:\Users\aaron\Desktop\NexusVision\native_orion\src\OrionAppController.cpp`
- `C:\Users\aaron\Desktop\NexusVision\rtt_sync_engine.py`
- `C:\Users\aaron\Desktop\NexusVision\tests\test_passive_court_flow_contract.py`

Nothing committed. `AutomationEngine.{cpp,h}` and `AutomationEngineTests.cpp` were read-only throughout. No fail-closed gate was weakened — the packet-bridge authority fence and the `rttTargetVerified` gating on `networkAutomationOffset()`/`rttMs()`/`jitterMs()` are untouched.

---

