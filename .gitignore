# Python/cache
__pycache__/
.ruff_cache/
*.py[cod]
*.pyo

# Wrangler
.wrangler/
*.pyd
*.egg-info/
*.egg

# Transpiled C byproducts of the root Python modules (Cython/Nuitka output, ~20MB).
# NOT build inputs (there is no setup.py/.pyx); regenerated by the ship-build step.
# Root-scoped so genuine C sources under vendor/ and chiaki-ng-src stay tracked.
/*.c

# Local virtual environments
.venv/
.venv311/
venv/
env/
ENV/

# IDE/user state
.vs/
.idea/
.vscode/
.cursor/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.sublime-*

# Build and release outputs
build/
build_release/
dist/
release/
archive/
nuitka-crash-report*.xml
*.exe
*.pkg
*.toc
*.o
*.obj
*.lib
*.a
*.so
*.dylib

# Stub build artifacts
build_stub/
build_stub_diag/

# Native CMake build artifacts
native_orion/build/
native_orion/build-regular-shot/
native_orion/build_prod/
native_orion/build_prod_codex/
native_orion/build_prod_review/
native_orion/backend/orion_sidecar_build_id.py
native_orion/CMakeFiles/
native_orion/CMakeCache.txt
native_orion/cmake_install.cmake
native_orion/Makefile
native_orion/*.vcxproj
native_orion/*.vcxproj.filters
native_orion/*.sln
native_orion/*.user

# Deploy artifacts and bundled binaries
native_orion/deploy/*.exe
native_orion/deploy/*.dll
native_orion/deploy/Qt*/
native_orion/deploy/Chiaki*/
native_orion/vendor/Qt/
native_orion/vendor/Chiaki/
native_orion/vendor/platforms/
native_orion/vendor/plugins/
native_orion/vendor/qml/

# Runtime state and logs
*.log
logs/
status.json
cv_status.json
license_cache.*
nexus.db
*.sqlite
*.sqlite3
*.mdb
release_log.json
security_events.log
shots.log
takeover_debug.log

# Secrets and vault
.vault/
*.secret
.secrets/
*.pem
*.key
*.crt
*.p12
*.pfx
credentials.json
auth_tokens.json
settings.json.local

# Local encrypted user state / signing config (kept local, never committed)
/settings.json
/settings.json.sig
/codesigning/
.claude/settings.local.json

# Local-only env / dev-key files (keys live here or in env vars, never in git)
.env
.env.*
*.local.ps1
*.local.env

# Local code-index DB (mirrors repo file content — including gitignored files — never commit)
.tokensave/

# Local scratch/measurement artifacts + ad-hoc debug dumps at repo root
/scratch/
/diagnostics/
/out*.txt
/focused_*.txt
/full_tempo*.txt
/tempo_focused*.txt
/test_output*.txt

# Vendored third-party fork with its own .git history (do not embed as a gitlink)
/vendor/chiaki-orion

# Captures/training/generated media
/training/
video_samples/
*_updated.png
*_constellation.png

# Trained meter calibration (user/environment-specific template anchor, written by
# the in-launcher Train button — like settings.json, kept local, never committed)
/calibration/

# Legacy bridge artifacts
*.gpc
*.bak_*

# Test coverage and reports
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
.tox/
testlog.txt

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.backup
*.old

# YOLO / model weights + training-run artifacts (large binaries, regenerable; not source)
*.pt
*.ckpt
*.pth
*.onnx
# The source-bound production meter detector is an explicit release input.
!models/orion_meter_detector.onnx
*.task
*.tflite
events.out.tfevents.*
/models/rfdetr_player_nano/
/models/rfdetr_player_small/
/models/fill_forecaster/
/models/meter_roi/

# ML training data + run outputs (generated, huge — never commit)
/datasets/
/runs/
/debug_frames/
/scratchpad/
tools/diagnostics/user_player_dataset/
tools/diagnostics/ring_scan_output/

# Local research/RE material — must NEVER enter git (reimplement technique, never copy)
/Starzen RE/
/tools/cronus/
/redteam_sandbox/
/redteam/
/docs/reference/2kvision_reverse_engineered.md

# Local-only launchers + agent worktrees
*.local.bat
.claude/worktrees/
datasets/meter_reader_synth_y/

# Bundled driver redistributables (downloaded at build time; do not commit binaries)
installer/redist/*.exe
installer/redist/*.msi

# Qt installer build output
installer/OrionSetup/build/

# Shelved experiment: functional WinDivert intercept/hold/reinject harness. Kept locally
# for experiments, deliberately OUT of source control -- the meter delay was removed
# because holding packets stutters the game, so a working hold tool must not ship.
/_shelf/

# Build/test artifact trees that leaked into staging during the 2026-08-04 session
native_orion/Testing/
native_orion/build_input_path/

# Side build trees (e.g. native_orion/build-timing: builds tests while the running app holds build/Release DLLs)
native_orion/build-*/
native_orion/build-*.log

# [ORION_BANNER_VERDICT_LIVE] Build artefact: scripts/build_orion_sidecar.ps1 copies
# tools/timing/panel_grade.py here for the duration of the Nuitka compile (--include-module
# needs a nameable repo-root module) and deletes it afterwards. Never commit it.
/orion_panel_grade.py

# Local verification output and dependency installs (never source inputs)
/.codex_artifacts/
/.audit_raw/
/.pytest_codex_strict*/
/Usersaaron*pytest*/
/website/node_modules/

# 2026-09-17 pre-ship audit: settings/learning/model backups carry the dev console IP + licence key
*.json.bak-*
/probe.txt
